name: Preview

on:
  pull_request:
    types: [labeled, unlabeled]

concurrency:
  group: railway-preview-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy preview workflow placeholder (intentionally)."
