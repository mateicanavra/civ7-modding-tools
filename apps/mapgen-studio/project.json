{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "mapgen-studio",
  "tags": ["kind:app"],
  "targets": {
    "dev": {
      "command": "vite",
      "dependsOn": ["serve-daemon"],
      "cache": false,
      "continuous": true,
      "options": {
        "cwd": "apps/mapgen-studio"
      }
    },
    "serve-daemon": {
      "command": "bun --conditions bun-source src/server/daemon/daemon.ts",
      "dependsOn": [
        "^build",
        {
          "projects": ["swooper-physics"],
          "target": "build"
        }
      ],
      "cache": false,
      "continuous": true,
      "options": {
        "cwd": "apps/mapgen-studio"
      }
    },
    "build": {
      "command": "node -e \"\"",
      "cache": false,
      "outputs": [],
      "dependsOn": ["build:vite"],
      "options": {
        "cwd": "apps/mapgen-studio"
      }
    },
    "typecheck": {
      "command": "tsc --noEmit",
      "dependsOn": [
        "check:test",
        "check:tools",
        "^build",
        {
          "projects": ["swooper-physics"],
          "target": "build"
        }
      ],
      "options": {
        "cwd": "apps/mapgen-studio"
      }
    },
    "check:test": {
      "command": "tsc -p tsconfig.test.json --noEmit",
      "dependsOn": [
        "^build",
        {
          "projects": ["swooper-physics"],
          "target": "build"
        }
      ],
      "options": {
        "cwd": "apps/mapgen-studio"
      }
    },
    "check:tools": {
      "command": "tsc -p tsconfig.tools.json --noEmit",
      "options": {
        "cwd": "apps/mapgen-studio"
      }
    },
    "check": {
      "executor": "nx:noop",
      "dependsOn": ["typecheck"],
      "cache": false,
      "outputs": []
    },
    "test": {
      "command": "vitest run --config ../../vitest.config.ts --project mapgen-studio",
      "dependsOn": [
        "^build",
        {
          "projects": ["swooper-physics"],
          "target": "build"
        }
      ],
      "options": {
        "cwd": "apps/mapgen-studio"
      }
    },
    "build:vite": {
      "command": "vite build",
      "cache": true,
      "dependsOn": [
        "typecheck",
        "^build",
        {
          "projects": ["swooper-physics"],
          "target": "build"
        }
      ],
      "outputs": ["{projectRoot}/dist/**"],
      "options": {
        "cwd": "apps/mapgen-studio"
      }
    }
  }
}
