# design-sync notes — mapgen-studio

Repo-specific gotchas for future re-syncs. Append a bullet whenever something
non-obvious comes up.

## Shape & wiring
- **App shape, not a published library.** `apps/mapgen-studio` is a Vite app; `package.json` has no `main`/`module`/`exports`/`types`. So this runs in **synth/source mode** with a curated `--entry` (`.design-sync/ds-entry.tsx`) that re-exports the DS surface. The curated entry is what scopes the bundle — the converter's auto-synth would `export *` from every `.tsx` under `src/` (server daemon, deck.gl workers, features) and blow up the bundle. Never drop `cfg.entry`.
- **PKG_DIR anchor.** `cfg.entry` also anchors `PKG_DIR` (the converter walks up from the entry to the nearest named `package.json` → `apps/mapgen-studio`). Without `--entry`, `PKG_DIR` would resolve to `node_modules/mapgen-studio` (nonexistent) and discovery fails.
- **Run from the package dir.** All converter commands run with cwd = `apps/mapgen-studio` so `.ds-sync/`, `ds-bundle/`, and `.design-sync/` stay package-local. `--node-modules ./node_modules` (react/react-dom live there; the repo root has none).
- **Config home = the package.** `.design-sync/` lives in `apps/mapgen-studio/`, not the repo root.

## Components
- **43 components** pinned in `cfg.componentSrcMap`: 15 shadcn primitives (`src/components/ui/`) + 7 composites + 3 panels (`src/ui/components/`) + **Batch 3 (2026-06-27)**: `PipelineStage` (`src/features/recipeDag/PipelineStage.tsx`, the recipe-DAG SVG stage view; path-derived group `recipedag`; `cardMode:single` + a wide viewport; preview drives it with a hand-authored static `RecipeDagResult` fixture — the `@civ7/studio-server/contract` type) + **Batch 1 (2026-06-27)**: `FieldRow` (`src/ui/components/fields/`, re-enabled from `null`), `LeftDock`/`RightDock`/`ErrorBanner` (`src/app/`), and `PresetErrorDialog`/`PresetSaveDialog`/`PresetConfirmDialog` (all three from `src/features/presets/PresetDialogs.tsx`) + **Batch 2 (2026-06-27)**: the rjsf cluster from `src/features/configOverrides/` — 7 widgets (`TextWidget`/`TextareaWidget`/`NumberWidget`/`SelectWidget`/`CheckboxWidget`/`SwitchWidget`/`TagSelectWidget`, all from `rjsfWidgets.tsx`) + 3 templates (`BrowserConfigFieldTemplate`/`BrowserConfigObjectFieldTemplate`/`BrowserConfigArrayFieldTemplate`, all from `rjsfTemplates.tsx`). `OptionSelect`, `StageViewTabs`, the Batch-1 `src/app` + `src/features/presets` components, and the Batch-2 rjsf components are NOT in a source barrel, hence explicit re-exports in `ds-entry.tsx`. **`FieldRow` is the exception: it IS in the `./fields` barrel (re-exported through `@/ui/components/index`), so it is surfaced via `componentSrcMap` alone — re-exporting it in `ds-entry.tsx` would be a duplicate-export collision.**
- **`SchemaConfigForm` synced in Batch 5 (2026-06-28, →46)** — the rjsf form ENGINE, `src/features/configOverrides/SchemaConfigForm.tsx`. The Batch-2-era deferral ("only makes sense against a real schema → demonstrate via a project, not the system") was **overturned by constraint relaxation**: the engine is pure prop-driven (`schema` + `value`, no oRPC/server), so a static fixture drives it exactly like Batch 3's `PipelineStage`. `@rjsf/core` + ajv8 bundle and render clean headless (the falsifier's empirical tail — passed). **`SchemaForm` stays an internal rjsf bridge, NOT a separate card** — it requires a pre-built `uiSchema`/`formContext` that only `SchemaConfigForm.buildUiSchema` produces, so a standalone `SchemaForm` fixture would be synthetic; the engine card renders `SchemaForm` internally, so the whole composition is already exercised + visible. **This closes the syncable presentational surface** — everything else under `src/features/configOverrides/` + the app is runtime/logic (the editable claude.ai/design PROJECT remains an exterior, optional follow-up).
- Composites are prop-driven/presentational but value-import a few `../../features/*` helpers (status enums, `SchemaConfigForm` (rjsf), `civ7Setup` config helpers). Those get bundled — that's their real composition.

## CSS / Tailwind v4
- Tailwind v4 CSS-first (`src/index.css`: `@import "tailwindcss"` + `@theme inline` tokens). Utilities are generated on-demand, so the **compiled** CSS is the source of truth, not `src/index.css`.
- `cfg.cssEntry = .design-sync/compiled.css` — a STABLE copy of the app's compiled stylesheet (the hashed `dist/assets/index-*.css`). The dist filename is content-hashed and changes every build, so we copy it to a stable path.
- **Re-sync risk:** `compiled.css` must be refreshed from a fresh `vite build` before re-syncing, or it goes stale vs. the components. Also, authored previews that use utility classes NOT present anywhere in the app source won't be in this CSS — if previews render unstyled, recompile Tailwind scanning `src/**` + `.design-sync/previews/**` (see Re-sync risks).
- Brand fonts (Inter, JetBrains Mono) ship in `dist/assets/*.woff2` via `@fontsource/*`; the compiled CSS carries their `@font-face`.

## Authoring learnings (folded from waves A–D + the panels)

- **shadcn `Tooltip` throws/renders BLANK without a `TooltipProvider` ancestor** — and **no error is printed** in the capture log (the cell just comes up empty). Any component that uses `Tooltip`/`TooltipTrigger`/`TooltipContent` needs a `TooltipProvider` above it. Affected previews (all fixed by wrapping the preview surface in `<TooltipProvider>` from the bundle): `ViewControls`, `WaterStatsSection`, `AppHeader`, `GameConsole`, `RecipePanel`, `ExplorePanel`. `AppFooter` self-provides one internally. This is ALSO a design-agent usage requirement → carried into `conventions.md`. (We chose per-preview wraps over a global `cfg.provider` to avoid clearing every already-`good` grade.)
- **Overlays portal their own dark `bg-popover` surface** — `Select`/`DropdownMenu`/`Popover`/`Tooltip`/`Dialog` need NO `Demo` backdrop. Author ONE open-state story (`defaultOpen` / `open`); `cardMode: single` + viewport overrides are set in config. Set `Select`'s `value=` so the open list shows the selected check/highlight.
- **Inline (non-portal) components need the dark `Demo`/`Dock` wrapper** (`bg-background text-foreground`, inline-style layout). Token utilities confirmed in the compiled CSS: `bg-background/card/popover/muted/input-background`, `text-foreground/muted-foreground`, `border-border/-subtle/-strong`, `text-data` (11px), `text-label` (10px uppercase eyebrow), `font-mono` (JetBrains), `font-medium`. For effects without a guaranteed utility, use `style={{ ...: "var(--token, fallback)" }}`.
- **Faint-by-design hairlines**: `Separator` (border-subtle) and the `ScrollArea` thumb (border-strong) nearly vanish on `bg-background` — flank them with a lighter `bg-card`/`bg-muted` panel. Vertical `Separator` needs a definite parent height; `ScrollArea` needs a fixed root height + content taller than it for the thumb to appear. `Tabs` active state is a `border-primary` underline; use `defaultValue` (not `value`) for a static-renderable selected tab.
- **Absolute-positioned composites** (`AppFooter`/`AppHeader` = `absolute top/bottom-4`; `StageViewTabs` = `absolute`) must be framed in a `relative` sized container.
- **Mocks are runtime-only** (esbuild, not typechecked) — pass just the fields read at render. `WaterStatsSection`/`ExplorePanel` water summary rows read `rowKey`/`label`/`counts` (a semantic key, not `layers`/`default`/`debug`) + `layerRefs[]` with `layerKey`/`dataTypeKey`/`label`/`presentation.categoryLabel`/`presentation.palette.activeColor`; divergence keys matching `/mismatch|reject|drift/i` with value>0 render amber. `AppHeader.setupConfig` needs `playerOptions: []` (falls back) + `savedConfig: {id, displayName}`. `RecipePanel` renders its rjsf config form when `configCollapsed={false}` with a valid RJSFSchema + matching config; `configCollapsed={true}` skips rjsf.

## Re-sync routine (one command, then grade)
1. `bash .design-sync/build-inputs.sh` (regenerates `dist/`, `dist/types/` .d.ts, and `dist/assets/_ds-compiled.css` with the dark-default + font-url rewrite). Use `DS_SKIP_VITE=1` only if `dist/` is already current.
2. Fetch the project's `_ds_sync.json` → `.design-sync/.cache/remote-sync.json`, then run the driver:
   `node .ds-sync/resync.mjs --config .design-sync/config.json --node-modules ./node_modules --entry .design-sync/ds-entry.tsx --out ./ds-bundle --remote .design-sync/.cache/remote-sync.json`
3. **Render check needs a browser**: there is NO playwright browser installed — set `DS_CHROMIUM_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"` (Homebrew `chromium` is a stale wrapper pointing at a non-existent app). The playwright JS is installed under `.ds-sync/node_modules` (`PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1`).

## Re-sync risks (watch-list)
- **Generated build inputs are gitignored** (`dist/`, `dist/types/`, `dist/assets/_ds-compiled.css`) — on a fresh clone they're absent until `build-inputs.sh` runs. The converter depends on all three (synth entry, `.d.ts` extraction, `cssEntry`).
- **`build-inputs.sh` carries two hand-rolled steps** that are easy to lose: the `url(/assets/ → ./` font rewrite (else fonts dangle), and the `.dark`-block→`:root` dark-default append (else previews/designs render light). If `src/index.css`'s `.dark` block moves or is renamed, the awk extractor (`/^\.dark[ ]*\{/`) silently no-ops → light output.
- **`vite build` standalone** (step 1) was not exercised here (the committed `dist/` was already current); if it fails outside nx, build just `dist/` + the compiled CSS some other way before the converter.
- **`componentSrcMap` + `docsMap` enumerate all 25 components by hand** — a NEW component added to `src/components/ui` or `src/ui/components` will NOT appear until added to both maps (and `ds-entry.tsx` if not in an existing barrel). Grouping relies on path segments being generic (`components`/`ui`); a component in a NON-generic subdir (like the dropped `fields/FieldRow`) takes its dir name as the group regardless of `docsMap`.
- **`FieldRow` is intentionally excluded** (`componentSrcMap.FieldRow: null`) — a trivial flex-row layout helper. Re-add to both maps to bring it back.
- **Tooltip-provider per-preview wraps** (`AppHeader`/`GameConsole`/`RecipePanel`/`ExplorePanel` previews wrap in `<TooltipProvider>`): if upstream adds/removes Tooltip usage in a component, the corresponding preview wrap may need adding/removing (silent blank cell otherwise).
- **`RecipePanel`'s form story uses a hand-written RJSFSchema mock** — if the real config-schema shape or rjsf API drifts, that mock may need updating (the `RecipeSelection` collapsed story is the safe fallback).
- **`conventions.md` names tokens/classes/components** — every name was validated against the built CSS + `components/` tree at sync time. On re-sync the conventions-header step re-validates; fix any name the build no longer carries (the pre-compiled `styles.css` only contains utilities actually used by the app + previews).
- **README token classifier patched for Tailwind v4 `--tw-*` internals** (applied 2026-06-26). `lib/emit.mjs` `emitReadme()` buckets every custom property by a name heuristic; with Tailwind v4 it misfiled the engine's internal `@property` vars (`--tw-border-style` → color, `--tw-space-y-reverse` → spacing, `--tw-font-weight` → typography, `--tw-shadow*` → shadow). Fix: classify the whole `--tw-*` namespace as `other` (one line before the heuristic: `t.startsWith('--tw-') ? 'other' : …`). This lives in the **ephemeral converter** (`.ds-sync/lib/emit.mjs`, gitignored, re-staged from the bundled skill) — so a Claude Code update that re-stages the skill **drops the patch**; re-apply it before the next `/design-sync`, or upstream it. Verify post-build: `sed -n '/^## Tokens/,/^## Components/p' ds-bundle/README.md` shows no `tw-` in color/spacing/typography/shadow.
- **Batch-1 DS-pane groups are PATH-DERIVED, not from docsMap** (2026-06-27). Components outside the generic dirs take their source dir name as the group: `app` (LeftDock/RightDock/ErrorBanner), `fields` (FieldRow), `presets` (the 3 dialogs). Mechanism: `lib/source-kit.mjs` `GENERIC_DIR` only treats `components/component/src/lib/ui/packages/react` as generic, and `package-build.mjs` only lets a docsMap `category` win when NO non-generic path segment exists. So the `docsMap` entries (primitives/composites) + `groups/layout.md` for these 7 are **inert** (kept for pattern-consistency with the 25, but the category never applies). Title-case groups (Layout/Composites/Primitives) would require forking `lib/source-kit.mjs` to extend `GENERIC_DIR` — judged not worth the perpetual fork; the path-derived names are honest and serviceable. The 3 dialogs are portal overlays → `cfg.overrides.<Name> = {cardMode:single, viewport}` (same idiom as `Dialog`).
- **`.design-sync/tsconfig.dts.json` `include` must enumerate any component source OUTSIDE `src/components/ui` + `src/ui/components`.** Batch 1 added `../src/app/{LeftDock,RightDock,ErrorBanner}.tsx` + `../src/features/presets/PresetDialogs.tsx`. `tsc` only emits `.d.ts` for files in `include` + their import graph; `src/app/*` isn't imported by the included dirs, so without these the new components ship empty `{[key]:unknown}` prop contracts. These 4 files import only `react` / `components/ui`, so they compile clean and keep `set -e` in `build-inputs.sh` green (no deck.gl/runtime pulled). Add to `include` whenever a future batch surfaces a component from a new source area.
- **Re-sync after Batch 1**: project now holds 32 component dirs + a user-authored `explorations/` design (`RecipePanel flat and flush.html`) that the sync must NEVER delete — the atomic path with `deletes:[]` preserves it. `_ds_manifest.json` + `_adherence.oxlintrc.json` are server-regenerated by the app self-check, not uploaded by us.
- **Batch 3 (2026-06-27, PipelineStage, →43)**: `tsconfig.dts.json` `include` extended with `../src/features/recipeDag/PipelineStage.tsx`. Its type-only `import type { RecipeDagLoadStatus } from "./useRecipeDagQuery"` makes tsc load `useRecipeDagQuery.ts` → the orpc client → `src/lib/orpc.ts`, which carries a **pre-existing `TS7056` ("inferred type too long")** that never surfaces in the esbuild app build. `noEmitOnError:false` still EMITS a correct `PipelineStage.d.ts` (full `PipelineStageProps`), but tsc then exits non-zero → `set -e` aborted `build-inputs.sh`. **Fix (durable): `build-inputs.sh` now runs the declaration `tsc` with `|| echo …` so a non-zero exit is tolerated** (declaration emit is best-effort by design; render-check + grading are the real gates). The preview drives the component with a hand-authored static `RecipeDagResult` fixture (recipeId/recipeKey/title + phases[]/stages[](steps,artifact{Requires,Provides},edge counts)/edges[]{from,to,artifact}/diagnostics[]); phase ids matching the domain palette (`foundation`/`morphology`/`hydrology`/`ecology`/…) get colored lanes via `domainPresentation`. `buildRecipeDagLayout` RUNS on the fixture at render, so the shape must satisfy the contract. The graph canvas is large/scrollable — the captured viewport shows the top-left (console + first lanes/ranks); that's representative, not a clip bug.
- **Batch 2 (2026-06-27, rjsf cluster, →42)**: `tsconfig.dts.json` `include` extended with `../src/features/configOverrides/{rjsfWidgets,rjsfTemplates}.tsx` (their `.d.ts` import graph pulls `schemaPresentation.ts` + `@rjsf/utils` types — `@rjsf/{core,utils,validator-ajv8}` are in `node_modules`, so tsc compiles clean and `set -e` stays green). DS-pane group is path-derived as **`configoverrides`** (lowercased source dir; `docsMap → groups/forms.md` is INERT, same mechanism as Batch 1's `app`/`presets`). The 3 templates get `cardMode:column` (full-width form structures); widgets use default grid. **Previews mock the rjsf prop shapes at RUNTIME only** (esbuild, not typechecked): widgets take a `{value,onChange,options:{emptyValue,enumOptions}}` `WidgetProps` subset; templates take `*TemplateProps` where each `property.content`/`item.children` is pre-rendered JSX and the collapse/transparent sets are passed as `{has:()=>…}` set-likes (so the real `pathToPointer` format is moot — forces always-expanded / nothing-transparent). If the real `BrowserConfigFormContext`/rjsf API drifts, these mocks may need updating. The resync verdict re-uploaded 4 unchanged primitives (Checkbox/Select/Switch/Textarea, the ones the widgets re-skin) alongside the bundle/style refresh — idempotent, `changed:[]` confirmed no render shifted.
- **Batch 4 / CONSOLIDATION REFACTOR (2026-06-27, →45, FIRST app-code PR — not sync-only)**: extracted two shared feature-tier primitives — `DisclosureHeader` (`src/ui/components/DisclosureHeader.tsx`, the controlled section-header row) and `EmptyState` (`src/ui/components/EmptyState.tsx`, the centered status card) — and migrated every in-repo consumer with ZERO render change. `DisclosureHeader` replaced 7 hand-rolled disclosure headers (ExplorePanel Stage/Step/Data/Group; WaterStatsSection; RecipePanel Recipe + Config). `EmptyState` replaced the CanvasStage "Awaiting matter" block + PipelineStage's local `CenteredState` (both call sites). Both surface through the EXISTING barrel (`export * from "@/ui/components/index"`), so **`ds-entry.tsx` needs NO new re-export and `tsconfig.dts.json`'s `../src/ui/components` include already covers them** — only `config.json` (componentSrcMap/docsMap→composites/overrides→`cardMode:column`) + 2 previews changed. Render check 45/0/0/0; the 43 existing components reported `changed:[]` (verbatim refactor → identical renderHash, which is itself the parity proof); `added:[DisclosureHeader,EmptyState]`; `deletePaths:[]` (explorations/ preserved).
  - **cn gotcha (critical)**: the primitives import the FEATURE-tier `cn` from `../utils` (plain `twMerge(clsx)`), NOT the shadcn `@/lib/utils` cn (which `extendTailwindMerge`-registers `text-data`/`text-label` as a font-size group). The plain cn DROPS `text-label` when it is merged with a text-color (treats both as generic `text-*`). So **dense type utilities (`text-label`/`text-data`) MUST live on the SLOT nodes (title/summary/trailing), never on the merged row `className`** — the Group sub-header eyebrow is applied on its title span, not the row.
  - **padding-free chrome**: `DisclosureHeader`'s `ROOT_CHROME` omits padding (`w-full flex items-center justify-between transition-colors hover:bg-accent`) because tailwind-merge's px↔pl/pr and py↔pt/pb conflicts are DIRECTIONAL — a baked `px-3 py-2.5` shorthand would linger under a consumer's `pl-3 pr-2`/`pt-2 pb-1` override. Each consumer passes exact padding (`px-3 py-2.5`; `px-3 py-2`; `w-auto flex-1 min-w-0 pl-3 pr-2 py-2` for the Data flex-1 sibling that keeps the Bug toggle as a sibling; `px-3 pt-2 pb-1` for the Group). Locked via a `cn()` precedence test BEFORE migrating.
  - **render() hatch**: RecipePanel Config is a `role="button"` div (it nests an On/Switch + Focus control whose `stopPropagation` is caller-owned); it uses `DisclosureHeader.render` so the primitive supplies the Enter/Space keyboard contract while the caller owns the root element + the nested guards.
  - **scope (left OUT, deliberate)**: GameConsole's status chip stayed hand-rolled (an `inline-flex` content-sized chip with 3-way conditional ring chrome + a flat Radio→dot→text row — not the `[left]⋯[right]` header pattern; forcing it in = over-unify). The rjsf `CollapsibleHeader` and the PipelineStage stage-node chevron also stayed OUT (schema-form / DAG-node specific). Two invisible improvements applied uniformly: decorative chevrons gained `aria-hidden`; the Group sub-header gained `transition-colors` (both invisible in the static render-check).
- **Batch 5 (2026-06-28, SchemaConfigForm, →46) — LAST sync batch; closes the syncable surface.** Synced the config-authoring ENGINE (`src/features/configOverrides/SchemaConfigForm.tsx`) as a fixture-driven card. NO app-code change — only `.design-sync/` (ds-entry re-export + `config.json` componentSrcMap/docsMap→`forms.md`-inert/overrides→`cardMode:column` + `tsconfig.dts.json` include + a new preview). Path-derived group `configoverrides` (joins the Batch-2 cluster). Render check **46/0/0/0**: the 45 existing reported `changed:[]` (verbatim-parity proof holds through the new rjsf bundle), `added:[SchemaConfigForm]`, `deletePaths:[]` (explorations preserved). `bundle/styling/aux` re-uploaded (new bundle pulls `@rjsf/core` + `@rjsf/validator-ajv8`; `changed:[]` proves zero visual regression despite the styleSha byte-churn).
  - **fixture (the engine's input is data, not a server call)**: a representative civ7-mapgen schema (`climate`/`landmass`) + matching `value` + a set-like `collapse` (`{expandedPointers:{has:()=>true}, toggle:noop}`). Two stories: `ConfigForm` (full multi-group form) + `FocusedStage` (`focusPath:["climate"]`, RecipePanel's per-stage focus mode). Exercises every Batch-2 part: object groups, select, number, switch, **tagSelect**, textarea, and the array template (Add + nested item).
  - **tagSelect gotcha (critical for any rjsf-array fixture)**: an array-of-enum only routes to the multi-select widget (with `enumOptions` populated) when the schema has **`uniqueItems: true`** — rjsf `ArrayField.isMultiSelect()` requires it. Without it the `tagSelect` widget renders EMPTY (label only, no chips). The render-check caught exactly this on the first pass; adding `uniqueItems:true` made the chips render. The real config schema expresses tag-sets this way.
  - **SchemaForm deliberately NOT a separate card**: it's the internal rjsf bridge (takes a pre-built `uiSchema`/`formContext` that only `SchemaConfigForm.buildUiSchema` derives); a standalone fixture would be synthetic. The engine card renders it internally → whole composition already visible.
- **SURFACE COMPLETE (post-Batch-5).** No syncable presentational components remain. The exterior (constructed, not absent): the editable claude.ai/design **project** (a product-surface activity the CLI can't create — `create_project` only makes `PROJECT_TYPE_DESIGN_SYSTEM`), and the runtime/logic **drops** (DeckCanvas/CanvasStage host/StudioProviders/StudioShell + hooks/stores/pure-logic modules). Future syncs are maintenance-only (a NEW presentational component → add to `componentSrcMap`/`docsMap`/`ds-entry`/`tsconfig.dts` + a preview; re-verify `changed:[]`).
