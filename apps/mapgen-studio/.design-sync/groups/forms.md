---
category: Forms
---

The rjsf config-form layer: the design-system-skinned RJSF widgets
(`src/features/configOverrides/rjsfWidgets.tsx`) and field/object/array templates
(`rjsfTemplates.tsx`) that render the schema-driven config-override form.

NOTE: these components live in a non-generic source dir, so the DS-pane group is
PATH-DERIVED as `configOverrides` (this `category: Forms` is inert — kept for
pattern-consistency with the rest of the catalog). See NOTES.md "Re-sync risks".
