{
  "name": "@swooper/mapgen-viz",
  "version": "0.1.0",
  "private": true,
  "description": "Portable MapGen visualization projection and materialization",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "check:test": "tsc -p test/tsconfig.json --noEmit",
    "clean": "rimraf dist",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "test": "bun test ./test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "typebox": "catalog:"
  },
  "engines": {
    "node": "22.22.0"
  }
}
