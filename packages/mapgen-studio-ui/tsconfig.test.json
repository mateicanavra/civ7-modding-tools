{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Tests run under Vitest on Node (fs fixtures and jsdom component tests).
    // Source stays checked by the environment-neutral production config.
    "types": ["node"]
  },
  "include": ["test/**/*.ts", "test/**/*.tsx"]
}
