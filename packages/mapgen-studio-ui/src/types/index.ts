/**
 * `@swooper/mapgen-studio-ui/types` — public type surface (types-only export
 * condition; no runtime target — DESIGN.md §2.8).
 *
 * B1 scaffold: empty. Component prop types and re-homed presentational types
 * re-export through here as the extraction branches land.
 */
export {};
