/** TypeBox schema types available to domain-model vocabulary modules. */
export type { Static, TObject, TSchema } from "typebox";

/** TypeBox builders available to domain-model vocabulary modules. */
export { Type } from "typebox";

/** Typed-array schema builders with MapGen cardinality annotations. */
export { TypedArraySchemas } from "./typed-array.js";
