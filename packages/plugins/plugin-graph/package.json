{
  "name": "@civ7/plugin-graph",
  "version": "0.1.0",
  "private": true,
  "nx": {
    "tags": [
      "kind:plugin"
    ]
  },
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@hpcc-js/wasm": "^2.32.3",
    "fast-xml-parser": "^5.3.3"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "dev": "tsup src/index.ts --format esm,cjs --dts --watch",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check .",
    "test": "vitest run --project plugin-graph",
    "clean": "rimraf dist"
  },
  "engines": {
    "node": "22.22.0"
  }
}
