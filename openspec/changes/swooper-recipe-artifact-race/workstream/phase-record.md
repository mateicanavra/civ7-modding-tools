# Swooper Recipe Artifact Race Phase Record

## State

- Branch/Graphite stack: `agent-F-swooper-recipe-artifact-race` above
  `agent-F-mod-swooper-catalog-order-proof`.
- Change id: `swooper-recipe-artifact-race`.
- Objective: remove the current H4 full-root proof blocker by ensuring
  `mod-swooper-maps:test` is ordered after the generated recipe artifact
  producer it imports.
- Status: IMPLEMENTED AND LOCALLY VERIFIED; full root `test` is green after
  this repair.

## Authority And Inputs

- Root `AGENTS.md`: generated outputs are read-only and must be regenerated by
  scripts, not hand-edited; use Graphite; leave worktree clean.
- `mods/mod-swooper-maps/AGENTS.md`: `mod/` is generated build output; use
  package/root test commands for verification.
- User protocol note (2026-06-13): structural/boundary/lint-style checks in
  tests are not the intended end state; repair only as H4 compatibility gates,
  then migrate to Habitat-owned enforcement in H5/H6.
- Cross-agent sharing protocol recorded in the workstream standing rules and
  applied to thread `019ebf67-7ffc-7130-afd0-b0419796c7fc` before this slice.

## Opening Evidence

- Full root proof after `mod-swooper-catalog-order-proof`:
  `NX_DAEMON=false bunx nx run-many -t test --outputStyle=static` failed with
  exit 1.
- `mapgen-studio:test` passed 47 files / 233 tests.
- `mod-swooper-maps:test` failed with an unhandled ENOENT while importing
  `mods/mod-swooper-maps/dist/recipes/standard-artifacts.js` in
  `test/config/standard-recipe-artifact-guards.test.ts`.
- The artifact exists after the run completes, which points at a parallel
  root-load clean/rebuild window, not a missing generator output.
- Resolved Nx state before this slice: `mod-swooper-maps:test` depends only on
  `^build`, while `mapgen-studio:test` depends on
  `mod-swooper-maps:build:studio-recipes`; both can appear in the same root
  execution.

## Implementation Plan

1. Add `build:studio-recipes` to the self dependencies for
   `mod-swooper-maps:test`.
2. Confirm the resolved Nx project target has the dependency and no cycle.
3. Re-run a focused two-project reproduction before full root proof.

## Verification

- Resolved Nx project target:
  `bunx nx show project mod-swooper-maps --json` shows
  `mod-swooper-maps:test` still uses `nx:run-script` with script `test` and now
  depends on `["^build", "build:studio-recipes"]`.
- Focused root-load reproduction:
  `NX_DAEMON=false bunx nx run-many -t test
  --projects=mod-swooper-maps,mapgen-studio --parallel=2 --skip-nx-cache
  --outputStyle=static` passed with exit 0. The run completed
  `mapgen-studio:test` at 47 files / 233 tests and `mod-swooper-maps:test` at
  567 passed, 2 skipped, 0 failed across 145 files. No `standard-artifacts`
  ENOENT appeared.
- Full root proof:
  `NX_DAEMON=false bunx nx run-many -t test --outputStyle=static` passed with
  exit 0 for 18 projects and 15 dependency tasks. Nx replayed cache for 29 of
  33 tasks, reported `@mateicanavra/civ7-cli:test` and
  `@civ7/direct-control:test` as historically flaky, and reported no failed
  task. `mapgen-studio:test` passed 47 files / 233 tests and
  `mod-swooper-maps:test` passed 567 tests, 2 skipped, 0 failed.
- Generated/protected drift check after the focused and full root proofs
  returned no tracked generated/protected paths.
- OpenSpec validation:
  `bun run openspec -- validate swooper-recipe-artifact-race --strict` and
  `bun run openspec -- validate habitat-biome-hygiene --strict` passed.
- Diff hygiene:
  `git diff --check` passed.

## H4 Carry-forward

- H4 task 2.4 can now close locally: root build/parity evidence was already
  green after promoted repairs, and full root `test` is green after this slice.
- `standard-recipe-artifact-guards.test.ts` remains a structural guard and
  should migrate to Habitat-owned enforcement in H5/H6 after parity proof.
