# D0 Residue Ledger

Date: 2026-06-14
Scope: non-D0 runtime residue named by the completed one-mount change or its historical authority.

| Residue | Current source | Risk | Owner domino | Blocking status |
| --- | --- | --- | --- | --- |
| Bare/generic 500 mapping for known engine failures | `mapgen-studio-runtime-one-mount/proposal.md` leaves error-spine work out of scope | Known Run/Save/Autoplay failures can still reach users as generic transport/internal errors if D3 is not completed. | D3 `mapgen-studio-error-spine` | Blocks D3, not D0. D0 records the transport baseline only. |
| `RunInGameHttpError` bridge | Runtime refactor frame names it as no long-term role | Transport-shaped exception bridge can preserve app-hosted imperative failure semantics. | D3 `mapgen-studio-error-spine` and D12 final negative search | Blocks runtime closeout; D0 only records deletion target. |
| Client `DedupeRequestsPlugin` / `BatchLinkPlugin` reevaluation | `mapgen-studio-runtime-one-mount/proposal.md` and `design.md` defer plugin choice | Could become cargo-cult transport tuning or hide polling pressure if adopted before operation polling dies. | D9 `mapgen-studio-operations-push` after polling authority deletion | Nonblocking for D0. D9 must either adopt with measured value or reject with evidence. |
| Browser operation recovery bridge | Historical PLAN and operations-current packet | Browser may remain a second owner of operation truth. | D6 `mapgen-studio-operations-current` | Blocks D6, not D0. |
| Polling/watchdog authority | Historical PLAN and operations-push packet | Client polling can remain a hidden state authority after event push lands. | D9 `mapgen-studio-operations-push` | Blocks D9 and D12 closeout. |
| App-local dev supervisor / Bun watcher | `mapgen-studio-bun-server` and current app package scripts | Dev process stack can keep restart/watch ambiguity after Nx exists. | D11 `mapgen-studio-nx-dev-runner` | Blocks D11 and D12 closeout. |
| Shared tuner session carveout for Run in Game | `mapgen-studio-tuner-session` originally left Run in Game untouched | Long-running mutations can remain outside the runtime game-wire door. | D5 `mapgen-studio-pipeline-effect-services` and D12 `mapgen-studio-game-door-invariant` | Blocks D12 closeout unless converged or permanently assigned with guard tests. |
