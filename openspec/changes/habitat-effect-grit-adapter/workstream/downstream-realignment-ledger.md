# Downstream Realignment Ledger

**Change:** `habitat-effect-grit-adapter`
**Owner:** DRA Habitat recovery owner

| Downstream artifact | Current risk | Interim status | Required disposition | Status |
| --- | --- | --- | --- | --- |
| `openspec/changes/habitat-grit-proof-repair/design.md` | Adapter tasks are blocked on a substrate that did not yet have its own packet. | substrate packet reviewed in this checkpoint | Update Grit proof repair records to consume the accepted adapter contract before implementation. | design realigned |
| `openspec/changes/habitat-grit-proof-repair/tasks.md` | Tasks 4, 6, and adapter tests cannot proceed until the substrate is accepted. | design dependency recorded in this checkpoint | Mark the relevant tasks as dependent on this change after acceptance, then unblock during implementation. | design realigned |
| `openspec/changes/habitat-grit-proof-repair/workstream/command-proof-log.md` | Row-level proof needs stable adapter proof ids; the first switched broad run exposed parser-input truncation and all-row infrastructure failure. | parser/output strategy repaired and proof-artifact schema implemented; row-level proof ids remain owned by `habitat-grit-proof-repair` when it runs all-row proof | Keep Grit row/current-tree/injected/baseline proof blocked until the downstream packet creates row proof ids from accepted adapter APIs. | deferred to downstream |
| Grit-pattern row workstreams consuming Habitat command evidence | Downstream rows can otherwise guess whether `grit`, `openspec`, `biome`, or `nx` were resolved through ambient PATH, package scripts, package binaries, or Nx targets. | command resolver implemented and documented: workspace tools use `workspace-bun-run` by default; `openspec` uses `workspace-bunx-binary` with `bun x --no-install` because a same-name root script exists; system prerequisites stay direct | Downstream rows may consume requested executable, effective executable, execution plane, effective cwd, argv, env, Git state, cache policy, and non-claims from command results, but must not treat resolver proof as row injected/current-tree/baseline/apply closure. | contract available |
| `deep_import_to_public_surface` apply proof consumers | Current pinned Grit apply dry-run emits compact human rewrite lines for matches, not structured Habitat rewrite inventory. A temporary source-AST fallback and later domain-shaped inventory classification would hardcode pattern semantics into the core harness. Inside-root create/delete diff evidence also must not be silently approved by root membership alone. | fallback and domain-shaped classifier rejected; isolated transaction-copy diff proof implemented and preserves dry-run apply matches without re-authoring pattern semantics; isolated-copy create/delete evidence now fails closed without pattern-owned create/delete approval | Downstream consumers may claim isolated transaction-copy apply diff proof for controlled `deep_import_to_public_surface` probes and `habitat:fix -- --dry-run` apply-match preservation, including generic create/delete blocking at the isolated-copy evidence boundary. They must not claim semantic symbol/import inventory, live worktree apply, live create/delete rejection, baseline shrink, row closure, or product proof from this packet. | contract available |
| `openspec/changes/habitat-grit-proof-repair/workstream/phase-record.md` | Next action text still described prior validation/review work instead of the opened substrate packet. | patched in this checkpoint | Keep current next action aligned with this change's review state. | patched |
| `openspec/changes/habitat-grit-proof-repair/workstream/review-disposition-ledger.md` | ESR-3 carried stale validation-status language after validation had passed. | patched in this checkpoint | Keep review ledger current before stacking implementation claims. | patched |
| `docs/projects/habitat-harness/effect-orchestration-evaluation.md` | Evaluation listed candidate slices but did not record the first selected adoption slice. | patched in this checkpoint | Keep the decision record clear that this packet is Grit-scoped, provisional until review/parity, and not a broad migration. | patched |
| `docs/projects/habitat-harness/recovery-claim-ledger.md` | `CLAIM-P1-EFFECT-FIT` can otherwise keep treating the accepted Grit adapter substrate as an unresolved implementation dependency. | recovery ledger records the accepted Grit-scoped Effect adapter substrate and keeps other command/check/baseline/hook/promotion surfaces as per-workstream proof decisions | Consume this packet as the accepted Grit adapter substrate only; do not treat it as all-row Grit proof, baseline shrink, live apply, hook, Nx/Biome, or product/runtime proof. | patched |
| `docs/projects/habitat-harness/grit-pattern-corpus-ledger.md` | Current rows need adapter proof ids for injected and apply proof. | adapter substrate implemented, but row proof ids are not generated in this repair packet | Fill proof ids when Grit proof repair consumes the accepted adapter and runs row-level proof. | deferred to downstream |
| `tools/habitat-harness/package.json` and `bun.lock` | Habitat previously had no Effect dependency even though this packet selects Effect. | package-manager dependency edit made and parity proved under Bun source/dev plus built Node paths; Grit adapter wiring is accepted for this packet's substrate boundary | Keep exact version/runtime rationale and non-claims in this packet's phase record; do not treat dependency parity or adapter wiring as Grit row/current-tree/injected/baseline/live-apply proof. | parity recorded |
| Future `habitat-effect-command-runner` | Shared command-runner extraction may be desired too early, especially after the resolver moved beyond a Grit-only PATH fix. | Grit-scoped central materialization now exists inside this packet and is backed by Bun/Nx documentation plus tests; extraction remains outside this repair unless another owner needs the same contract. | Open only when Biome, Nx, hook, baseline, or command-surface work proves it needs the same command-result contract outside Grit without changing command semantics. | watched |
