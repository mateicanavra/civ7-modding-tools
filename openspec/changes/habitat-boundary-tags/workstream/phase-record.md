# Phase Record — H3 `habitat-boundary-tags`

## Phase

- Project: habitat-harness (FRAME.md controlling)
- Phase: H3 — project-plane boundary tags and Nx boundary enforcement
- Owner: workstream owner agent (F), continued by Codex takeover
- Branch/Graphite stack: `agent-F-habitat-boundary-tags` -> `agent-F-habitat-harness-scaffold` -> `agent-F-habitat-nx-adoption` -> `agent-F-habitat-harness-workstream` -> `main`
- Started: 2026-06-12
- Status: COMPLETE (local gates recorded; tests unchanged-red for pre-existing failures only)

## Objective

- Target movement: encode the derived project-plane taxonomy as Nx project tags, run `@nx/enforce-module-boundaries` through a quarantined boundaries-only ESLint config, and expose the locked empty-baseline rule through the habitat harness, Nx targets, and CI.
- Non-goals: no taxonomy redesign, no dependency edits that weaken constraints, no intra-project GritQL/file-layer rules, no general ESLint lint migration, no generated-output edits. H3 did make one hidden tag-legal test edge explicit (`@civ7/direct-control` -> `@civ7/map-policy`) because the boundary rule found a relative cross-project import.
- Done condition: H3 tasks 1.1-4.3 checked; boundaries gate green; illegal-import probe fails with the expected tag-rule message and is reverted; OpenSpec validation/build/check/test posture recorded; Graphite branch committed cleanly.

## Authority

- Root/subtree `AGENTS.md`: root AGENTS.md governs docs/OpenSpec/root config/package metadata edits; no closer router applies to this phase's current write set.
- Product refs: N/A (structure-only workstream; no product/runtime behavior claim)
- Architecture refs: `docs/projects/habitat-harness/FRAME.md` hard core #2/#4, `docs/projects/habitat-harness/taxonomy.md` §1-§3, `docs/projects/habitat-harness/review-disposition-ledger.md` Architecture-Review Lane A1-A6, `civ7-architecture-authority`
- Project refs: `openspec/changes/habitat-boundary-tags/{proposal.md,tasks.md,specs/habitat-harness/spec.md}`
- Excluded/stale inputs: chat-only H3 task state; earlier proposal wording that mentioned the removed `kind:control -> kind:mod` allowance was repaired and is subordinate to taxonomy.md + architecture-review ledger.

## Current State

- Repo/Graphite state: branch `agent-F-habitat-boundary-tags`; stack top over H2 (`713fef6b2`); no staged changes at takeover.
- Dirty files and owner: inherited H3 dirty changes from Claude/Fable plus Codex continuation edits — workspace `package.json` tag edits, root dependency/lockfile update, `@civ7/direct-control` test import/devDependency repair, docs review/taxonomy/workstream updates, `eslint.boundaries.config.mjs`, harness target/rule wiring, CI and README updates.
- Current code evidence: H1/H2 closed; H2 pre-seeded `tools/habitat-harness` with `kind:tooling`; pre-H3 architecture review reported declared package-manifest edges green, and H3 implementation repaired one hidden source-level relative test import that scan missed.
- Generated outputs affected: `bun.lock` generated by package-manager install. `bun run build` regenerated `mods/mod-civ7-intelligence-bridge/mod/ui/civ7-intelligence-bridge.js` due the known net-import bundle drift; that generated file was restored and is not part of H3.
- Tests/guards affected: new `boundaries` target, `nx-boundaries` rule-pack entry, `habitat check` / `verify` composition, CI affected target list.

## Scope

- Write set: workspace `package.json` files for `"nx.tags"` plus the repaired direct-control test devDependency/import; root `package.json`/`bun.lock`; root `eslint.boundaries.config.mjs`; `tools/habitat-harness/**` target/rule/check wiring and README; `.github/workflows/ci.yml`; H3 OpenSpec docs/tasks/spec and this phase record; project docs needed for H3 realignment.
- Protected files: product source code in apps/packages/mods except the tag-legal direct-control test import repair and temporary reverted probe files; generated `dist/`, `mod/`, and resource outputs; unrelated suggested-task fixes unless explicitly moved into this slice.
- Owners: project-plane import boundary = Nx tags + `@nx/enforce-module-boundaries`; rule orchestration = habitat harness.
- Forbidden owners: no additional ESLint rules; no project.json files; no dependency-edge fixes that weaken or bypass the rule; no silent taxonomy weakening.
- Consumer impact: contributors/CI get a new `boundaries` target and `nx-boundaries` habitat rule; existing `lint` scripts stay untouched.
- Downstream assumptions: H4 depends on tags being in package manifests; H6/H7 depend on `boundaries` target and `nx-boundaries` rule id.

## Spec/Tasks

- Spec/proposal: `bun run openspec -- validate habitat-boundary-tags --strict` PASS after H3 edits.
- Tasks: `openspec/changes/habitat-boundary-tags/tasks.md`
- Validation status: strict-valid after H3 edits.

## Review

- Review lanes: spec lane CLOSED pre-H1; architecture-review lane CLOSED
  pre-H3 with adoption-time LOCK-SAFE verdict.
- Blocking findings: none open.
- Accepted findings repaired: A1-A6 in taxonomy/review docs and inherited config shape; verify during implementation.
- Rejected/invalidated/waived/deferred findings: none.

## Agent Fleet State

- Team design applied during Codex continuation; main owner retained synthesis,
  task truth, and commit accountability.
- Effect/oRPC lifecycle sidecar: complete. Verdict: H3 remains a project-plane
  boundary slice, but docs must not flatten control lifecycle ownership.
  Taxonomy now records the `@civ7/direct-control` raw-session owner,
  `@civ7/studio-server` Effect-scoped `Civ7TunerSession` owner, and
  `@civ7/control-orpc` procedure/service owner split.
- Boundary verification sidecar: complete. Verdict: taxonomy constraints are
  not weakened and gates are green; closure was not truthful until the AGENTS
  router mentioned the `boundaries` target. Root AGENTS.md now carries the
  stable router pointer.
- Graphite/repo hygiene sidecar: complete. Verdict: dirty changes are H3 on an
  existing Graphite branch with no unique commit yet; use `gt modify --commit`
  after staging the reviewed H3 write set.

## Implementation

- Completed tasks: 1.1-4.3.
- Remaining tasks: none inside H3 implementation.
- Stop conditions triggered: no true taxonomy-constraint red edge. The first direct rule run found a hidden relative test import from `@civ7/direct-control` into `@civ7/map-policy`; because `kind:control -> kind:foundation` is tag-legal, this was repaired as an explicit package import/devDependency rather than baselined or used to weaken taxonomy.

## Verification

- Commands run:
  - `git status --short --branch`
  - `git diff --cached --stat`
  - `gt log short --stack`
  - `jq -r '.name + "\t" + (.nx.tags // [] | join(","))' ...`
  - `bunx nx show project @civ7/adapter --json`
  - `bunx nx show project @swooper/mapgen-core --json`
  - `bunx nx show project mod-swooper-maps --json`
  - `bunx nx show project @civ7/studio-server --json`
  - `bunx eslint . --config eslint.boundaries.config.mjs` (initial red; runner/import-shape repairs applied)
  - `FORCE_COLOR=0 bunx eslint . --quiet --config eslint.boundaries.config.mjs --no-config-lookup`
  - `bunx nx run-many -t boundaries --all --skipNxCache`
  - `bun tools/habitat-harness/src/bin/habitat.ts check --rule nx-boundaries --json`
  - probe: added `packages/config/src/h3-boundaries-probe.ts` importing `@civ7/adapter`; `bunx nx run @internal/habitat-harness:boundaries` failed with `kind:foundation` constraint; probe removed; uncached boundary rerun green
  - `bunx nx affected -t boundaries --base=HEAD --skipNxCache`
  - `bun run openspec -- validate habitat-boundary-tags --strict`
  - `bun run openspec:validate`
  - `bun run habitat:check -- --json`
  - `bunx nx run @internal/habitat-harness:check`
  - `bun run build`
  - `bun run check`
  - `bun run test`
  - restored generated `mods/mod-civ7-intelligence-bridge/mod/ui/civ7-intelligence-bridge.js` after build dirtied it
- Historical adoption results: tags matched taxonomy; spot-checked Nx graph
  included expected `kind:*` tags plus harmless `npm:*` tags; `nx-boundaries`
  locked empty; direct, run-many, and affected boundary gates were green after
  repairs; violation probe failed with the expected tag-rule message.
- Current recovery note: these results are H3 adoption evidence. The active
  current-state taxonomy proof is now owned by
  `habitat-boundary-taxonomy-tightening`, which rechecks workspace manifests,
  resolved Nx metadata, config parity, graph-edge legality, selected
  false-negative probes, and Habitat `nx-boundaries` wrapper output with
  repo-local normal Nx command forms. H3 affected/run-many rows must not be used
  as standalone current recovery proof.
- `bun run build`: PASS for 20 build projects plus dependent task.
- `bun run check`: PASS for 22 projects plus root lint scripts; mapgen-docs emitted its pre-existing warning-only `@mapgen/*` mentions.
- `bun run habitat:check -- --json`: PASS; `nx-boundaries` present, locked, and diagnostics-empty; `adapter-boundary` remains baselined for its known 6 allowlist entries + the H2 river metadata baseline; `doc-ambiguity` advisory findings remain advisory.
- `bun run openspec:validate`: PASS, 146/146 items strict-valid.
- `bun run test`: exits 1 with pre-existing failure classes only:
  - `mod-civ7-intelligence-bridge/test/controller-mod-package.test.ts` still fails because generated UI bundle contains `from "net"` (known triage task).
  - package-local bare Vitest runs still surface the SDK async-write teardown race from `packages/sdk/test/mod.test.ts` / `packages/sdk/src/files/XmlFile.ts:26` as unhandled ENOENT (known triage task).
  - full-load `mapgen-studio` run timed out two river Studio-emission tests (`test/viz/dataTypeModel.test.ts`, `test/viz/riverLakeInspector.test.ts`), matching H1's load-flake classification.
- Skipped gates and rationale: none; test is classified unchanged-red, not H3-green.
- Evidence boundary: local structural enforcement only; no runtime/product proof claimed.

## Realignment

- Effect/oRPC grounding added: official Effect docs for Requirements
  Management, Layers, Resource Management/Scope, Runtime/ManagedRuntime, and
  effect-orpc service-injection/error handling were reconciled against local
  Studio/control records. H3 makes no runtime code change; the taxonomy records
  that `kind:control` is a project-plane tag, not permission to add raw
  transports or caller-local session ownership.
- Downstream docs/specs/issues updated: taxonomy locked-at-adoption note; H3 proposal stale `kind:control -> kind:mod` text repaired; review ledger overclaim repaired; README tag vocabulary/revision protocol added; root AGENTS.md router points at the `boundaries` target and taxonomy.
- Tests/guards updated: `nx-boundaries` rule-pack entry; inferred `boundaries` target; `habitat verify` affected target list; CI boundary-tags step.
- Deferrals/triage updated: existing pending triage tasks remain: intelligence-bridge net import leak, SDK async-write ENOENT teardown race, adapter-boundary river metadata allowlist.
- Downstream realignment ledger: N/A unless H3 changes downstream assumptions beyond its proposal.

## Next Action

- Exact next step: continue the Habitat harness workstream with the next
  OpenSpec slice; keep the three suggested red-test triage tasks outside H3
  until explicitly promoted.
- First files to inspect: `docs/projects/habitat-harness/workstream-record.md`,
  this phase record, and the next OpenSpec change's tasks.
- Stop condition: staged diff includes generated output or unrelated suggested-task fixes.
