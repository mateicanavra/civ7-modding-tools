## Tasks

- [x] Move the service runtime tag, layer, and requirement/error types into
      `service/context.ts`.
- [x] Delete the duplicate `service/base.ts` vocabulary split.
- [x] Standardize module context implementer exports to `module`.
- [x] Update service routers to import the standard module implementer.
- [x] Route structural enforcement through the Habitat service architecture
      guard instead of the verify router source-text test.
- [x] Run package validation.
- [x] Run OpenSpec validation.
- [x] Commit as a local Graphite slice without submitting the stack.
