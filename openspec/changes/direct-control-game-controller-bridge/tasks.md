## 0. Realignment

- [x] 0.1 Open the `direct-control-game-controller-bridge` OpenSpec change.
- [x] 0.2 Realign solution, actuation, ADR, project, and direct-control docs
  around the game-scoped controller baseline.
- [x] 0.3 Record downstream realignment for completed Tuner-first read/action
  specs.
- [x] 0.4 Disposition agent audit findings.

## 1. Controller Mod

- [ ] 1.1 Create the source-owned controller mod package and build script.
- [ ] 1.2 Emit a modinfo with `scope="game"` `UIScripts` and optional
  `scope="shell"` `UIScripts`.
- [ ] 1.3 Implement `Civ7IntelligenceBridge.invoke(...)` with protocol version,
  request id, method allowlist, bounds, and structured errors.
- [ ] 1.4 Implement `system.ping`, `capabilities.list`, `game.snapshot`, and
  `intent.ack`.
- [ ] 1.5 Add controller unit tests for dispatch, validation, and error shapes.
- [ ] 1.6 Keep generated `mod/` output regenerated by script, not hand-edited.

## 2. Direct-Control Client

- [ ] 2.1 Add a typed direct-control controller invocation helper targeting App
  UI game state.
- [ ] 2.2 Add mock socket tests proving bounded envelope encoding, selected
  state, timeout handling, version mismatch handling, and no raw caller JS.
- [ ] 2.3 Add CLI inspection support for controller `ping` and capabilities.

## 3. Read Parity

- [ ] 3.1 Implement controller map, plot, player, unit, city, visibility, and
  bounded `GameInfo` read methods.
- [ ] 3.2 Compare controller outputs against existing direct-control wrappers on
  the same turn.
- [ ] 3.3 Promote matching wrapper families to controller-backed calls, with raw
  wrappers kept only as diagnostics until removal criteria are met.

## 4. Operation Validation And Approved Action

- [ ] 4.1 Implement `operations.validate` for unit, city, and player operation
  families.
- [ ] 4.2 Compare validation outputs against existing direct-control operation
  validators.
- [ ] 4.3 Design the approval token and idempotency contract for
  `actions.executeApproved`.
- [ ] 4.4 Run one disposable approved-action proof: approval record, controller
  send, no replay, and semantic postcondition reread.

## 5. Lifecycle Proof

- [ ] 5.1 Deploy the controller mod through the repo-owned deploy path.
- [ ] 5.2 Prove game-context global presence after game action-group load.
- [ ] 5.3 Prove shell entrypoint behavior separately if shell support is present.
- [ ] 5.4 Prove Tuner absence unless a future Tuner-specific entrypoint is
  deliberately added and verified.
- [ ] 5.5 Prove reload, restart, save/load, turn transition, and age transition
  behavior or record exact blockers.

## 6. Verification And Closure

- [ ] 6.1 Run controller package tests/check/build.
- [ ] 6.2 Run direct-control package tests/check/build.
- [ ] 6.3 Run focused CLI tests/check/build if CLI is touched.
- [ ] 6.4 Run live read-only proof and record command outputs with proof labels.
- [ ] 6.5 Run OpenSpec validation and `git diff --check`.
- [ ] 6.6 Update downstream realignment, review disposition, proof ledger, and
  Next Packet before closure.
