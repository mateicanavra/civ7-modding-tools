# Evidence Log - Empty Schema Default

| Evidence id | Source / command | Result | Proof class | Non-claims |
| --- | --- | --- | --- | --- |
| ESD-E1 | `tools/habitat-harness/src/rules/rules.json` | `grit-empty-schema-default` is enforced `grit-check`, scoped to contract schema files, forbidding empty object defaults in schema definitions. | Record truth / authority extraction | Does not prove Grit predicate behavior or wrapper enforcement. |
| ESD-E2 | `docs/projects/habitat-harness/invariant-corpus.md` | Retired `eslint-empty-schema-defaults` maps no `{ default: {} }` in contract schemas to `grit-check`. | Record truth / authority extraction | Retired parity remains open. |
| ESD-E3 | `scripts/lint/lint-domain-refactor-guardrails.sh` | Full profile includes schema-related guardrail lineage. | Record truth / lineage extraction | Does not prove wrapper/current-tree behavior or retired parity closure. |
| ESD-NATIVE-FIXTURES-2026-06-15 | `GRIT_TELEMETRY_DISABLED=true bunx grit patterns test --filter empty_schema_default --json` | Exit 0; one testable pattern passed. `Matches fixture` produced 5 current-predicate matches across domain op `*.contract.ts` object-literal default, TypeBox options object, nested schema object, recipe step `*.contract.ts`, and other-mod raw predicate classes. `Ignores fixture` produced 0 matches for non-empty object default, property-level default, array/null/scalar defaults, `defaultValue` lookalike, ordinary domain op `contract.ts`, ordinary recipe step `contract.ts`, config, repo test, map, package, and `.tsx` controls. | Native tool behavior | No Habitat wrapper, raw acquisition, baseline, injected cleanup, Effect adapter, apply safety, retired parity, exact schema-policy closure, neighboring row, or product proof. |
| ESD-SCHEMA-INVENTORY-2026-06-15 | Inline Bun/TypeScript compiler API parser inventory over `mods/mod-swooper-maps/src/recipes` and `mods/mod-swooper-maps/src/domain` | Scan roots: `mods/mod-swooper-maps/src/recipes`, `mods/mod-swooper-maps/src/domain`; exclusions: `node_modules`, `dist`, and `mod`. Count terms: "basename contains `contract`" means a `.ts` file whose basename includes the substring `contract`; "basename ends `contract.ts`" means a `.ts` file whose basename ends with `contract.ts`; ordinary `contract.ts` means basename exactly `contract.ts`; current predicate means the current `empty_schema_default` path regex. Counts: 886 scanned TS/TSX files, 174 TS files whose basename contains `contract`, 167 TS files whose basename ends `contract.ts`, 119 ordinary `contract.ts` files, 30 current-predicate `*.contract.ts` files, 0 current-predicate TSX files, 0 domain-op current-predicate files, 30 recipe-step current-predicate files, 3 total `default` properties inside current-predicate files, 0 current-row empty object default candidates inside current predicate, 2 empty object default candidates inside ordinary `contract.ts` files, 0 empty object default candidates outside current predicate and ordinary contracts, 3 non-empty default properties inside current predicate, 0 exact `defaultValue` properties inside current predicate, and 2 non-default property names containing `default` inside current predicate, both `defaultStrategy`. Ordinary-contract matches: `mods/mod-swooper-maps/src/domain/placement/ops/plan-starts/contract.ts:525` and `mods/mod-swooper-maps/src/domain/resources/ops/select-resource-sites/contract.ts:304`. | Parser inventory / record truth; predicate-gap blocker evidence for exact schema-policy closure | No Habitat wrapper, raw acquisition, baseline, injected cleanup, Effect adapter, apply safety, retired parity, exact schema-policy closure, neighboring row, or product proof. Current-predicate zero-candidate evidence is not proof that ordinary `contract.ts` files are covered. |
