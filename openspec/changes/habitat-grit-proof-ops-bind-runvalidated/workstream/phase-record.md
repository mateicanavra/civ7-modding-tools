# Phase Record - Ops Bind RunValidated

## Current Gate

The active `grit-ops-bind-runvalidated` check row is implemented, verified, and
record-aligned for supervisor review. Native fixture proof, parser inventory,
Habitat wrapper/per-rule selector proof, explicit empty baseline inventory, and
registered injected-probe proof are current for this checkpoint.

## Proof Boundary

This row may close only as a non-apply check row for Swooper domain op runtime
entrypoint `ops.bind(...)` and direct `runValidated(...)` calls. Raw direct
Grit acquisition, export/dynamic closure, source remediation, apply safety,
classify/generator behavior, retired full-profile parity, broader
domain-refactor closure, and product/runtime proof remain non-claims.

## Next Actions

1. Supervisor review.
2. Keep raw direct Grit acquisition, export/dynamic closure, source
   remediation, apply safety, classify/generator behavior, retired parity,
   broader domain-refactor closure, and product/runtime proof as non-claims
   unless a later row proves them.
