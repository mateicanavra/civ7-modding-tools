# Phase Record - Generated Bundle Node Builtins

## Current Gate

Implementation, local proof, and the local Graphite checkpoint are complete for
the generated-bundle wrapped-test checkpoint. The row is intentionally not an
active Grit check because generated outputs are not approved Grit scan roots.
The current gate is supervisor disposition of the checkpoint and this
record-truth repair before any next HG row.

## Implemented

- Hardened the Intelligence Bridge generated UI bundle package test for Node
  builtin import/require/dynamic-import source forms and direct-control/runtime
  transport tokens.
- Added a package-owned Nx target for that test with this package's build chain
  as its dependency.
- Made the package bundle build depend on upstream workspace builds before it
  resolves workspace package exports.
- Registered `arch-test-intelligence-bridge-bundle-runtime-imports` as a
  Habitat wrapped-test rule through the graph-owned target.
- Added the explicit empty Habitat baseline for the new wrapped-test rule.
- Recorded current bundle inventory and Swooper generated-output freshness
  blocker.
- Repaired the graph target after an initial failed target run deleted the
  generated UI bundle before failing; the corrected target regenerates the
  bundle and leaves it present without a tracked generated-output diff.

## Next Actions

1. Await supervisor disposition for the committed generated-bundle wrapped-test
   checkpoint and this record-truth repair.
2. Do not open the next HG row until supervisor disposition clears this gate.

## Protected Paths

- Do not hand-edit generated bundle outputs.
- Do not change HR repair-chain implementation or Grit adapter scan-root
  policy.
- Do not convert this checkpoint into active generated-output Grit proof.

## Non-Claims

No active Grit check, Grit baseline, injected probe, raw Grit acquisition,
Swooper map bundle freshness repair, apply safety, classify/generator
behavior, or product/runtime proof is claimed.
