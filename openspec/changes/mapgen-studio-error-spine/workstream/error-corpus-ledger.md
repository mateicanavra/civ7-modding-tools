# D3 Error Corpus Ledger

Status: draft corpus
Date: 2026-06-14

| Surface | Current evidence | D3 target | Risk if omitted | Oracle |
| --- | --- | --- | --- | --- |
| Failure bridge | `apps/mapgen-studio/src/server/studio/engineErrors.ts` defines `StudioEngineError(statusCode, details?: unknown)`. | Replace public truth with typed failure ADT and constructors. | Status code remains the domain model and D4 cannot adopt typed Effect failures. | Negative search for status-code bridge truth; ADT constructor tests. |
| Router/context mapper | `apps/mapgen-studio/src/server/studio/context.ts` maps status-derived `StudioEngineFailureKind` to raw `ORPCError`. | Package-owned mapper total over namespace plus failure tags; raw ORPC construction router/runtime-only. | Known categories can drift from contract and mapping remains app-hosted. | Mapper totality tests and raw ORPC ownership scan. |
| Declared error data | `packages/studio-server/src/contract/errors.ts` uses permissive `details?: Type.Unknown()`. | TypeBox-owned sanitized failure data per error family. | D2.5 unknown bridge becomes durable protocol. | TypeBox Value.Check/Parse tests; negative search for expected-failure `Type.Unknown`. |
| Run in Game validation | `requestValidation.ts` throws `Error`; `engines.ts` wraps selected parse failures as 400. | Validation helpers emit typed `InvalidRequest` failures or return parse results consumed into typed failures. | Validation messages can bypass failure taxonomy. | Scenario tests for raw-control, recipe id, config, seed, map size, player count failures. |
| Run in Game operation status miss | `runRunInGameStatusEngine` throws 404 `StudioEngineError` with request id details. | `OperationNotFound` with daemon identity in wire data. | Restart detection regresses or missing request id becomes anonymous. | Handler/client 404 identity test. |
| Run in Game operation-state failure projection | `apps/mapgen-studio/src/server/runInGame/operationState.ts` consumes `StudioEngineError` / `Civ7DirectControlError` and projects status failure text plus recovery actions. | Operation-state projection consumes typed failures and emits TypeBox recovery-action values plus redacted diagnostics. | UI/current-operation state can keep a second failure taxonomy after router errors are fixed. | Operation-state tests for blocked, failed, uncertain, direct-control unavailable, redaction, and recovery vocabulary. |
| Run in Game materialization/proof | `engines.ts` throws several 500/503 `StudioEngineError`s for setup visibility, mapgen failure, exact authorship, launch/log proof. | Distinguish materialization, dependency unavailable, and proof failed tags with typed diagnostics. | All failures collapse into `RUN_IN_GAME_FAILED`. | Failure-path scenario tests and mapper cases. |
| Save/Deploy validation/path guards | `mapConfigs/requestValidation.ts` throws `Error`; `engines.ts` wraps parse/path failures. | `InvalidRequest` typed failures with bounded path diagnostics. | Path guard failures become generic invalid or failed with opaque details. | Request parser tests and path guard scenario. |
| Save/Deploy deploy/rollback | `runSaveDeployEngine` records failed operation details and rollback failure in unknown-ish details. | `DeployFailed` with typed saved/deployed/rollback diagnostics. | Rollback data stays untyped and app-only. | Operation-state failure projection tests. |
| Save/Deploy status miss | `runSaveDeployStatusEngine` throws 404 with request id details. | `OperationNotFound` with daemon identity in wire data. | One-mount restart detection parity can regress. | Handler/client 404 identity test. |
| Save/Deploy operation-state failure projection | `apps/mapgen-studio/src/server/mapConfigs/operationState.ts` stores `details: Record<string, unknown>` and free-string recovery actions. | Operation-state projection consumes typed failures and emits declared deploy/rollback diagnostics plus TypeBox recovery-action values. | Save/Deploy current-operation state remains an unknown-details tunnel. | Operation-state tests for deploy failure, rollback failure, unavailable dependency, redaction, and recovery vocabulary. |
| Autoplay mutex/dependency | `runAutoplayEngine` throws 409 and wraps direct-control as 503. | `OperationBlocked` and `DependencyUnavailable` typed failures. | Autoplay stays a side channel outside the runtime failure spine. | Autoplay mapper and scenario tests. |
| Autoplay start/stop failure | The frame requires Autoplay start/stop failure coverage even if current code does not model it explicitly. | `AutoplayStartStopFailed` mapping to `AUTOPLAY_FAILED`. | `AUTOPLAY_FAILED` stays an untyped catch-all. | Autoplay start/stop failed mapper and scenario tests. |
| Autoplay verification failure | The frame requires Autoplay verification failure coverage. | `AutoplayVerificationFailed` mapping to `AUTOPLAY_FAILED`. | Autoplay verification outcomes become defects or generic failed states. | Autoplay verification failed mapper and scenario tests. |
| Operation lifecycle | D4 frame requires expired operation, daemon identity mismatch, runtime disposed, and unsupported operation type outcomes. | `StudioOperationFailure` lifecycle variants with deterministic public projection. | D4 invents a second taxonomy or treats lifecycle outcomes as defects. | Mapper totality tests and D4 service-union tests excluding defect containment. |
| Unknown direct-control exception | `context.ts` maps `Civ7DirectControlError` to namespace unavailable with unknown details. | Typed dependency unavailable data with sanitized direct-control code/cause. | Raw direct-control payload leaks through public error data. | Sanitized cause schema test. |
| Unknown non-domain exception | `context.ts` maps to namespace failed. | `UnexpectedDefectData` at router edge only. | Defects are confused with expected runtime outcomes. | Handler test for `throw new Error` plus negative expected-failure scan. |
