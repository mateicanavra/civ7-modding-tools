# Phase Record

## Phase

- Project: Habitat Harness
- Phase: H4.5 / `habitat-oclif-cli`
- Owner: Codex
- Branch/Graphite stack: `agent-F-habitat-oclif-cli` above `agent-F-intelligence-bridge-ui-bundle`
- Started: 2026-06-13
- Status: Implemented and verified locally; ready for Graphite commit

## Objective

- Target movement: migrate the repo-local Habitat command surface from the H2 Bun hand parser to the repo-standard oclif command package shape without changing rule semantics, ratchet semantics, JSON schema, or exit behavior except for oclif-native help/parse formatting.
- Non-goals: no new harness rules, no taxonomy changes, no new baselines, no product/runtime architecture changes, no published shared harness package, no generated `dist/**` or `oclif.manifest.json` hand edits.
- Done condition: oclif bin/dev runners, package metadata, command classes, reusable command libraries, tests, README/root/Nx command strings, OpenSpec validation, build/check/smoke probes, downstream realignment, Graphite commit, and clean repo state.

## Authority

- Root/subtree `AGENTS.md`: root `AGENTS.md`; no deeper `AGENTS.md` under `tools/habitat-harness`; `packages/cli/AGENTS.md` read for local oclif precedent.
- Product refs: `docs/projects/habitat-harness/FRAME.md` D7, hard core #1/#2/#5.
- Architecture refs: `docs/projects/habitat-harness/taxonomy.md`, `docs/projects/habitat-harness/workstream-record.md`, `tools/habitat-harness/README.md`, H2 scaffold spec.
- Project refs: `openspec/changes/habitat-oclif-cli/proposal.md`, `tasks.md`, `specs/habitat-harness/spec.md`.
- External refs: oclif docs read 2026-06-13: Introduction, Commands, Flags, Args, Testing, Configuring CLI, Command Discovery Strategies, JSON, Hooks, ESM.
- Excluded/stale inputs: H2 `src/bin/habitat.ts` parser is scaffold evidence only; it is not the durable target shell after Matei's oclif decision.

## Current State

- Repo/Graphite state: dirty worktree on `agent-F-habitat-oclif-cli` with owned H4.5 implementation files; ready to stage/commit after final diff check.
- Dirty files and owner: all dirty files belong to this oclif slice (`tools/habitat-harness/**`, root scripts/lockfile, `.gitignore`, `vitest.config.ts`, OpenSpec phase record/tasks).
- Current code evidence: `tools/habitat-harness/src/bin/habitat.ts` is now an oclif shim; command behavior lives in reusable `src/lib/command-engine.ts` and oclif command classes under `src/commands/**`.
- Generated outputs affected: `dist/**` and `oclif.manifest.json` are regenerated by build and ignored; neither is hand-edited or staged. `habitat graph` now writes Nx graph output under OS temp and cleans it up, so no `.nx/habitat-graph.json` status drift remains.
- Tests/guards affected: harness command tests, `@internal/habitat-harness:build`, `:check`, `:test`, root `habitat:*`, Nx inferred `habitat:check`, Biome targets, OpenSpec validation.

## Scope

- Write set: `tools/habitat-harness/package.json`, `bin/**`, `src/base/**`, `src/commands/**`, `src/lib/**`, `src/bin/**`, `src/plugin.js`, `README.md`, `tsconfig.json`, root `package.json`, harness tests/config as needed, OpenSpec phase artifacts/tasks.
- Protected files: generated `dist/**`, generated `oclif.manifest.json`, unrelated package sources, existing baselines unless a verification gate proves an oclif-specific issue.
- Owners: `@internal/habitat-harness` command shell and local enforcement entrypoints.
- Forbidden owners: product CLI replacement in `packages/cli`, rule-pack taxonomy, generated bundle outputs, runtime control architecture.
- Consumer impact: existing `bun run habitat:*` calls continue to work; help becomes oclif help; invalid command formatting becomes oclif formatting.
- Downstream assumptions: H5-H8 should extend oclif command classes, not the H2 hand parser. If Effect internals are added later, scopes must close before `Command.run()` resolves.

## Compatibility Matrix

Old H2/H4 runner snapshot captured before implementation:

| Probe | Old exit | Old behavior | Expected oclif delta |
| --- | ---: | --- | --- |
| `bun run habitat` | 0 | Prints scaffold usage and rule count. | Oclif help/list output is acceptable. |
| `bun run habitat -- --help` | 2 | Treats `--help` as invalid command, prints scaffold usage. | Verified exit 0 with generated oclif root help. |
| `bun run habitat -- nope` | 2 | Prints scaffold usage. | Oclif invalid-command formatting acceptable; non-zero preserved. |
| `bun run habitat:check -- --json --rule doc-ambiguity` | 0 | Emits `CheckReport` JSON with `doc-ambiguity` plus `baseline-integrity`; advisory finding does not fail. | Verified schema/rule selection/exit preserved. |
| `bun run habitat:check -- --json --output /tmp/habitat-old-check-output.json --rule doc-ambiguity` | 0 | Writes same schema to output path and also prints JSON to stdout. | Verified output-file and stdout behavior. |
| `bun run habitat:fix -- --dry-run` | 0 | Runs `bunx --bun @biomejs/biome check .`; no writes. | Verified exit 0 after Biome formatting. |
| `bun run habitat -- graph --json` | 0 | Emits compact Nx graph JSON. | Verified parseable graph JSON and no `.nx/habitat-graph.json` drift. |
| `bun run habitat -- classify tools/habitat-harness/src/bin/habitat.ts` | 0 | Emits project/tags/rules/verifyTargets JSON for `@internal/habitat-harness`. | Verified same shape against `src/commands/check.ts`. |
| `bun run habitat -- hook pre-commit` | 0 | Prints H7 not-wired stub. | Verified stub preserved until H7. |

## Spec/Tasks

- Spec/proposal: `openspec/changes/habitat-oclif-cli/`
- Tasks: `openspec/changes/habitat-oclif-cli/tasks.md`
- Validation status: `bun run openspec -- validate habitat-oclif-cli --strict` passed after implementation.

## Review

- Review lanes: local oclif precedent review; command lifecycle review; JSON compatibility review; generated-output discipline review.
- Blocking findings: none open at phase start.
- Accepted findings repaired: N/A.
- Rejected/invalidated/waived/deferred findings: `effect-orpc` not used in this CLI slice; structure/lifecycle guidance retained for future Effect internals.

## Agent Fleet State

- Active agents: none known active at phase open.
- Completed agents: Halley and Newton oclif research lanes completed before this phase implementation; both converged on oclif package layout, command classes, bin scripts, manifest generation, shared libs, and no single-file bundle.
- Assigned write sets: solo integration owner now owns implementation.
- Latest evidence by agent: oclif docs align with repo pattern; `effect-orpc` not indicated for this local CLI shell.
- Open findings by agent: none.
- Running/stale status: no stale process/agent sessions known.
- Integration owner: Codex.

## Implementation

- Completed tasks: all tasks in `openspec/changes/habitat-oclif-cli/tasks.md` completed and checked.
- Remaining tasks: stage and commit via Graphite; continue broader Habitat train after restack.
- Stop conditions triggered: none.

## Verification

- Commands run:
  - `bun run habitat`
  - `bun run habitat -- --help`
  - `bun run habitat -- nope`
  - `bun run habitat:check -- --json --rule doc-ambiguity`
  - `bun run habitat:check -- --json --output /tmp/habitat-old-check-output.json --rule doc-ambiguity`
  - `bun run habitat:fix -- --dry-run`
  - `bun run habitat -- graph --json`
  - `bun run habitat -- classify tools/habitat-harness/src/bin/habitat.ts`
  - `bun run habitat -- hook pre-commit`
  - `bun run openspec -- validate habitat-oclif-cli --strict`
  - `bun run habitat -- --help`
  - `bun run habitat -- check --help`
  - `bun run habitat:check -- --json --output /tmp/habitat-new-check-output.json --rule doc-ambiguity`
  - `bun run habitat:fix -- --dry-run`
  - `bun run habitat -- graph --json`
  - `bunx nx run @internal/habitat-harness:build --skip-nx-cache`
  - `bunx nx run @internal/habitat-harness:check --skip-nx-cache`
  - `bunx nx run @internal/habitat-harness:test --skip-nx-cache`
  - `bunx nx run-many -t biome:ci --projects=@internal/habitat-harness --skip-nx-cache`
- Results: all final verification commands passed after Biome formatting. Help smoke exits 0 for root and check help. JSON output file validates `schemaVersion: 1`, rules `doc-ambiguity,baseline-integrity`, `ok: true`. Graph JSON exits 0 and leaves `.nx/habitat-graph.json` absent.
- Skipped gates and rationale: full `habitat verify`/`nx affected` not run in this slice because H4/H5 train still has known broader workstream blockers; this slice ran the command-string, build/check/test, JSON, graph, and Biome gates scoped to oclif migration.
- Evidence boundary: proves oclif CLI shell migration and command compatibility; does not close broader H4 2.4 SDK root-test blocker or downstream H5-H8 implementation.

## Realignment

- Downstream docs/specs/issues updated: `tools/habitat-harness/README.md`, `docs/projects/habitat-harness/workstream-record.md`, root scripts, Nx inferred target command strings.
- Tests/guards updated: added `tools/habitat-harness/test/commands/habitat-commands.test.ts`; added `habitat-harness` Vitest project; added generated manifest ignore.
- Deferrals/triage updated: no new deferral; `effect-orpc` remains not indicated for CLI shell.
- Downstream realignment ledger: phase record is sufficient; downstream H5-H8 proposals already require `habitat-oclif-cli`.

## Next Action

- Exact next step: run final diff/status checks, stage, commit via `gt modify`, then restack.
- First files to inspect: `git diff --stat`, `git status --short --branch`, `gt log short --stack`.
- Stop condition: unexpected dirty/generated files outside the H4.5 write set.
