# Evidence Log - Domain Ops Root Config

| Proof id | Evidence | Result | Proof class | Non-claims |
| --- | --- | --- | --- | --- |
| `DORC-NATIVE-FIXTURES-2026-06-15` | `GRIT_TELEMETRY_DISABLED=true bunx grit patterns test --filter domain_ops_root_config --json 2>&1` from `/Users/mateicanavra/Documents/.nosync/DEV/worktrees/wt-agent-HG-habitat-grit-pattern-chain` | Exit 0. One testable pattern passed. `Matches fixture` produced 10 current-predicate matches: side-effect import from `../../config.js`; default imports from two, three, four, five, and six parent levels; named import; single-quoted default import; namespace import; and type-only import. `Ignores fixture` produced 0 matches across local `./config.js`, one-parent `../config.js`, non-op domain path, other-mod path, `.tsx`, recipe path, extensionless path, JSON path, re-export, dynamic import, and source-string controls. | Native Grit fixture/parser-edge proof and row-owned predicate evidence | No raw direct Grit acquisition, row-specific injected cleanup/path-control closure, baseline mutation, classify/generator behavior, apply safety, retired parity, broader domain-refactor closure, export-from closure, dynamic import closure, neighboring row, or product/runtime proof. |
| `DORC-DOMAIN-OPS-INVENTORY-2026-06-15` | Inline Node/TypeScript compiler API parser inventory over `mods/mod-swooper-maps/src/domain`, excluding `node_modules`, `dist`, and `mod`, with current predicate `mods/mod-swooper-maps/src/domain/.*/ops/.*\.ts$` | Exit 0. Counts: 664 scanned TS/TSX/JSON files; 664 `.ts`; 0 `.tsx`; 0 `.json`; 574 current-predicate `.ts` files; 0 current-predicate `.tsx`; 0 current-predicate JSON; 1,255 current-predicate import declarations; 150 type-only imports; 1,105 value imports; 0 side-effect imports; 6 current local `./config.js` imports; 0 one-parent `../config.js` imports; 0 extensionless parent config imports; 0 JSON parent config imports; 0 upward root-config imports at any depth; 276 current export-from declarations; 0 upward root-config export-froms; 0 dynamic imports; 0 upward root-config dynamic imports; 0 source-string lookalikes; 0 outside-predicate upward root-config imports; 0 parse diagnostics. Current-row match list is empty. | Parser inventory / live zero-candidate record truth | Parser inventory is not native Grit current-tree behavior, Habitat wrapper proof, raw acquisition, row-specific injected cleanup proof, baseline behavior, classify/generator behavior, apply safety, retired parity, broader domain-refactor closure, export-from closure, dynamic import closure, neighboring row, or product proof. |
