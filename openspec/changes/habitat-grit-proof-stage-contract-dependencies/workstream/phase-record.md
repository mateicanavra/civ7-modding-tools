# Phase Record - Stage Contract Dependencies

## Current Gate

The STCD row checkpoint is implemented, verified locally, record-aligned, and
committed for supervisor review.

## Implemented Scope

- Row-owned `stage_contract_dependencies` Grit pattern.
- Active Habitat registration for `grit-stage-contract-dependencies`.
- Explicit empty baseline and injected-probe metadata.
- Parser inventory over standard recipe stage contracts.
- Row packet plus aggregate corpus/proof/command records.

## Proof Boundary

Accepted local proof classes are native fixture proof, parser inventory,
Habitat wrapper/current-tree projection, explicit empty baseline inventory, and
registered injected violation/path-control proof.

Raw direct Grit acquisition, artifact dependency enforcement, generated
artifact parity, semantic DAG validation, source remediation, classify/generator
behavior, apply safety, retired parity, broader recipe architecture closure,
and product/runtime proof remain non-claims.

## Next Actions

- Supervisor review of the STCD checkpoint.
- If accepted, select the next eligible HG Grit row.
