# Phase Record — H1 `habitat-nx-adoption`

## Phase

- Project: habitat-harness (docs/projects/habitat-harness/FRAME.md is controlling)
- Phase: H1 — Nx adoption (train root)
- Owner: workstream owner agent (F)
- Branch/Graphite stack: `agent-F-habitat-nx-adoption` → `agent-F-habitat-harness-workstream` → `main`
- Started: 2026-06-12
- Status: IN PROGRESS

## Objective

- Target movement: Nx fully adopted via the native Turbo migration; turbo retired; `tools/*` workspace glob + mise pin in place; all root scripts and CI re-pointed; build/check/test parity proven.
- Non-goals: no tags, no boundary rules, no harness package (H2/H3); no new enforcement of any kind.
- Done condition: all tasks 1.1–4.6 checked with the verification gates green (graph = 21 projects + scripted edge set-comparison; byte parity on `mod/**`; affected probe; cache gate; turbo gone).

## Authority

- Root/subtree `AGENTS.md`: root AGENTS.md Tooling Defaults (updated in 3.2)
- Product refs: n/a (structure-only)
- Architecture refs: docs/projects/habitat-harness/FRAME.md §4 (de-risk evidence, settled D1)
- Project refs: openspec/changes/habitat-nx-adoption/{proposal,design,tasks,specs}
- Excluded/stale inputs: chat history pre-compaction; FRAME pre-repair numbers (fixed via review ledger F26)

## Current State

- Repo/Graphite state: stacked branch created off workstream branch at `5aca40a29`; worktree clean at start
- Dirty files and owner: none at start
- Current code evidence: turbo.json (18 task entries), turbo 2.7.6 devDependency, no nx.json/mise.toml/tools/
- Generated outputs affected: `mods/mod-swooper-maps/mod/**` (byte-parity target, not regenerated by intent)
- Tests/guards affected: `scripts/lint/lint-workspace-entrypoints.mjs` (nested-turbo rule → nested-nx, task 2.3, expected green at landing — this rule-semantics edit deliberately precedes the H2 ratchet machinery, per ledger F39)

## Scope

- Write set: root `package.json`, `turbo.json` (delete), `nx.json` (new), `mise.toml` (new), `.gitignore`, per-project `package.json` `"nx"` fields (7 project-scoped overrides), `.github/workflows/ci.yml`, `scripts/lint/lint-workspace-entrypoints.mjs`, root `AGENTS.md` + doc sweep, this phase record
- Protected files: everything else; especially `mod/**` outputs (parity target)
- Owners: this phase (solo)
- Forbidden owners: per proposal (no harness package, no eslint/biome/grit changes)
- Consumer impact: every contributor's `bun run build/check/test` path switches engine; script names/semantics preserved
- Downstream assumptions: H2+ assume nx graph + targets exist and root scripts are nx-backed

## Spec/Tasks

- Spec/proposal: strict-valid at `5aca40a29`
- Tasks: openspec/changes/habitat-nx-adoption/tasks.md (1.1–4.6)
- Validation status: `openspec validate habitat-nx-adoption --strict` PASS (pre-execution)

## Review

- Review lanes: spec lane CLOSED pre-H1 (docs/projects/habitat-harness/review-disposition-ledger.md); implementation/evidence/closure lanes run at slice end
- Blocking findings: none open
- Accepted findings repaired: F12, F13, F20, F33, F39 (this slice's share)
- Rejected/invalidated/waived/deferred findings: none

## Agent Fleet State

N/A - solo phase

## Implementation

- Completed tasks: (updated as executed)
- Remaining tasks: 1.1–4.6
- Stop conditions triggered: none

## Verification

- Commands run: (appended as executed)
- Results: (appended)
- Skipped gates and rationale: none
- Evidence boundary: local-commit class only until gt submit; no runtime proof claimed (structure-only)

## Realignment

- Downstream docs/specs/issues updated: task 3.2 doc sweep
- Tests/guards updated: task 2.3
- Deferrals/triage updated: discrepancies → docs/projects/habitat-harness/discrepancy-log.md (append-only)
- Downstream realignment ledger: n/a unless mid-train changes land

## Next Action

- Exact next step: task 1.1 — record `main`-state baselines (build/check/test green + hash `mod/**`)
- First files to inspect: turbo.json, root package.json
- Stop condition: FRAME falsifier — Nx cannot represent the turbo pipeline + bun workspace graph with `bun run check`/CI parity → return to Matei with evidence
