# Systematic Workstream Record

## Frame

- Objective: Build a read-only, source-backed MapGen recipe artifact dependency
  DAG for the selected Studio recipe, served through oRPC + Effect and rendered
  as a full-screen secondary Studio tab.
- Future state: A recipe author can select a recipe, open the DAG tab, see
  phase clusters, stage nodes, artifact dependency edges, and expandable
  sequential steps without reading source files.
- Non-goals: Editable graph mutation APIs; runtime trace overlays; generated
  artifact edits; Civ7 runtime/direct-control changes; phase-as-order semantics.
- Hard core: Artifact edges come from authored `contract.artifacts` producer and
  consumer contracts. Recipe order remains recipe-owned. React consumes a
  prepared DTO and does not infer artifact semantics.
- Exterior: Styling overhaul, in-game proof, map rendering layers, and legacy
  generated Studio artifact formats unless the implementation proves a minimal
  contract addition is required.
- Falsifier: If authored recipe source cannot expose complete stage/step
  artifact contracts to Studio without generated-output imports or forbidden
  deep imports, this frame must be redesigned before implementation continues.
- Redesign trigger: Editable graph work needs mutation semantics that cannot
  reuse the read-only stage/step/artifact identity model.

## Status

- Last updated: 2026-06-10
- Current gate: 11 - Review and closure after implementation verification.
- Next gate: Graphite slice amendment and handoff.
- Blocked by: none.
- Stop condition: Retarget if the unmerged MapGen Studio redesign stack is
  required for correctness.

## Repo State

- Worktree: `/Users/mateicanavra/Documents/.nosync/DEV/worktrees/wt-mapgen-recipe-dag`
- Branch: `codex/mapgen-recipe-dag-visualization`
- Parent branch: `main`
- Stack position: Graphite-tracked slice on `main` for
  `mapgen-recipe-dag-visualization`.
- Dirty files and owner: this workstream owns
  `openspec/changes/mapgen-recipe-dag-visualization/**` and later write-set
  files listed in `proposal.md`.
- Protected paths: original checkout untracked
  `openspec/changes/river-lake-adversarial-workstream-design/`; all generated
  `dist/` and `mod/` outputs.
- Generated/read-only paths: generated Swooper recipe artifacts and mod outputs
  are regenerated by package scripts when needed, never hand edited.

## Corpus Gate

- Corpus source(s): `STANDARD_STAGES`, `BROWSER_TEST_STAGES`, authored stage
  arrays, step contracts, and explicit artifact contracts.
- Corpus shape: mixed action surface/materialization target graph; stages are
  graph nodes and artifacts are dependency edges.
- Coverage ledger: pending implementation tests must enumerate cross-stage,
  same-stage, missing-provider, duplicate-provider, and no-artifact recipe
  cases.
- Open uncertainty: future editable graph semantics are intentionally deferred
  until mutation ownership and artifact edit contracts are specified.

## Proof Gates

- Local stats: not applicable; this is structural graph extraction.
- Generated/deploy proof: only `build:studio-recipes` when exports or generated
  recipe artifacts are touched.
- Runtime proof: not required for read-only recipe source visualization.
- Product proof: browser/component verification that selected recipe load,
  full-screen DAG render, artifact edges, diagnostics, and stage expansion are
  visible and accessible.
- Closure boundary: source tests, Studio build, OpenSpec validation, diff check,
  clean Graphite state.

## Team

- Owner: main Codex agent in this worktree.
- Evidence agents:
  - Turing: recipe/stage/step/artifact corpus exploration.
  - Dewey: oRPC + Effect code pattern exploration.
  - Raman: Studio frontend/visualization exploration.
- Review agents: to be spawned after implementation for bounded review lanes.
- Open findings:
  - Turing confirmed no separate persisted DAG exists; source recipe `stages`
    arrays and step `contract.artifacts` are the right derivation surface.
  - Dewey confirmed trunk Studio has direct oRPC client/server use and
    `@civ7/control-orpc` is the canonical `effect-orpc` example; no TanStack
    Query exists on this trunk snapshot.
  - Raman confirmed no React graph library exists; a deterministic SVG/HTML DAG
    view under `apps/mapgen-studio/src/features/recipeDag/` fits better than
    deck.gl or `packages/mapgen-viz`.
