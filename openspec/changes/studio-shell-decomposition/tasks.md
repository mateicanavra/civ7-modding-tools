## 1. Phase Opening

- [ ] 1.1 Confirm the worktree is on `agent-S-studio-shell-decomposition` off `main`, the analysis corpus is committed, and `StudioShell.tsx` is unmodified at the base commit.
- [ ] 1.2 Re-read `BEHAVIOR-TEST-PLAN.md` §5 (sequencing) and `INVESTIGATION-FINDINGS.md` §7 (contracts) before the first slice.
- [ ] 1.3 Each slice below is its own stacked Graphite branch (`gt create`); one logical change per branch; pure-move and improve-slices never mixed.

## 2. Implementation slices (in dependency order; each: implement → author gating tests → green → review → close)

- [ ] 2.0 **Step 0 — jsdom + RTL test infra.** Add `@testing-library/react` + `@testing-library/dom` + `jsdom` via `bun add -d` (lockfile-safe; mature MIT, no `trustedDependencies` needed). Scope jsdom to controller tests via the vitest-4 per-file `// @vitest-environment jsdom` docblock (NOT `environmentMatchGlobs`, deprecated in v4); add `test/controllers/_setup.ts` polyfilling `ResizeObserver` (jsdom lacks it). Smoke: a trivial `renderHook` under `test/controllers/` passes AND the existing `mapgen-studio` suite stays green in `node`.
- [ ] 2.1 **H0 — module pure-helpers (pure move).** Move `isSaveDeployTerminal` + `saveDeployResultFromTerminalStatus` to `features/mapConfigSave/status.ts`; confirm the auto-run/cascade/selection decision logic that later slices will call is exported pure from `features/*`/`app/*`. Gating: §3.0 (PL-1/12/13, PL-9, RIG-2/4, LS-1/2/3, EO-2, SC-3, BG-2, PL-8, RIG-1/3 — incl. the 4 already-pinned).
- [x] 2.2 **H0b — `useLatestRef` (improve, IMPROVE-2).** Add `app/hooks/useLatestRef.ts` (render-phase write). Gating: IMPROVE-2 (two-render `renderHook` + source-text: no `useEffect`). _Done: hook + `test/controllers/useLatestRef.test.tsx` (3 behavioral, incl. during-render falsifier) + `useLatestRef.source.test.ts` (Vite `?raw` source guard). Mutation-verified (effect-based impl fails both the probe and the guard); full suite baseline clean (6 pre-existing config failures, 0 new). Adoption deferred to owning slices (2.4 `runInGameOperationRef`/`saveDeployOperationCurrentRef`, 2.6 `vizIngestRef`, 2.5 `shortcutsRef`)._
- [x] 2.3 **`useViewportLayout` (pure move).** Lift viewport/ResizeObserver, `deckApiRef` (single owner) + `deckApiReadyTick`, panel geometry, DAG query+prune. NO viz dependency; autofit + backgroundGrid are NOT here (autofit → 2.7b; `backgroundGridEnabled` → host memo after `useVizSelection`). Gating: §3.2 (VL-1, VL-2, **VL-6 — E27 DAG fetch-gate**). Init before `useVizSelection`. _Done: `app/hooks/useViewportLayout.ts` (owns containerRef/viewportSize+RO, deckApiRef/deckApiReadyTick/handleDeckApiReady, panel geometry, DAG query+prune+toggle; reads pipelineExpanded* from viewStore internally) + `features/recipeDag/prunePipelineExpansion.ts` (pure, identity-preserving). Host: removed inline decls/effects + now-unused imports (useRecipeDagQuery, DeckCanvasApi, LAYOUT); destructures hook; autofit (575/584) + backgroundGridEnabled stay in host reading hook outputs. Gating: VL-1 (jsdom RO probe: observes container, tracks resize, disconnects on unmount, 1×1 floor), VL-2 (pure identity), VL-6 (behavioral fetch-gate via mocked query + `?raw` source). Mutation-verified (eager-enable + always-new-set both caught). tsc clean; baseline clean (6 pre-existing, 0 new). Review gate: 2 independent adversarial reviewers (Claude deep pass + Codex) — NO drift, 0 findings across all 10 parity-contract points._
- [x] 2.4 **`useStudioOperations` (atomic move with contract — busy-gate + error channel).** Own `runInGameOperation`/`saveDeployOperation`/`localError` state; derive `runInGameRunning`/`saveDeployRunning`/`error`/`status` synchronously, stable-from-first-render; host completes the composite with `browserRunning`. Gating: §3.3 (BG-1/3/4, BR-12, ADD-2, ADD-3). Init FIRST. _Done: `app/hooks/useStudioOperations.ts` — effect-free coordination hook, called FIRST (before `useViewportLayout`). Owns the 3 state decls + `clearLocalErrorIfCurrent` + the 2 synchronous busy booleans + the 2 `useLatestRef` mirrors (H0b adoption of `runInGameOperationRef`/`saveDeployOperationCurrentRef`). Per design.md resolution of §9-open-#1 (confirmed by the surface-map critic): `error`/`status`/`browserRunning`/`operationControlsDisabled` stay HOST-derived (they need `browserRunner.state`, owned later by `useBrowserRun`) — so BG-3/BG-4 remain host JSX, unchanged. All ~40 consumer sites unchanged (identical destructured names). Host drops now-unused `RunInGameOperationStatus` import. Surface mapped via 3-agent parallel workflow + completeness critic. Gating: ADD-3 (same-render busy invariant: boolean AND latest-ref === op every render), ADD-2 (localError single-owner + identity-guarded clear), BG-1/BR-12 + partition (`?raw` source: no effect, plain-const booleans, `useLatestRef`, NO error/status/browserRunner in hook). Mutation-verified (effect-republish lag fails behavioral+source; in-hook `status` fails partition). tsc clean; baseline clean (6 pre-existing, 0 new). Review gate: 2 independent adversarial reviewers (Claude deep pass + Codex) — NO drift, 0 findings across all 8 parity points (both P1 risks — init-order before all consumers, and the effect-written third ref `saveDeployOperationRef`+waiter machinery correctly left in host — verified)._
- [x] 2.5 **`useKeyboardShortcuts` (pure move).** Lift `shortcutsRef` (via `useLatestRef`) + the global keydown effect; export `shouldIgnoreGlobalShortcutsInEditableTarget` if needed for KB-3. Gating: §3.4 (KB-1..4). _Done: `app/hooks/useKeyboardShortcuts.ts` — owns `shortcutsRef` via `useLatestRef` (H0b adoption #4, the render-phase ref write) + the single global keydown `useEffect`. Host now calls `useKeyboardShortcuts({...})` assembling the action context inline; the two panel-toggle closures STAY host-side (panel-expand/collapse is panel-domain, not keyboard-domain — correct seam). KB-3's `shouldIgnoreGlobalShortcutsInEditableTarget` was ALREADY a named export from `shared/shortcuts/shortcutPolicy` (no new extraction); host's now-unused import dropped. **Deps `[]` decision:** byte-faithful to the original AND lint-clean — `biome.json` is `recommended:false` with no `useExhaustiveDependencies`, and `check` = `tsc --noEmit` only, so the empty array (reading the stable `useLatestRef` ref) needs no suppression. Gating: KB-3 (pure truth-table + behavioral editable-suppression wiring through real keydown dispatch), KB-4 (boundary clamp + `event.repeat` via dispatched events), KB-1/KB-2 (behavioral rerender-freshness + `?raw` source guards: `useLatestRef(context)` render-write, single `addEventListener`, deps `[]`, reads `shortcutsRef.current`, never captures `context` directly). Mutation-verified: Mut A (bare `isEditable`) kills KB-3 wiring (pure stays green); Mut B (drop `===selected` guard) kills both stage boundaries; Mut C (drop `event.repeat`) kills repeat; Mut D (`useLatestRef`→`useRef`) kills freshness behavioral AND the KB-1 source guard (double-gate). tsc clean; baseline clean (293 passed / 6 pre-existing config failures, +13 new, 0 regressions). Review gate: 2 independent adversarial reviewers (Claude deep pass + Codex, which additionally ran `check`→exit 0) — NO drift, 0 findings across all parity-contract points (effect-body byte-identical, latest-ref timing identical, context assembly field-for-field equal, no over-pull, no smuggled improvement)._
- [x] 2.6 **`useBrowserRun` (atomic move — auto-run trio Tier-A; BR-13 improve folded as its own commit).** Lift browser-runner wiring, `startBrowserRun`/`reroll`/`triggerRun`, the auto-run trio (929/939/973) + its refs, `isDirty`/`status`, `vizIngestRef` (render scope). Read busy flags THREADED from `useStudioOperations`. Prefer extracting the trio as a pure state machine (fake timers). Gating: §3.5 (BR-1..13). _Done (2 commits): **A `agent-S-studio-shell-br-browser-run`** (pure move) + **B `agent-S-studio-shell-br13-no-leak-timer`** (BR-13 improve). **Crux design (resolved via surface-map workflow + critic, confirmed by both reviewers):** the host-owned `viz` call PHYSICALLY SPLITS the surface — `browserRunner` must precede `viz` (viz reads `browserRunning`), but `startBrowserRun`+trio+run-actions need `viz` so follow it; only `startBrowserRun` touches viz, and a `vizRef` indirection would change its identity-churn (a forbidden improvement). So `useBrowserRun` (called after viz) owns the COMMAND surface (startBrowserRun + the 3-effect trio + reroll/triggerRun + isDirty), reading authoring/run stores DIRECTLY (established pattern); the HOST keeps the tiny pre-viz runner glue (`vizIngestRef`/`handleVizEvent`/`browserRunner`/`browserRunning` + the render-scope `vizIngestRef.current = viz.ingest` write — the 2.6↔2.7 seam). Args = genuinely-crossing only: `runnerActions`(=`browserRunner.actions`), `browserRunning`, viz handle, `runInGameRunning`/`saveDeployRunning` (threaded), `toast`, `setLocalError`. Dep arrays preserved BYTE-FOR-BYTE (stable setters `setLocalError`/`setLastRunSnapshot`/`setRecipeSettings` omitted as in original). `error`/`status` composite stay host (BR-11/BR-12 host invariants preserved). `setLastRunSnapshot(null)` preset-reset stays host. Gating: 19 tests (BR-1..13 behavioral with fake-timer reactive-runner model + `?raw` source guards). Trio chose behavioral fake-timer tests over pure-SM extraction (preserves byte-level parity; SM is a future improve). **Mutation pass found + fixed a THEATER gate:** initial BR-1 passed even with E1 fully deleted (E2's cleanup masked it) — traced E1's unique job (clearing the deferred-pending flag) and rewrote BR-1 to gate it; also established BR-2 as the completion-correctness gate (catches both drop→0 AND duplicate→2 via E3-flush + E2-cleanup). 8 gates individually mutation-killed (BR-1 E1-pending, BR-2 E3+cleanup, BR-3 E2-busy, BR-4 E2-overrides, BR-5 debounce, BR-8 overrides-ternary, BR-9 seed-abort, BR-10 isDirty-multifield); BR-6/7 falsifiable by construction; BR-13 = co-location structural guard + mid-debounce behavioral lock. tsc 0; baseline 310 passed / 6 pre-existing, 0 regressions. Review gate: 2 independent adversarial reviewers (Claude deep pass + Codex, latter ran `check`→0) — **NO drift, 0 findings across all 8 parity points** (startBrowserRun byte-identical, trio verbatim+source-order, dep-array fidelity, host-invariant preservation, no over-pull, B additive-only)._
- [ ] 2.7 **`useVizSelection` (atomic move — cascade + 4th atomic group; HIGH risk).** Lift `useVizState` + the full stage/step/dataType/space/mode/variant/era/overlay cascade; sole owner of stage/step/viz writes; `overlayDataTypeKey` render-phase before `useVizState`; keep 867's `exhaustive-deps` suppression; era-clamp(2164)→overlay-pref(2198) co-resident ordered. Gating: §3.6 (SS-1..6, LS-1..7, EO-1..6).
- [ ] 2.7b **`useDeckAutofit` (atomic move — deck-autofit ordered pair; lifted AFTER `useVizSelection`).** Lift the autofit effects (608/617) + `lastAutoFitSpaceRef`/`hasEverSeenVizManifestRef` + the Fit-button handler; receive `deckApiRef`/`viewportSize`/`deckApiReadyTick` from `useViewportLayout` AND the viz read-projection (`activeBounds`/`manifest`/`effectiveLayer.spaceId`) by value from `useVizSelection`. Gating: §3.2a (VL-3, VL-4, VL-5, LS-7). Init AFTER `useVizSelection`.
- [ ] 2.8 **`usePresetLifecycle` (atomic move with contract — markPresetApplied + applyAuthoringSnapshot).** Lift catalog/local presets, `resolvePreset`, apply-effects (410→435 Tier-A), `lastAppliedPresetRef` sole ownership; expose `markPresetApplied` (synchronous, identity-preserving; `rememberRepoBackedConfig` runs BEFORE the key-flip `setRecipeSettings`) + `applyAuthoringSnapshot` (5-setter ordered write). Host keeps `livePresets[0].config` referentially the proved-source config. Gating: §3.7 (PL-2..15, **ADD-1 save-path + ADD-1b live-sync-path identity**).
- [ ] 2.9 **`useSaveDeploy` (atomic move with contract — calls markPresetApplied; waiter E21).** Lift `saveRepoBackedConfigWithState`, the waiter map + effects (548 before 558), `waitForSaveDeployTerminalEvent` (prefer standalone factory); call `markPresetApplied` before `setRecipeSettings`. Init before the adoption effect fires. Gating: §3.8 (SD-1..11, PL-7, PL-11).
- [ ] 2.10 **`useLiveRuntime` (pure move — mount-lifecycle refs; + IMPROVE-1).** Lift snapshot/setup staleness machine, abort/mounted refs, `applyLiveGameState`, relation; accept `orpcClient` as param/context. Remove dead `setLiveRuntimeSnapshot` (IMPROVE-1, separate commit). Init before `useRunInGame`. Gating: §3.9 (LR-2..9, SC-7/8) + IMPROVE-1.
- [ ] 2.11 **`useRunInGame` (atomic move with contract — calls applyAuthoringSnapshot + markPresetApplied).** Lift fingerprint/relation/materialization, `handleRunInGame`, `syncStudioFromLiveGame` (delegates the 5-setter write to `applyAuthoringSnapshot`), `copyRunInGameDiagnostics`. Receive host-computed `provedRunInGameSource`/`runInGameMaterializationMode`. Init after `useLiveRuntime`+`useSaveDeploy`. Do NOT touch the hardcoded `recipeId` (D1). Gating: §3.10 (RIG-2/4/5/6/7) + MAN-1.
- [ ] 2.12 **`useSetupControls` (pure move — setup options + drift + live-game actions).** Lift `setupControlOptions`, `savedSetupConfigModified`, `handleSavedSetupConfigChange`, `handleToggleAutoplay`, `handleExplore` (receive `setLiveRuntime`). Gating: §3.11 (SC-4/5/6) + MAN-3.
- [ ] 2.13 **Host collapse.** Confirm `StudioShell.tsx` is reduced to layout + error-boundary + shortcuts host + coordination wiring (host-owned live-source preset derivations + composite busy/error/status); no residual domain orchestration.

## 3. Review (per slice — close before stacking the next)

- [ ] 3.1 Behavior lane: gating tests green; contract test green for atomic-with-contract slices; Claude/Opus + `codex:review`.
- [ ] 3.2 Architecture-boundary lane: clean seam; refs travel with effects; only declared values cross boundaries; init-order invariants hold.
- [ ] 3.3 Maintainability lane (`dev:review-code-quality`): complexity reduced not relocated; no preserved-bad-practice; no wrapper sprawl.
- [ ] 3.4 `codex:adversarial-review` on this design (once) and on each improve-slice.

## 4. Verification & Closure

- [ ] 4.1 `vitest run --config vitest.config.ts --project mapgen-studio` — full suite green (node + jsdom-scoped controller tests).
- [ ] 4.2 `nx run mapgen-studio:build` + typecheck on touched packages green; `check:worker-bundle` still green.
- [ ] 4.3 `bun run habitat classify <diff>` + Habitat/Nx/Biome gates.
- [ ] 4.4 Manual in-game proof MAN-1/2/3 (run-in-game / live-runtime / setup actions) per `.agents/skills/civ7-operational-debugging`; record SHA + per-flow markers.
- [ ] 4.5 `bun run openspec -- validate studio-shell-decomposition --strict`.
- [ ] 4.6 `git diff --check`; `gt status`; `gt log --no-interactive`; worktree clean or precise handoff packet.
- [ ] 4.7 Confirm coverage ledger: all 106 gating specs accounted for (green / already-pinned / manual-proved); no unflagged behavior change shipped in a pure-move slice.
