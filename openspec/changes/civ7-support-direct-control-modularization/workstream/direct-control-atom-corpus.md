# Direct-Control Atom Corpus

This ledger has the completed direct-control inventory and package-test boundary
reports merged. It is still pre-code planning: do not edit direct-control source
until the target atom row names exact source ranges, module owner,
public/private exports, required tests, consumers, and proof class for the
slice. The current package evidence is still concentrated in one broad
`packages/civ7-direct-control/test/direct-control.test.ts` suite; future source
extraction starts with package-owned atom tests, not source moves.

| Atom Candidate | Source Region | Proposed Owner | Existing Evidence / Consumers | Required Before Extraction | Runtime Proof | Status |
|---|---|---|---|---|---|---|
| Public constants/types/schemas | `index.ts` lines 8-1780: tuner defaults, state names, command constants, API roots, bounds, `Civ7ComponentIdSchema`, runtime probe and play result/input types, operation/postcondition types, capability catalog schemas | `src/primitives/*`, `src/types/*`, `src/constants/*` with package barrel compatibility | `test/public-api.test.ts` now owns component ID schema/assertion, default tuner host/port, state/command constants, API root catalogs, setup parameters, and representative limit constants. CLI and Studio import public barrel. Protected user TODO says tests first, then modularize/export constants/types where consumers need them. | Before source moves, extend API-shape coverage only where a concrete consumer needs a public type/constant. Do not move/export constants from source until owner modules are defined. | not runtime proof for type-only extraction | Completed as first source-adjacent test slice; source extraction still pending. |
| Transport/session/framing | `index.ts` lines 1817-2076 and 3758-3904: config, endpoint discovery, `Civ7DirectControlSession`, state selection, frame encode/parse, socket lifecycle, pending requests | `src/session/{config,error,state,frame,socket,session,execute}.ts` | Package tests cover health/config, command execution, state helpers, playable status, restart/begin, state error classification, restart output, frame parsing, and log proof. All CLI/Studio consumers go through package API. | Add focused `test/session.test.ts` and `test/framing.test.ts`; prove unchanged exports before source moves. | no live proof for pure move; runtime proof for reconnect/state behavior changes | Planned; direct-control owned only. |
| Runtime command source builders | `index.ts` lines 3909+ and 4714-4847: command builders and embedded JS helpers | `src/runtime-sources/*` and atom-local source modules | Direct package tests often match function names; CLI consumers invoke package wrappers | Exact string/source behavior equivalence tests before moves | runtime proof required for embedded JS behavior changes | Planned; high-risk source surface. |
| Notification HUD/materialization | Play types around 913-1000, wrapper around 2925, builder around 4714, source around 5119+ | `src/play/notifications/{view,details,decision-hints}.ts` | Package has one broad notification view test; CLI owns the scenario matrix in notification HUD, queue, and priorities suites. | Add `test/play-notification-view.test.ts`; cover package-owned scenario matrix before moving source. | runtime proof for live HUD/source behavior changes | Planned after CLI HUD suite; package proof gap remains. |
| Notification dismissal/verification | Types around 1005-1045, wrappers around 2935/2946, source around 7491, polling/identity verification around 10472-10534 | `src/play/notifications/{dismissal,verification}.ts` | Package covers verified, engine-front-train-absent, engine-front-dismissed, none-blocker, and expired non-dismissible paths; CLI adds stale nonblocking front evidence. | Add `test/notification-dismissal.test.ts`; include stale-nonblocking and existing verification modes before source moves. | runtime proof for timing/routes/identity behavior changes | Planned after exact dismiss suite. |
| Operation validation/send/postconditions | Operation types around 1190-1341, wrappers around 2996-3130, builders 4722/4729, router around 7753, classifiers 10660-11224; diplomacy/narrative wrappers around 3194/3246 | `src/play/operations/{families,validate-request,postconditions,production-choice,chooser-closeouts}.ts` | Package covers unit operation, production choice, unit target basic/no-op, technology closeout, and culture closeout. CLI has delayed/path-shortfall unit-target cases and diplomacy/narrative coverage. | Add focused tests: `unit-operation`, `production-choice`, `unit-target-action`, `technology-choice-closeout`, `culture-choice-closeout`, `diplomacy-response`, and `narrative-choice`; preserve approval-first semantics and no-repeat-after-unverified semantics. | runtime proof for mutation/postcondition behavior changes | Planned after CLI command surfaces stabilize; diplomacy/narrative package proof missing. |
| Ready unit/city/move preview | Ready-unit types 1392-1449, move preview 1451-1482, ready-city 1484-1549, wrappers 3313/3328/3344, sources 9094/9343/9481 | `src/play/ready/{unit,city,move-preview}.ts` | Package covers ready unit/city and now covers `getCiv7UnitMovePreview` read shape, command routing, bounds rejection, and conservative relationship policy. CLI covers ready unit/city and compact move-preview envelopes. | Add focused `test/ready-unit-view.test.ts` and `test/ready-city-view.test.ts` before source moves; preserve host/port fixture note and relationship-unproven policy shape. | runtime proof for live read/source behavior changes | Unit-move-preview package proof completed; ready unit/city test extraction still pending. |
| Tactical/progression/read lenses | Unit target source 8041, settlement 8258, target candidates 8349, battlefield 8637, destination 8927, traditions 4864, progress dashboard 4975 | `src/play/tactical/*` and `src/play/progression/*` | CLI owns settlement, progression, tactical, and priorities consumer coverage; package lacks direct tests for settlement, target candidates, traditions, progress dashboard, battlefield, and destination reads. | Add `test/settlement-recommendations.test.ts`, `test/traditions-view.test.ts`, `test/progress-dashboard.test.ts`, `test/target-candidates.test.ts`, `test/battlefield-scan.test.ts`, and `test/destination-analysis.test.ts`; assert conservative relationship labels. | read-only live proof only when claiming runtime behavior | Planned; relationship invariant protected. |
| Map/visibility/gameinfo reads | `index.ts` wrappers around 2358-2469 plus bounded map/grid source regions | `src/play/map/{summary,plot,grid,visibility,gameinfo}.ts` | Package covers map summary, plot snapshot, bounded grid cap, visibility, reveal, and GameInfo rows in broad suite. | Add `test/map-and-visibility.test.ts`; preserve hiddenInfoPolicy and local bounds caps before source moves. | runtime proof only for reveal/mutation behavior changes | Newly added row from package-test boundary report. |
| Setup and lifecycle orchestration | Setup types around 690-822, wrappers around 2488-2762, setup/start orchestration around 2488-2787 | `src/setup/{snapshot,map-rows,prepare,start,run}.ts` | Package covers setup snapshots/map rows, prepare/start, reload missing map rows, exit-to-shell orchestration, no replay after socket close, seed mismatch, and begin failure. | Add `test/setup-and-lifecycle.test.ts`; preserve no-replay semantics and expected map row proof. | runtime proof for start/setup mutation behavior changes | Newly added row from package-test boundary report. |
| Autoplay and turn completion | Autoplay types around 849-872, turn completion types around 893-906, wrappers around 2788-2920 | `src/play/autoplay.ts` and `src/play/turn-completion.ts` | Package covers autoplay configure/start/stop; turn-completion exported surface is not yet directly isolated in package tests. | Add `test/autoplay.test.ts` and `test/turn-completion.test.ts`; prove approval gates and status/action result shapes. | runtime proof for autoplay/turn-completion behavior changes | Newly added row from package-test boundary report. |
| Root inspection and runtime API snapshots | Root inspection types around 829-840, App UI/Tuner snapshots around 199-288 and wrappers around 2097-2349 | `src/runtime/{inspection,app-ui-snapshot,tuner-health}.ts` | Package covers App UI snapshot, Tuner readiness, runtime API inspection, shell App UI health, and unready Tuner state. | Add `test/root-inspection.test.ts` or split with session tests; preserve shell/playable classification. | runtime proof for runtime-state behavior changes | Newly added row from package-test boundary report. |
| Capability catalog and proof/log support | Static entries around 10089, catalog exports 3594/3616, file/log proof helpers 3694-3758 | `src/catalog/*` and `src/proof/*` | Package covers log marker proof only; no direct package proof for static/runtime/official-resource catalog functions. | Add `test/capability-catalog.test.ts` and keep log proof in `test/framing.test.ts` or `test/proof.test.ts`; do not use generated output as source authority. | runtime proof only for live proof behavior claims | Planned; catalog package proof missing. |

## Forbidden Owners

- CLI must not own raw socket framing, state discovery, reconnect polling,
  embedded runtime JS, or postcondition classification.
- Direct-control must not own CLI flag parsing, compact envelope formatting,
  batch dismissal policy, or command-specific human guardrail prose.
- oRPC must compose stable direct-control atoms; it must not fork runtime source
  strings or define new proof semantics.
- Effect can be evaluated for transport/stream/fixture mechanics, not introduced
  as gameplay policy ownership before atom seams exist.
