## 1. Candidate Packet And Authority

- [x] 1.1 Open this per-candidate packet before any closure claim.
- [x] 1.2 Confirm taxonomy, retired full-profile guardrail, recipe-compile
  architecture records, corpus ledger, and recovery reference.
- [x] 1.3 Define proof classes, non-claims, and reopen trigger.

## 2. Draft Predicate Proof

- [x] 2.1 Test draft native predicate classes for `?? {}` and
  `Value.Default(...)`.
- [x] 2.2 Include positives for recipe step, domain op, domain strategy, and
  `Value.Default(...)`.
- [x] 2.3 Include controls for stage config, `.tsx`, other mod, non-empty object
  fallback, `|| {}`, qualified lookalike call, and source string.
- [x] 2.4 Remove the draft `.grit` pattern after proof because active
  registration is blocked by live source candidates.
- [x] 2.5 Record `RCM-DRAFT-FIXTURES-2026-06-15`.

## 3. Parser Inventory

- [x] 3.1 Run deterministic TypeScript parser inventory over Swooper recipe and
  domain roots.
- [x] 3.2 Record current-predicate scan roots, broader retired-stage context,
  live candidates, counts, and non-claims.
- [x] 3.3 Record `RCM-RUNTIME-INVENTORY-2026-06-15`.

## 4. Shared Proof And Non-Claims

- [ ] 4.1 Register active Habitat Grit rule.
  - Blocked: five live current-predicate candidates lack source-owner/baseline
    disposition.
- [ ] 4.2 Habitat wrapper/current-tree proof.
  - Non-claim: no active registered rule exists for this candidate.
- [ ] 4.3 Raw direct Grit acquisition.
  - Non-claim: raw direct proof remains separate.
- [ ] 4.4 Baseline file/integrity proof.
  - Non-claim: no baseline is added for an unregistered candidate.
- [ ] 4.5 Injected violation and cleanup/path-control proof.
  - Non-claim: no injected probe is added for an unregistered candidate.
- [ ] 4.6 Source remediation, apply safety, retired parity,
  classify/generator behavior, broader runtime-purity closure, and
  product/runtime proof.
  - Non-claims for this checkpoint.

## 5. Downstream Realignment

- [x] 5.1 Update the corpus ledger candidate row with the blocker disposition.
- [x] 5.2 Update the command proof log with draft fixture and inventory proof
  ids.
- [x] 5.3 Record no proof-matrix row because this candidate is not registered
  as an active Habitat Grit check.

## 6. Verification

- [x] 6.1 Draft native predicate proof for `runtime_config_merge`.
- [x] 6.2 Deterministic TypeScript parser inventory over Swooper recipe/domain
  roots.
- [x] 6.3 `bun run openspec -- validate habitat-grit-proof-runtime-config-merge --strict`
- [x] 6.4 `bun run openspec:validate`
- [x] 6.5 `git diff --check`
- [ ] 6.6 Supervisor review of this blocker checkpoint.
