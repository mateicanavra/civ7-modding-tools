# Evidence Log - Apply Helper Redeclarations

| ID | Command or source | Result | Proof class | Non-claim |
| --- | --- | --- | --- | --- |
| AHR-SOURCE-RHR-2026-06-15 | `RHR-RUNTIME-INVENTORY-2026-06-15` | RHR recorded three live `function clamp01` candidates in Swooper domain strategies. | Corpus extraction / source remediation target set. | Not apply proof. |
| AHR-GRITQL-DOCS-2026-06-15 | GritQL docs and examples for rewrites plus `contains bubble` traversal | The final pattern uses `contains bubble { ... => ... }` to rewrite all non-finite helper call sites in a matched program. | GritQL syntax authority. | Not current-tree proof. |
| AHR-NATIVE-FIXTURES-2026-06-15 | `GRIT_TELEMETRY_DISABLED=true bunx grit patterns test --filter helper_redeclarations_to_imports --json 2>&1` | Exit 0; one testable apply pattern passed. The non-finite fixture rewrites finite, `NaN`, `Infinity`, and `-Infinity` call sites to explicit `clampPct(value, 0, 1, 0)` calls. | Native Grit apply fixture proof. | No Habitat apply registration or transaction proof. |
| AHR-EXACT-DRY-RUN-2026-06-15 | Direct `grit apply` dry-run over the three original live files | Exit 0; processed 3 files and found 3 matches. | Direct exact-root apply discovery. | The final semantic repair also includes P1 call-site repair; not a registered Habitat apply proof. |
| AHR-EXACT-LIVE-APPLY-2026-06-15 | Direct `grit apply --force` over the three original live files plus Biome handoff | Exit 0; processed 3 files and found 3 matches; Biome checked the three changed files. | Direct source remediation discovery. | Not generic transaction or rollback proof. |
| AHR-P1-SEMANTIC-REPAIR-2026-06-15 | Source inspection after P1 repair | No `clampPct as clamp01` alias remains in the touched non-finite files; call sites use `clampPct(value, 0, 1, 0)`. | Record truth / semantic repair. | Not product/runtime proof. |
| AHR-RUNTIME-EQUIVALENCE-2026-06-15 | Bun eval comparing old non-finite helper semantics to `clampPct(value, 0, 1, 0)` | Exit 0; finite values below range, in range, and above range match; `NaN`, `Infinity`, and `-Infinity` all return `0`; mismatches `[]`. | Runtime-equivalence proof for the P1 repair. | Not product/runtime proof beyond representative helper semantics. |
| AHR-RHR-INVENTORY-2026-06-15 | TypeScript parser inventory over `mods/mod-swooper-maps/src/recipes` and `mods/mod-swooper-maps/src/domain` with the RHR current-predicate path regex | Exit 0; 886 scanned TS/TSX files, 344 current-predicate TS files, 159 runtime recipe step files, 185 domain strategy files, and 0 current-row helper redeclaration candidates. | Parser inventory / source remediation result. | Not native Grit current-tree acquisition. |
| AHR-SWOOPER-CHECK-2026-06-15 | `bun run --cwd packages/mapgen-core build`; `nx run mod-swooper-maps:check --outputStyle=static` | Both proof commands exited 0. The graph-owned Swooper check regenerated transient generated outputs during execution; generated-output residue was restored and is not part of this row. | Package-local validation for touched Swooper source. | Raw `bun run --cwd mods/mod-swooper-maps check` remains red on a known generated `studio-current.config.json` gap and is not closure proof. |
| AHR-RHR-CURRENT-TREE-2026-06-15 | `bun run habitat:check -- --json --rule grit-runtime-helper-redeclarations` | Exit 0; selected exactly `grit-runtime-helper-redeclarations` plus `baseline-integrity`; both passed with 0 diagnostics. | Habitat wrapper/current-tree proof for the downstream check after remediation. | Not Habitat apply registration proof. |
| AHR-GRIT-CORPUS-REFRESH-2026-06-15 | `GRIT_TELEMETRY_DISABLED=true bunx grit patterns test --json 2>&1` | Exit 0; 30 testable patterns, 0 failures. | Native Grit corpus refresh. | Not product/runtime proof. |
