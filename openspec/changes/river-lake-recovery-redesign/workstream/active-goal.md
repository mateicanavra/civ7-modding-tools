Active goal:
Finish the river/lake recovery workstream by first redesigning it from live
authority and physical expectations so execution can no longer drift into
wrong-owner fixes or premature closure. The redesign must produce an
architecturally aligned, physically grounded change train in which Morphology
owns terrain/basin precursors, Hydrology owns canonical flow routing plus river
and lake truth, `@civ7/map-policy` owns pure Civ facts and generated semantic
catalog parity, and `map-*` stages own projection/materialization only. It must
cover minor and major rivers plus lakes, remove stale or legacy selector logic
instead of preserving it by inertia, predeclare user-facing knobs that decouple
real decisions, and define same-run Studio/Civ proof plus rendered in-game
acceptance strong enough that the lane cannot be declared done until visible
rivers and lakes are proven or explicitly dispositioned.
