Active goal:
Finish the river/lake recovery workstream end to end from authority-grounded,
Earth-sourced physical benchmarks rather than inherited local behavior. The
remaining change train must keep Morphology as owner of terrain/basin
precursors, Hydrology as owner of canonical flow routing plus river/lake truth
and physical benchmark metrics, `@civ7/map-policy` as owner of pure Civ facts
and generated semantic catalog parity only, and `map-*` stages as
projection/materialization only. Representative earthlike thresholds must be
derived from authoritative Earth hydrology sources and datasets rather than
back-fit from current Swooper output, and user-facing knobs must decouple real
product decisions instead of preserving selector internals. Do not close this
lane until minor and major rivers plus lakes either satisfy same-run
Hydrology/Studio/Civ acceptance with explicit proof-class separation
(`hydrology-truth`, `projection-plan`, `terrain-readback`,
`metadata-readback`, `studio-visible`, `civ-rendered`, `product-acceptance`) or
are explicitly dispositioned by proven engine capability limits and reviewer
agreement.
