## 1. Frame And Authority

- [x] 1.1 Record the superseding river/lake recovery objective in a durable local
  goal artifact.
- [x] 1.2 Audit the live authority set: architecture packet, hydrology docs,
  existing river/lake/parity/acceptance changes, and current repo/worktree
  state.
- [x] 1.3 Record excluded or stale inputs that must not drive the redesign.

## 2. Diagnose And Re-Ground

- [x] 2.1 Reconstruct the full river/lake problem space from upstream hydrology
  truth through downstream rendered visibility.
- [x] 2.2 Predeclare physical expectations, benchmark matrix, and proof classes
  before additional tuning.
- [x] 2.3 Record the clean ownership boundary among Morphology, Hydrology,
  `@civ7/map-policy`, projection stages, and proof tooling.
- [x] 2.4 Reconcile the redesign against the already-installed river/lake
  OpenSpec change inventory and record what each existing change still owns.

## 3. Execution Change Train

- [x] 3.1 Define the execution-ready change set sequence with owner, write set,
  verification gates, and stop conditions for each slice.
- [x] 3.1a Use existing river/lake change ids where they are still correct; mark
  explicit supersessions where they are not.
- [x] 3.2 Identify required parity/acceptance updates and downstream docs that
  must move with the execution slices.
- [x] 3.3 Record the execution handoff boundary and non-negotiable closure
  criteria.

## 4. Verification And Handoff

- [x] 4.1 Run `bun run openspec -- validate river-lake-recovery-redesign --strict`.
- [x] 4.2 Run `bun run openspec:validate`.
- [ ] 4.3 Create the follow-on execution goal only after the redesign/workstream
  artifacts are coherent and validated.
