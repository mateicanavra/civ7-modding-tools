# Phase Record - Domain Engine Imports Active Check

## Current Gate

Gate: active check repair implemented and locally verified, pending supervisor
review. The prior predicate blocker is repaired for the proven static import
subset, the rule is registered with an explicit empty baseline and injected
probe, and current source contains zero exact engine-import candidates under the
domain-op predicate.

## Scope

This checkpoint owns:

- record truth for `habitat-grit-proof-domain-engine-imports`;
- active `domain_engine_imports` predicate/fixture repair;
- parser inventory over `mods/mod-swooper-maps/src/domain`;
- wrapper/current-tree selector proof, explicit baseline proof, injected probe
  proof, and downstream aggregate record updates.

This checkpoint does not own:

- source remediation;
- export-from, dynamic import, source-string, or broader inline type-only
  formatting closure;
- HR classify/generator behavior;
- apply/codemod proof;
- product/runtime proof.

## Evidence

- `DEI-PREDICATE-REPAIR-2026-06-15`: native predicate design repair.
- `DEI-NATIVE-FIXTURES-2026-06-15`: native fixture/parser-edge proof.
- `DEI-NATIVE-CORPUS-REFRESH-2026-06-15`: native sample corpus refresh.
- `DEI-DOMAIN-OPS-INVENTORY-2026-06-15`: parser inventory / current-source
  zero-candidate evidence.
- `DEI-PER-RULE-SELECTOR-2026-06-15`: wrapper/per-rule selector proof.
- `DEI-HABITAT-GRIT-TOOL-2026-06-15`: aggregate Grit wrapper proof with
  DEI present.
- `DEI-BASELINE-FILES-2026-06-15`: explicit empty baseline inventory.
- `DEI-INJECTED-PROBE-2026-06-15`: clean-start injected proof for the
  registered DEI row; aggregate injected-corpus closure remains blocked by
  DDIT.

## Review / Findings

`DEI-PREDICATE-BLOCKER-2026-06-15` is historical and repaired for the bounded
static import subset. The local active-row proof set is complete; supervisor
review remains pending for the repaired active row checkpoint.

## Next Actions

1. Stop for supervisor review before the next HG row.
