# Evidence Log - Domain Engine Imports Candidate

| Evidence id | Source / command | Result | Proof class | Non-claims |
| --- | --- | --- | --- | --- |
| DEI-E1 | `scripts/lint/lint-domain-refactor-guardrails.sh` | The retired full-profile guard checks domain ops for imports from `@swooper/mapgen-core/engine` or `@mapgen/engine`, plus a PCRE negative-lookahead check for non-type engine imports. | Authority / proving-source extraction | Does not prove current native Grit predicate behavior. |
| DEI-E2 | `docs/projects/habitat-harness/grit-pattern-corpus-ledger.md` | The candidate asks for a positive non-type engine import, a type-only control if allowed, parser-edge import forms, current scan, and non-apply disposition. | Corpus extraction | Does not register the rule. |
| DEI-PREDICATE-BLOCKER-2026-06-15 | Native Grit predicate design attempts for `domain_engine_imports` | Structural import-source snippets matched positive value imports but also matched pure `import type` and `import { type ... }` controls. Import-node text guards using `includes` or regex negation still false-positived the controls. A root regex with negative lookahead was rejected because Grit regex does not support lookaround. Root or `contains` regex alternatives without lookahead did not match the positive samples. Unsafe registration surfaces were removed. | Predicate blocker / native parser-edge evidence | No active `.grit` pattern, `rules.json` entry, baseline, injected probe, native positive fixture proof, Habitat wrapper proof, raw acquisition, apply safety, or product proof. |
| DEI-DOMAIN-OPS-INVENTORY-2026-06-15 | Inline Node/TypeScript compiler API parser inventory over `mods/mod-swooper-maps/src/domain` | Scan root: `mods/mod-swooper-maps/src/domain`; exclusions: `node_modules`, `dist`, `mod`; current predicate: `.ts` files under `mods/mod-swooper-maps/src/domain/**/ops/**/*.ts`. Counts: 664 scanned TS/TSX/JSON files, 664 `.ts`, 0 `.tsx`, 0 `.json`, 574 current-predicate `.ts` files, 0 current-predicate `.tsx`, 0 current-predicate JSON, 1,255 current-predicate import declarations, 0 current type-only engine imports, 0 current value engine imports, 0 current side-effect engine imports, 0 total current exact engine import declarations, 0 current engine export-from declarations, 0 current engine dynamic imports, 0 current engine source lookalikes, 0 outside-predicate engine imports, 0 parse diagnostics, and an empty current-row match list. | Parser inventory / current-source evidence | Not native Grit current-tree behavior, not Habitat wrapper behavior, not raw Grit acquisition, not baseline behavior, not injected violation proof, not apply safety, not classify/generator behavior, not retired parity, not broader domain-refactor closure, and not product proof. |
