# Downstream Realignment Ledger: D4 Classify Orientation And Routing

| Downstream Surface | Disposition | Required Action |
| --- | --- | --- |
| D0 compatibility matrix | source-blocking prerequisite | Before D4 source edits, cite concrete D0 rows and one closed D0 action for classify invocation, path JSON, diff JSON, human output, package exports, docs examples, and generated surfaces. |
| D1 command outcome/refusal vocabulary | design-consumable vocabulary | Use D1 refusal, recovery instruction, command outcome, and non-claim vocabulary for malformed/pathless diff, unresolved-owner, graph-refusal, and unsupported-path states. D1 source implementation is not a D4 prerequisite by itself. |
| D2 rule-routing projections | design-consumable; source-blocking until live | D4 design uses `ruleRoutingFacts`; D4 source waits until D2 live projections replace prose `scope` as route authority. |
| D3 graph facts | design-consumable; source-blocking until live | D4 design uses project ownership, target availability, unavailable target, aggregate/workspace target, and `GraphRefusal`; D4 source waits for live D3 facts and does not reconstruct graph truth locally. |
| D14 Authoring Topology Fence | example corpus ready for D14 source implementation | D4 hands D14 examples for `project-path`, `workspace-path`, `diff`, `malformed-or-pathless-diff`, `unresolved-owner`, `graph-refusal`, unavailable targets, D2 unresolved routing, and authoring-looking unsupported requests. D14 may use them only to fence authoring requests and must not infer generator support, MapGen authoring support, rule correctness, target freshness, apply safety, or verify closure. |
| D5/D6/D8 parallel packets | parallel-safe with non-owned facts | D4 must not define baseline authority, diagnostic catalog semantics, or Pattern Authority governance. Those facts may appear only as non-owned/unresolved until owning packets accept and implement their contracts. |
| Packet index | pending final rereview | Update D4 status only after final D4 rereview finds no unresolved accepted P1/P2 blockers. |

## D14 Example Corpus

| D4 state/fact | Example input | State/facts D14 receives | Recovery/human-output obligation | Non-claims D14 must preserve |
| --- | --- | --- | --- | --- |
| `project-path` | `tools/habitat-harness/src/plugin.js` | State `project-path`; D3 owner `@internal/habitat-harness`, project root, tags; D2 `ruleRouting` entries with `coverageKind`; D3-backed runnable target guidance; unavailable targets kept separate. | Human-facing topology text may name the owner, routing facts, and graph-backed target guidance, but must not imply that any target has run. | Does not run targets, prove rule correctness, prove safety, or prove authoring support. |
| `workspace-path` | `package.json` | State `workspace-path`; `workspaceOwner: "workspace"`; D2 workspace gate routing when available; D3 aggregate/workspace target guidance only. | Human-facing text must say this is an intentional workspace surface and must not invent a project owner. | Does not infer project owner, run targets, prove rule correctness, prove safety, or prove authoring support. |
| `diff` | A unified diff touching `apps/mapgen-studio/src/main.tsx` and `packages/plugins/plugin-graph/src/index.ts` | State `diff`; deterministic child `paths`; each child keeps its own path state, owner, routing, target guidance, or recovery facts. | Human-facing text must describe each changed path independently and must not collapse the diff wrapper into one owner, rule list, or target list. | Does not run targets, prove rule correctness, prove safety, or prove authoring support. |
| `malformed-or-pathless-diff` | Literal input `not a diff\njust text` | State `malformed-or-pathless-diff`; reason `no-classifiable-diff-paths`; no `paths`; no runnable targets. | Human-facing text must refuse successful orientation and tell the user to provide a repo path or unified diff with changed-file headers. | Does not infer project owner, prove target availability, run targets, prove rule correctness, prove safety, or prove authoring support. |
| `unresolved-owner` | `notes/not-yet-created.md` | State `unresolved-owner`; reason `no-project-or-workspace-owner`; preserved path; no owner-dependent routing or runnable target guidance. | Human-facing text must state that no project/workspace owner was resolved and ask for a path under a resolved Nx project root or intentional workspace-level file. | Does not infer project owner, prove target availability, run targets, prove rule correctness, prove safety, or prove authoring support. |
| `graph-refusal` | D3 graph reader fixtures for missing project, missing target, malformed graph JSON, Nx read failure, and Nx daemon failure | State `graph-refusal`; D3-owned refusal reason/message; no graph-backed runnable target commands. | Human-facing text must preserve the D3 refusal reason and tell the user to resolve the workspace graph refusal before relying on target guidance. | Does not prove target availability, run targets, prove rule correctness, prove safety, or prove authoring support. |
| unavailable target guidance | `packages/civ7-adapter/src/index.ts` when a selected target is missing from the owning Nx project | State `project-path`; available project targets in `runnableTargets`; missing project targets in `unavailableTargets` with reason `missing-nx-target`. | Human-facing text must keep missing targets out of runnable guidance and may describe them only as unavailable. | Does not prove target availability beyond D3 facts, run targets, prove rule correctness, prove safety, or prove authoring support. |
| D2 unresolved routing metadata | A path owned by a project whose routing fact has `pathCoverage.kind: "unresolved-metadata"` | State `project-path`; D2 `ruleRouting` row with `coverageKind: "unresolved-metadata"` and D2-projected reason. | Human-facing text may surface the unresolved routing reason, but must not treat malformed metadata as normal absence of rules. | Does not prove rule correctness, run targets, prove safety, or prove authoring support. |
| authoring-looking unsupported request | `authoring/topology/request.json` | State is either `unresolved-owner` or another explicit D4 state if the path becomes D3-owned later; no generator or topology support is inferred from the string. | Human-facing text must route authoring support questions to the D13/D14-owned contracts and refuse to claim generator availability from classify alone. | Does not claim generator support, MapGen authoring support, rule correctness, target freshness, apply safety, verify closure, or target execution. |
