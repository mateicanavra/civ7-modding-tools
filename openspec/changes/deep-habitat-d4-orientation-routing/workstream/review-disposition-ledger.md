# Review Disposition Ledger: deep-habitat-d4-orientation-routing

| Finding | Severity | Disposition | Repair Evidence |
| --- | --- | --- | --- |
| Global domain, OpenSpec, information, validation, and cross-domino concern catalogs were applied to the D4 draft scaffold. | Global constraint | applied, not acceptance evidence | Global findings remain corpus-wide constraints only. D4 acceptance depends on D4-specific review rows below. |
| Prior D4 review found the explicit classification state model missing. | P1 | accepted and repaired | `design.md` now defines the closed `ClassifyResult` states and forbidden field combinations. `spec.md` now requires `project-path`, `workspace-path`, `diff`, `malformed-or-pathless-diff`, `unresolved-owner`, and `graph-refusal`. Final rereviews found no unresolved P1/P2 blocker. |
| Prior D4 review found malformed/pathless diff and graph behavior under-specified. | P1 | accepted and repaired | `spec.md` now makes malformed/pathless diff a refusal state and uses D3 `GraphRefusal`/`graph-refusal` for graph read, malformed graph JSON, daemon/read failure, missing project, missing target, and unresolved alias dependency categories. Final rereviews found no unresolved P1/P2 blocker. |
| Prior D4 review found public JSON compatibility unresolved. | P1 | accepted and repaired | `design.md` now has a public surface compatibility table with concrete D0 citations for classify invocation, path JSON, diff JSON, human output, package exports, docs examples, and generated surfaces. Source implementation did not begin until the D0 rows existed. Final rereviews found no unresolved P1/P2 blocker. |
| D4 domain/ontology review found D4 lacked a closed command result ontology and blurred D2/D3 truth with D4 rendering. | P1 | accepted and repaired | `design.md` now names D4 ownership, D2/D3 consumption rules, target ontology, forbidden terminology, and the D3-owned `graph-refusal` state family. Final domain/ontology rereview accepted the repair. |
| D4 code/topology review found current `Classification` and `DiffClassification` permit invalid optional combinations and malformed diff false success. | P1 | accepted and repaired | `design.md` lists forbidden field combinations. `tasks.md` requires characterize -> introduce union -> migrate consumers -> delete invalid optionals, with target-contract tests. Source implementation versions the old DTO exports out instead of adding a second DTO path. |
| D4 TypeScript review found a shallow discriminant or optional DTO extension would not collapse state space. | P1 | accepted and repaired | `design.md` requires a closed union and exhaustive switches. `tasks.md` explicitly forbids building the target model by mutating the old optional DTO. Final topology/cross-domino rereview accepted the repair. |
| D4 OpenSpec/testing review found tasks were design prompts and validation gates could pass while ambiguity remained. | P2 | accepted and repaired | `tasks.md` is now ordered by prerequisites, characterization, target model, D2/D3 consumption, command adapter/export handling, validation, and downstream realignment. `spec.md` carries state-specific scenarios and bad cases. Final OpenSpec/testing rereview accepted the repair. |
| D4 information-design review found artifact decisions hidden outside normative paths and brittle local path repetition. | P2 | accepted and repaired | `proposal.md` and `phase-record.md` now use `$REMEDIATION_DIR/context.md` variables. `context.md` defines D4 path variables. State model and compatibility decisions moved into `design.md` and `spec.md`. Final rereviews found no unresolved P1/P2 blocker. |
| D4 cross-domino review found D0-D3 dependency semantics and D14 handoff under-specified. | P1 | accepted and repaired | `proposal.md`, `design.md`, `tasks.md`, `phase-record.md`, and `downstream-realignment-ledger.md` now distinguish design/spec consumption from source blockers and define the D14 classify example handoff. Final topology/cross-domino rereview accepted the repair. |
| Supervisor DRA note rejected a D4-owned graph state name. | P1 | accepted and repaired | Active D4 packet uses D3 `GraphRefusal`/`graph-refusal`; infrastructure failure cases are D3-owned reason values under that state family. Final rereviews accepted the current vocabulary. |
| Final D4 rereview gate. | P1 | accepted and closed for source implementation | Temporary-supervisor source/API, record/public-surface, and TypeBox/API rereviews accepted the submitted D4 source and records with no unresolved P1/P2 findings. |
