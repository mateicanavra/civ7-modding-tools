# Studio Redesign — Full Restack Handoff (post-integration main)

**Audience:** the agent owning the MapGen Studio redesign lane (`design/mapgen-studio-redesign` stack).
**Trigger:** the Graphite integration project drained **every** other stack to main on 2026-06-12. Your lane is now the only remaining unmerged stack. This doc is the semantic map for your one-time restack onto current main.
**Companion doc:** [`DAG-STUDIO-REDESIGN-HANDOFF.md`](./DAG-STUDIO-REDESIGN-HANDOFF.md) — the recipe-DAG feature handoff (§6 there is your execution mandate; it still applies). This doc covers everything that merged *besides* the DAG.

## 0. What landed on main (merge order)

| Body of work | PRs | Footprint center |
| --- | --- | --- |
| Recipe-DAG visualization | #1587–#1591 | `apps/mapgen-studio` (features/recipeDag, server/recipeDag, App.tsx chrome), `mapgen-core/authoring` |
| Placement realignment s0–s11 | #1565–#1575, #1580 | `mods/mod-swooper-maps` placement/resources/starts, `civ7-map-policy`, `civ7-adapter`, small studio touches |
| CLI command taxonomy | #1576–#1579, #1582–#1586 | `civ7-direct-control`, `civ7-control-orpc`, `cli` |
| Rivers recovery (folded 64→5) | #1596–#1600 | `mods/mod-swooper-maps` hydrology, `civ7-adapter`, **`packages/studio-server` (new)**, `apps/mapgen-studio` |
| Docs/skills trivia | #1583, #1592–#1595 | `.agents/skills`, `docs/projects` |

Restack once, against final main. Expect the heavy reconciliation in `apps/mapgen-studio`; everything else is additive or out of your lane's footprint.

## 1. Per-body semantic heads-up for YOUR restack

### 1.1 Recipe-DAG (already documented)
See the companion doc: 7 colliding files, materially only `App.tsx` (your decomposition deletes the monolith; re-mount the DAG client as a first-class tab in your shell). Preserve the projection contract, the `recipeDag.get` oRPC service, layout/domain/label semantics, and the 5 test suites as behavioral pins.

### 1.2 Placement (small studio surface, one structural change)
- **`apps/mapgen-studio/src/civ7-data/civ7-tables.gen.ts` is GONE.** The Civ7 tables live solely in `@civ7/map-policy` (`packages/civ7-map-policy/src/civ7-tables.gen.ts`, generated by `scripts/civ7-map-policy/generate-tables.ts`). If your lane touches anything importing the old studio-side table copy, re-point to `@civ7/map-policy`.
- `defaultConfig.ts` / `overlaySuggestions.ts` / `pipeline.worker.ts` were realigned to the placement-era recipe config schema; `defaultConfigSchema.test.ts` dropped two "legacy Studio source defaults" tests deliberately (no-legacy doctrine). Don't resurrect them.
- Studio map configs now include `mountain-patch` and `mountain-rivers-patch` (a controlled comparison pair — a schema test pins them identical except river-density knobs).

### 1.3 Taxonomy (no studio code collision, but the control vocabulary changed)
- The canonical control surface for game-UI flows is now in `@civ7/control-orpc`: **`display.queue.current` / `display.queue.close` / `display.explore.request`** (official display-queue control; explore = suppressed tracked reveal grants) and **`view.appshot.capture` / `view.camera.focus`** (window-scoped ScreenCaptureKit capture; camera-to-plot focus).
- Any Studio feature in your redesign that wants reveal/explore/camera/screenshot behavior must call these procedures through the existing `civ7ControlOrpcClient` — do **not** rebuild bespoke reveal or capture paths (the rivers stack's earlier bespoke versions were explicitly dropped in favor of these).
- CLI grammar moved to noun topics (`game map summary|plot|grid|visibility|starts`, `game view appshot|camera`, `game play screen dismiss|show`); `game visibility` was deleted (full migration). Relevant only if your lane shells out to the CLI.

### 1.4 Rivers (the big studio reconciliation, beyond the DAG)
This is new since your base and overlaps your decomposition directly:

- **`packages/studio-server` is restored** (Effect-oRPC package: contract modules `civ7.ts`, `live.ts`, `mapConfigs.ts`, `runInGame.ts`, `studio.ts`; router/services/runtime; mounted at the studio `/rpc` path — see `apps/mapgen-studio/src/server/studioServer/rpcPath.ts` and the vite host mount). **This package is now the canonical Studio server seam.** Your `design/server-orpc` / `design/app-decompose` work must reconcile with it, not parallel it: treat `@civ7/studio-server` as the home for studio state-machine operations (run-in-game lifecycle, map-config deploy via the Turbo build graph + `@civ7/plugin-mods` deploy API, operation-store mutex semantics in the Vite mount). If your lane built a competing server shape, fold yours into this package's contract/router structure — orchestration stays in the Effect layer.
- **`apps/mapgen-studio/src/features/studioServer/studioServerClient.ts`** (new) is the client for it; `App.tsx` instantiates it alongside the DAG and control-orpc clients. Your decomposed shell needs all three clients provided.
- **River/Lake/Floodplain inspector**: `src/features/viz/riverLakeInspector.ts` (new) + `ExplorePanel.tsx` wiring. Semantics to preserve: the inspector separates Hydrology truth / navigable projection / engine terrain readback / Civ metadata readback / lake proof / floodplain application / rendered proof / product acceptance — projection masks must never masquerade as engine truth. Re-express the panel surface in your design system the same way you're re-expressing the DAG tab (treat the merged inspector as semantic source, not chrome).
- **Run-in-Game proof gating**: `src/server/runInGame/proofIdentity.ts` + `features/runInGame/*` now require materialization content proof (request/config/envelope + native river markers in local AND deployed map JS) before Civ launches. Keep this gate intact through your restack — it's a correctness boundary, not chrome.
- **Config migration**: `features/configMigrations/pipelineConfig.ts` migrates retired `map-rivers.knobs.riverDensity` → `navigableRiverDensity` at import time with conflict reporting. Behavioral pin: `test/config/pipelineConfigMigration.test.ts`.
- `package.json` gained the `@civ7/studio-server` workspace dep; `vite.config.ts` gained the `/rpc` mount (alongside the recipe-dag middleware — keep both).

### 1.5 Mechanical notes
- `bun.lock`: take the union (placement, rivers, and dag all touched it; rivers added the studio-server workspace package). After resolving, `bun install` and commit the regenerated lockfile rather than hand-merging hunks.
- `.civ7/outputs/resources` gitlink: main is at `fbc38ef8` — if your lane pins the older `44faecab`, take main's.
- Test counts on main as of this handoff: mapgen-studio **174 tests / 41 files** (recipeDag 5, studioServer 2, riverLakeInspector, pipelineConfigMigration); mod-swooper-maps 569; direct-control 427; control-orpc 345. Green at every survivor tip — use these as your post-restack baseline.

## 2. Suggested restack order for your lane

1. `gt sync --no-restack`, then a targeted `gt restack --branch <your-leaf> --downstack` (your lane is ~36 branches; expect conflicts concentrated in the `design/server-orpc`→`design/app-decompose`→`design/app-shell` region for studio-server/App.tsx, and in whatever branches touch `ExplorePanel`/footer for the inspector wiring).
2. Resolve App.tsx in your favor (your decomposition wins), then re-home: the three oRPC clients (control, recipe-dag, studio-server), the DAG tab, the inspector surface, and the run-in-game proof gate into your decomposed tree.
3. Run the studio + mod suites; port semantic assertions from any chrome-bound tests you replace (`RecipeDagView.test.tsx`, ExplorePanel-adjacent tests) onto your new components in the same change.
4. The integration lane (this doc's author) is done and will not touch your stack; sequencing questions go to the operator.
