# Results — Homeland Rebalance (S7, post-fix)

Local-stats verification of D1–D3 against the S2 baseline. Same instrument and
seed set as `baseline-2026-06-20.md`: `runPlacementMetrics({ width: 84,
height: 54, playersLandmass1: 4, playersLandmass2: 4 })`, seeds 1337–1341
(8 players), shipped swooper-earthlike config on the mock adapter.

## The fix, domino by domino (aggregate over 5 seeds)

| Metric | S2 baseline | +D1 partition | +D2 allocation | +D3 dispersion | Target |
|---|---|---|---|---|---|
| **E5.1** allocationCapacityGap (mean) | 0.353 | 0.263 | 0.088 | **0.088** | ≤ 0.15 ✓ |
| **E5.2** clusteredHalfInSmallLandmass (seeds) | 1/5 | 1/5 | 0/5 | **0/5** | 0 ✓ |
| **E5.2** startShareExcess (mean) | −0.072 | −0.071 | −0.059 | **−0.059** | ≈ 0 ✓ |
| **E5.3** spreadIndex (mean) | 0.838 | 0.856 | 0.797 | **0.943** | ≥ 0.85 ✓ |
| **E5.4** regionReassignedRate (mean) | 0.000 | — | 0.025 | **0.025** | ≤ 0.05 ✓ |

- **D1** rebalanced the region partition: `westLandShare` mean 0.333 → 0.50 (the
  meridian now halves real land), closing ~25% of the allocation gap.
- **D2** closed the rest: player allocation now tracks capacity
  (`westStartShare` ranges 0–0.75 instead of the fixed 0.5), gap 0.263 → 0.088,
  and the headline clustering pattern disappears (0/5). `startShareExcess ≈ 0`
  means where many civs share a landmass it genuinely holds that share of the
  capacity — civs follow the land.
- **D3** restored spread within land-rich homelands: spreadIndex 0.797 → 0.943
  (mean nearest-neighbor spacing 11.4 → 13.5 vs the 14.3 even-dispersion ideal),
  above the pre-fix baseline, without disturbing the allocation.

## Preserved quality gates — no regression (post-fix, 5 seeds)

| Gate | Target | Result |
|---|---|---|
| E1.1 starts on passable settleable land | 0% invalid | 0% (mountain/volcano/lake/water/wonder all 0) |
| E1.3 freshwater within ≤1 tile | ≥ 80% | 100% |
| E1.4 start radius-2 fertility vs land mean | ≥ 1.05× | 1.17× |
| E1.5 min pairwise spacing | ≥ 6, 0 pairs below | 9.2, 0 below |
| E1.6 worst-pair fairness gap | ≤ 0.3 | 0.11 (balanced 5/5) |
| E1.8 starts in climate extremes | ≤ 10% | 0% |

## Build / type / lint gates

- `bun test packages/civ7-map-policy` — 37 pass (primitives + existing).
- `bun test mods/mod-swooper-maps test/placement` — 78 pass.
- `bun run --cwd mods/mod-swooper-maps check` — **fully green (0 errors)**. The
  one pre-existing base error (`derive-placement-inputs:46` read
  `hydrography.slopeClass`, absent on that artifact) is fixed in
  `fix-hydrography-slopeclass-typeerror` at the stack base, which sources
  `slopeClass` from `riverNetworkMetrics` (its real owner). That fix also
  restored real-slope ranking to NW feature group E and broke zero tests.
- `nx run mod-swooper-maps:build` — schema-compile gate passes.

## Proof labels (kept separate)

- **Local stats: PROVEN** (above, mock adapter, 5 seeds).
- **In-game (live engine): OBSERVED** (2026-06-20 03:32 EDT). Mutating
  `studio-run-in-game-live --mutate` gate: map `swooper-earthlike`,
  `MAPSIZE_HUGE` (106×66, 6996 plots), 10 players, seed 1337, launched via
  `runCiv7SinglePlayerFromSetup` (`--from-running-game exit-to-shell`). The full
  53-step standard recipe ran to `[mapgen-complete]` + `"seed":1337` in order
  with **no rejectPattern hit**; every placement step executed without throwing —
  `plot-landmass-regions` (D1 partition) and `plan-starts` (D2 allocation +
  D3 dispersion) included — then `[52/53] assign-advanced-starts` (28 ms) and
  `[53/53] placement.placement` (8 ms) both `ok`. This proves the
  start-distribution change is **live-engine-safe** (no SIGSEGV / null-deref /
  runtime-global gap) — the only failure mode MockAdapter cannot establish, and
  the exact risk the closure rule guards. Durable proof: `Scripting.log`
  03:32:28–03:32:31, configHash `e832953c…`, seed 1337.
  - **Boundary:** one Huge / earthlike / 10-player seed. Does **not** generalize
    to other sizes, seeds, or eras. Distribution *quality* (ER1–ER4) is proven by
    the local stats above, not by this gate; the gate proves the new code paths
    don't crash the engine.
  - **Parity (`final-surface-parity`): not pursued.** The setup-launch path mints
    `requestId: null`, so `exactAuthorshipProof` has no studio `sourceSnapshot`
    and parity returns `blocked` by construction (not `complete`). The
    `in-game observed` gate is the closure requirement and is satisfied.

## Calibration note (tunables)

`balanceBias = 0.5` and `spacingFootprintFactor = 0.75` (in
`CIV7_START_PLACEMENT_POLICY_V0`) and the D3 `1.5×desired` dispersion cap were
chosen on first principles and validated on this seed set. They are the levers
to revisit if live play wants more equal hemispheres (raise balanceBias) or
tighter/looser spread (lower/raise the dispersion cap).
