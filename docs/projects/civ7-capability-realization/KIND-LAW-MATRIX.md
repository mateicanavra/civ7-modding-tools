# Civ7 Capability Realization Kind-Law Matrix

**Status:** Accepted target; implementation waits for the corrected shared
successor
**Verdict:** `LAW_CORRECTION`
**Date:** 2026-07-30
**Scope:** Kinds required by the selected capability-port topology only

## Purpose

This matrix defines the constructive boundary that must be accepted before any
destination law, generator, or source move in this initiative. It distinguishes
current Civ7 law from proposed generic law and assigns every invariant to its
native owner.

The selected shared service direction comes from the corrected successor to the
RAWR HQ Template audit baseline described below. Magic Migration is executable
prior art for
selected-depth law, owner separation, and migration proof. It is not a source
of Civ7 roots, product vocabulary, package manifests, or instance inventories.
An older Magic topology never overrides the latest shared Template law.

## Reading The Matrix

- **Settled** means current accepted Civ7 law remains authoritative.
- **Proposed** means this packet selects a positive law that still requires
  Habitat admission and proof.
- A root grammar describes where a selected instance may live. Placement alone
  does not admit an instance.
- A selected root must satisfy its Habitat member/root grammar. Nx metadata may
  describe and schedule that project, but does not admit its architectural kind.
- `dist/`, `mod/`, `node_modules/`, generated runtime manifests, and
  `*.tsbuildinfo` are recognized output slots, not authored interiors or source
  authority. Blueprint-defined `habitat.toml` is instance authority, not output.
- A bare composition kind is not generable when a specialization is required to
  close its source interior.

## Shared Service Substrate And Exact Gap

The inspected RAWR HQ Template audit baseline is `origin/main` at
`33eabdd741d8961dbb2be0b6d4c7284ac58aad52` (2026-07-30). It establishes the
selected vendor and service direction, but it is not the implementation pin:
its CLI, provider-family, service-module collection, and proof interiors require
the corrections recorded below. Implementation pins the accepted successor
commit after those changes land. The audit baseline selects:

- `@orpc/*` `2.0.0-beta.20`, including the official
  `@orpc/experimental-effect` extension;
- Effect `4.0.0-beta.101`;
- TypeBox `1.3.8`;
- a native oRPC declaration-to-implementation lineage with native error maps
  and handler-supplied error constructors;
- the standalone semantic-service topology and distinct projection-only API
  topology specified below.

The audit baseline keeps the service construction packets for spine,
anchors, context, composition, module isolation, and router authorship under
`.habitat/staged/blueprints/service` while its own corpus burns down. That
staging status means the packets are not yet enforced shared law; it does not
authorize Civ7 to substitute its current residue. This initiative proposes
adopting the corrected shared destination, importing the portable packets, and
promoting them in the same branches that expose and burn down their live red.
Magic Migration at
`a96c84f4ffa7b927b9dc5bdb5e72d0be9b7ec83d` proves the oRPC 2 and Effect 4
substrate in a product migration, but its older module-router and API-shell
topologies remain prior art only.

The accepted Template successor must additionally close the resource
`providers/` family, standalone service `modules/`, API projection `modules/`,
the platform-neutral TypeBox `entity` source packet used by service model
categories, and every admitted proof interior. It must define blueprint
instance anchors and generated manifest facts. Loose-file, invalid-child,
empty/nonempty, and missing-manifest fixtures prove those corrections before
Civ7 imports them.

The canonical runtime realization spec remains authoritative for ownership and
lifecycle: plugins declare service requirements, profiles select providers,
and runtime realization binds, mounts, observes, and releases. The API
composition reuses the shared oRPC service-source construction packet at
`src/service`, but that selected depth remains the API's caller projection. It
does not gain independent semantic service authority.

Civ7 has **not** vendored this latest substrate. Its affirmed service blueprint,
service package, Habitat tool, catalog, lockfile, and patch still encode
`@orpc/*` `1.14.6`, Effect `3.21.3`, TypeBox `1.3.6`, and the patched community
`effect-orpc` `0.5.0`. Its topology additionally requires public
`contract.ts`/`index.ts`, service `context.ts`/`schema/`, and module
`router/index.ts`, none of which defines the destination. Those files,
dependencies, patch, and source relationships are migration corpus.

The Habitat tool's own vendor migration and source topology belong to the
shared Habitat workstream that produces the accepted successor. This Civ7
capability program consumes that sealed substrate; it does not redesign or
half-migrate `tools/habitat` as an unowned third service composition.

No oRPC 1 or community-bridge exception is admitted by default. An exception
requires a bounded incompatibility proof against the exact official oRPC 2 and
Effect 4 substrate, an explicit product-authority decision, and a revised
positive generic law. Migration cost, current tests, or a patched dependency
are not such proof.

## Native Owners

| Invariant | Native owner |
| --- | --- |
| Required spine, finite optional interiors, protected generated slots | Habitat `structure.toml` |
| Bounded source relationships and public/private import routes | Habitat Grit pattern |
| Package exports, exact construction types, assignability | TypeScript and `package.json` |
| Project classification, dependency edges, and scheduling | Nx project metadata and targets |
| Build, generation, currentness, deployment, and proof ordering | Nx targets and `dependsOn` |
| Observable behavior, failure translation, lifecycle, cancellation | The owning kind's closed proof axes |
| Reachability and unused public exports | Knip |
| Instance-specific product inventory | Qualified Civ7 niche or product authority, never a generic blueprint |

## Kind-Owned Proof Topology

Proof structure is part of each blueprint's positive law, not a loose optional
directory and not an instance-by-instance naming choice. Each kind selects the
smallest disjoint confidence layers justified by its risks: public behavior,
contract/type exactness, external collaboration, lifecycle, projection,
assembly, or delivery. Test-only data and helpers remain beside the proof that
consumes them rather than forming a generic cabinet. A layer with no distinct
oracle is omitted rather than populated ceremonially.

The reusable confidence axes are:

| Axis | Oracle |
| --- | --- |
| Contract | Public type, schema, encoding, and error coherence |
| Semantics | Owner-specific value, error, and state outcomes |
| Execution | Lifecycle, isolation, ordering, cancellation, and once-only effects |
| Collaboration | Compatibility with one concrete external substrate |
| Projection | Faithful mapping between caller input/output and declared capability |
| Assembly | Exact members, profiles, roles, and bootstrap plan |
| Delivery | Rendered artifact, currentness, placement, deployment, and live compatibility |

These axes decide who owns an oracle, not how many assertions to write.
Within an admitted leaf, cases remain risk-proportional and falsification-first:
they target durable public behavior, failure boundaries, and state transitions,
not implementation keys or coverage quotas. Exact source mirrors make public
command, operation, profile, entrypoint, and adapter subjects visible; they do
not require duplicating SDK guarantees or freezing every internal detail.

The selected proof constraints are below. A proof shape never admits its kind;
the state column remains authoritative:

| Kind | Admission state | Closed proof shape |
| --- | --- | --- |
| Package | Proposed shared packet | Optional closed `test/contract/` and `test/semantics/` axes; the instance manifest selects exact component ids in each admitted axis and at least one axis is nonempty |
| Resource | Proposed shared correction | Required `test/contract/contract.typecheck.ts`; optional colocated `contract.test.ts` only for runtime schema/failure values |
| Provider | Proposed shared correction | Required exact `test/semantics/provider.test.ts` and `test/execution/lifecycle.test.ts`; optional exact `test/collaboration/provider.live.test.ts` only for real substrate compatibility |
| Standalone service | Proposed shared correction | Required exact `test/contract/client.typecheck.ts`, operation-mirrored `test/semantics/modules/<module>/<operation>.test.ts`, and exact `test/execution/root.test.ts`; manifest-selected direct semantics suites are optional and reserved for cross-module service invariants |
| Server API plugin | Proposed shared correction | Required exact `test/contract/client.typecheck.ts` plus manifest-selected `test/projection/<component>.test.ts`; optional manifest-selected `test/execution/<component>.test.ts` only for API-owned scoped behavior; its selected service-source interior owns no proof or independent semantic authority |
| CLI topic | Accepted ownership; proof correction pending | Required `test/tsconfig.json` plus an exact source-derived mirror in `test/commands`; optional source-derived `test/adapters`; fixtures stay beside their consumer |
| CLI shell | Accepted ownership; shared-app composition/proof/anchor correction pending | Shared app `definition`, `profiles`, and `entrypoints` layers plus exact `test/assembly/shell.test.ts` and one `test/tsconfig.json`; shared runtime owns Oclif lifecycle proof |
| Web projection | `UNCONSTRUCTIBLE` candidate minimum | Required nonempty `test/views/*.test.tsx`; optional nonempty `test/interactions/*.test.tsx` and `test/execution/*.test.tsx` selected by the qualified product |
| App realization | `UNCONSTRUCTIBLE` candidate minimum | Required `test/definition.test.ts` and `test/tsconfig.json` plus exact source mirrors for profiles and entrypoints; selected cold adapters add an exact `test/execution/adapters/<adapter>.test.ts` mirror |
| Mod definition | `UNCONSTRUCTIBLE` candidate minimum | Required `test/definition.test.ts`; each qualified family packet closes its semantic layers; the selected map packet uses domain/module/artifact/operation/strategy and recipe/stage/step ownership |
| Mod realization | `UNCONSTRUCTIBLE` candidate minimum | Generic app definition plus exact profile/entrypoint mirrors; required selected `test/artifact/*.test.ts`, `test/deployment/*.test.ts`, and `test/live/*.live.test.ts`; optional selected `test/runtime/*.test.ts`; currentness, deployment, and live remain distinct Nx targets |

All proof directories are closed. `*.fixture.ts` or `*.fixture.tsx` may live
only beside the suites that consume them and are never suites. There is no
generic `support`, `shared`, `behavior`, `mechanics`, or `integration` cabinet;
those names are admitted only when a kind gives them one unambiguous oracle.

Closed layer grammar is not proof membership. Each kind also names the
authority that selects its finite proof leaves. A source-shaped kind may
require an exact source-to-proof mirror; an app mirrors admitted profiles and
entrypoints; a product-qualified kind records exact proof-component identities
in its instance manifest or an already-authoritative product manifest.
An instance may select members only inside its kind's declared confidence
axes; it cannot invent another layer or redefine the oracle for an existing
one. Selection changes the finite subjects proved inside an axis, never the
testing strategy itself.
`structure.toml` permits only the selected layer grammar, and bounded source
relations require one matching leaf for every selected identity while
forbidding unselected leaves. A `*.test.*` wildcard is therefore only the
terminal filename grammar inside a selected layer, never discovery authority
or permission to invent proof case by case.

Generic kinds use reusable proof layers. Domain-qualified kinds may replace
those layers with a stronger domain grammar where the subject demands it:
MapGen definition proof follows domain/module/artifact/operation/strategy and
recipe/stage/step ownership rather than being flattened into generic package
tests. A qualified proof law may narrow or add to its parent, but never leave
`test/` open, duplicate provider guarantees in consumers, or preserve tests
whose only subject is superseded wiring.

An already admitted kind outside this matrix keeps its accepted qualified proof
grammar; it does not inherit an arbitrary row by analogy. If that kind admits a
`test/` root without closing its internal confidence layers, the kind is not
sealed and generation must remain refused until its proof law is completed.

## Decision Gates

### T1: Nx Role Vocabulary

The current Civ7 Nx taxonomy uses domain-shaped primary tags such as
`kind:control`, `kind:adapter`, `kind:plugin`, and `kind:mod`. The shared
substrate uses structural `type:*` facts plus orthogonal role, surface,
resource, provider, and product/capability facts. Civ7's current vocabulary
cannot select this topology without ambiguity:

- current `kind:plugin` means reusable helper packages, not projections;
- current `kind:adapter` exclusively owns Civ7 engine-global access;
- current `kind:control` combines resource, service, and host concerns.

The target graph vocabulary follows the shared convention:

| Structural type | Required qualifying facts |
| --- | --- |
| `type:package` | an accepted `layer:*` or shared-model role when proven |
| `type:resource` | `resource:<resource-id>` |
| `type:provider` | matching `resource:<resource-id>` and `provider:<provider-id>` |
| `type:service` | one product, capability, or domain identity |
| `type:plugin` | the selected role/surface specialization |
| `type:app` | one product/runtime identity; optional product and capability graph facts |

These facts describe project role and graph policy. They do not admit a Habitat
kind, select source topology, or replace blueprint membership. Gate T1 closes
when the target table, complete dependency matrix, workspace project metadata,
and graph proof are accepted together. Reusing current `kind:plugin`,
`kind:adapter`, or `kind:control` as a local graph exception is rejected.
Nx targets schedule entrypoints. `defineApp(...)` owns plugin membership,
profiles own provider selection, and each `startApp(...)` entrypoint owns its
process-role selection.

### T2: Instance Admission Carrier

The current Habitat ontology requires exactly one blueprint-defined instance
manifest, normally `habitat.toml`, at every admitted instance anchor. No
manifest means no admitted instance. Each accepted blueprint packet must
therefore define:

- its anchor grammar and manifest location;
- the blueprint id and version facts;
- every governed root for single-root, nested, or composite instances;
- allowed capability and niche facts;
- validation and migration behavior.

Generators materialize those facts; Nx only corroborates graph identity and
schedules proof. Existing locked CLI path selectors remain operative for the
already-present shell and topic corpus, but this initiative creates no new CLI
instance through them and does not treat them as a generic admission bridge.
Every new package, resource, provider, service, API, web, app, or mod
destination waits for its manifest-bearing packet. Exact instance names in
generic `structure.toml`, Grit patterns, or generator tables remain rejected.

### T3: Generator Interface

After T1 and T2 close, the project generator must accept:

```text
nx g @habitat/cli:project <name> \
  --kind=<admitted-blueprint-kind> \
  --surface=<optional-specialization> \
  --family=<optional-mod-family> \
  --parent=<required-parent-anchor>
```

Only fields required by the selected kind are accepted. `--parent` is required
for nested providers. A bare plugin or app request is refused when its source
interior is not closed until a specialization is selected. Refusals produce an
empty write set. Generated Nx schemas are regenerated from the TypeScript
schema rather than edited by hand. Every successful write also emits the
blueprint-defined `habitat.toml` with id, blueprint version, governed roots,
selected capabilities, and accepted niche facts. It refuses before writing if
the selected blueprint has no accepted anchor contract.

The current generator supports only the legacy helper `plugin` scaffold at
`packages/plugins/plugin-*`. It is not authority or implementation support for
any proposed row below.

## Kind Summary

| Kind at selected depth | State | Root grammar | Habitat selection |
| --- | --- | --- | --- |
| Package project | Proposed upstream closure, then shared-law adoption | `packages/<package>` | pending shared `package` packet |
| Resource contract project | Proposed upstream closure, then shared-law adoption | `resources/<resource>` | `resource` |
| Provider project | Proposed upstream closure, then shared-law adoption | `resources/<resource>/providers/<provider>` | `provider` at the nested root |
| Semantic service project | Proposed shared-law adoption | `services/<service>` | service project at root; private implementation packet at `src/service` |
| CLI topic plugin | Settled ownership law; legacy roots pending anchor and proof correction | `plugins/cli/topics/<topic>` | current `cli-topic-plugin` packet after correction |
| Server API projection plugin | Proposed upstream closure, then shared-law adoption | `plugins/server/api/<plugin>` | `plugin-server-api` at project root plus reusable `service` source packet at the API-owned `src/service` projection depth |
| Web app projection | `UNCONSTRUCTIBLE` until qualified closed law | `plugins/web/app/<app>` | pending qualified web-app packet |
| CLI app | Settled commandless ownership; proposed shared-app composition and anchor correction | `apps/cli` | corrected `cli-shell` specialization composed with the shared `app` packet |
| MapGen Studio app | `UNCONSTRUCTIBLE` until generic app and qualified role laws exist | `apps/mapgen-studio` | pending shared app packet plus Studio specialization |
| Existing Swooper mod split | Legacy product owners; unadmitted until the qualified map envelopes, proof roots, and anchors close | `plugins/mod/map/swooper-physics`, `apps/mods/map/swooper-physics` | pending corrected map definition/realization packets plus nested laws |
| Civilization mod definition | `UNCONSTRUCTIBLE` until qualified closed law | `plugins/mod/civ/<mod>` | pending qualified definition packet |
| Civilization mod realization | `UNCONSTRUCTIBLE` until qualified closed law | `apps/mods/civ/<mod>` | pending qualified realization packet |

## Package Project

**State:** Proposed upstream closure, then direct shared-law adoption.

Magic Migration provides useful package-shell evidence, but RAWR HQ Template
does not yet publish a generic package blueprint. Civ7 does not make the
candidate authoritative locally. The shape below is the proposed upstream
shared packet required before this workstream creates or reclassifies a package.

**Selected depth and root grammar**

- One Nx project rooted at `packages/<package>`.
- Package kind is selected explicitly; it is not inferred for every current
  directory below `packages/`.
- This topology selects no package instance merely to adapt a service or carry
  an oRPC contract. A package owns portable pure protocol, SDK, algorithm,
  schema, mechanics, or support matter with no managed lifetime or product
  policy.

**Required spine**

```text
habitat.toml
package.json
project.json
src/
  index.ts
tsconfig.json
```

`src/` is closed to exact `index.ts` plus optional direct lower-kebab `.ts`
leaves. It admits no child directory. A semantic child collection requires an
independently selected narrower kind rather than recursive generic package law.
Additional file types require a qualified niche.

**Finite optional authored interiors**

```text
AGENTS.md
README.md
test/
tsconfig.paths.json
tsconfig.test.json
tsconfig.tools.json
tsconfig.tsup.json
tsup.config.ts
vitest.config.ts
bunfig.toml
```

Recognized output slots are `dist/`, `node_modules/`, and
`tsconfig.tsbuildinfo`.

A package-owned tool or script interior requires an independently selected
closed tooling kind. Generic package law does not admit `scripts/` as an open
escape hatch.

**Public face and import direction**

- `package.json.exports` exposes only deliberate entries rooted in `src/`.
- `src/index.ts` is the default public entry; additional subpaths point to
  named semantic owners, not broad barrels.
- A plain package consumes only other public package surfaces.
- A package may expose genuinely reusable non-oRPC vocabulary or pure
  mechanics. It never authors or re-exports a service-owned oRPC contract.
- Packages never import resource contracts, providers, services, plugins,
  apps, private source, or generated output.
- Packages never read or mutate ambient process, filesystem, network, engine,
  or host state. Path injection and one-shot execution do not turn an external
  effect into pure package mechanics. A package may parse bytes, derive plans,
  compare snapshots, or serialize values supplied by its caller; a qualified
  runtime adapter performs the effect.

**Proof topology**

If `test/` exists, it is nonempty and closed to optional nonempty
`contract/` and `semantics/` axes. Contract contains `*.typecheck.ts`;
semantics contains `*.test.ts`. The instance manifest selects a finite list of
lower-kebab proof-component ids in each admitted axis, and each selected id
requires exactly one matching leaf. At least one axis and one component are
selected. Unselected leaves are forbidden. Colocated `*.fixture.ts` is allowed
only beside its consuming suite. Structure, reachability, and task wiring
remain Habitat, Knip, and Nx proof rather than package suites.

**Nx mapping**

- Candidate graph metadata: `type:package`.
- Optional secondary: an accepted role such as `role:shared-model` or
  `role:pure-mechanics`.
- A service or runtime adapter is not an admitted package role. The current
  engine-global `kind:adapter` project is migration corpus: its portable port,
  static metadata, and mock may remain package-shaped, while its Civ7-global
  implementation moves to the qualified mod realization.

**Generator**

Unsupported today. After law acceptance, add a package scaffold that writes the
required spine and no product vocabulary. It must not generate a service
adapter, filesystem implementation, or parallel oRPC contract. Any retained
mod-install, save-file, or Studio-run package selects this kind only for its
pure plan, parser, schema, hashing, serialization, or comparison surface.

## Resource Contract Project

**State:** Proposed upstream proof closure, then direct shared-law adoption.

**Selected depth and root grammar**

- One provider-neutral contract project at `resources/<resource>`.
- Static corpora, generated data, and submodules do not qualify as resources.
- A resource represents a foreign capability with a real
  acquire/use/release lifetime. Stateless filesystem calls and service-owned
  process state do not qualify merely because cleanup exists.

**Required spine**

```text
habitat.toml
package.json
project.json
contract.ts
providers/
test/
tsconfig.build.json
tsconfig.json
```

`providers/` is closed, may be empty, and admits only lower-kebab provider
project directories. Loose files and non-provider child directories are
violations. The resource root has no `src/`, implementation directory, or
provider selector.

**Finite optional authored interiors**

```text
AGENTS.md
```

No additional authored root interior is admitted by the shared resource
packet.

**Public face and import direction**

- The package default export resolves to `contract.ts`.
- The contract owns the managed capability interface and typed Effect failure
  vocabulary.
- It may consume product-free package types but imports no provider, service,
  projection, app, or product policy.
- Concrete acquisition, foreign exceptions, reconnect behavior, and selection
  do not appear in the contract.

**Proof topology**

Correct the shared packet before adoption:

```text
test/
  contract/
    contract.typecheck.ts
    contract.test.ts        # optional; only for runtime schema/failure values
```

The contract layer is closed and nonempty. Compile proof demonstrates that a
provider and consumer depend only on the public resource contract; the optional
runtime suite owns exact schema/failure-value boundaries. Provider lifecycle is
not resource-contract proof. The resource instance manifest carries one
generic `runtime-contract-values` proof fact derived from the admitted contract
declaration. Selecting that fact requires `contract.test.ts`; omitting it
forbids the leaf. A bounded relation keeps the fact and the contract's exported
runtime schema/failure values coherent, so the optional leaf is never chosen
case by case.

**Nx mapping**

- Shared graph metadata: `type:resource`.
- Required identity: `resource:<resource-id>`.
- No surface selector.
- Resource projects may depend on `type:package`; no project may depend on a
  resource's provider through the resource contract.

**Generator**

Unsupported today. After law acceptance, generate the contract project and an
empty closed provider family, but no concrete provider.

## Provider Project

**State:** Proposed upstream proof closure, then direct shared-law adoption.

**Selected depth and root grammar**

- One concrete realization at
  `resources/<resource>/providers/<provider>`.
- Its parent anchor must already be an admitted resource.

**Required spine**

```text
habitat.toml
index.ts
project.json
test/
tsconfig.json
```

Additional provider-owned implementation modules are direct root `*.ts`
leaves. Protocol, session, transport, and vendor decomposition must remain
behind that finite flat provider face unless a narrower accepted blueprint
admits a semantic interior.

**Finite optional authored interiors**

```text
AGENTS.md
*.ts
```

No `package.json`, `src/`, README, or other authored root interior is admitted
by the shared provider packet.

**Selected-depth relation**

- The parent root is independently admitted by `resource`.
- The nested root is independently admitted by `provider`.
- Resource structure does not apply its package-root shape to the provider
  root; the physical parent relation does not merge the two kinds.

**Public face and import direction**

- `index.ts` exports one implementation assignable to the parent contract.
- The provider imports its parent resource contract and product-free packages.
- It translates foreign failures once into contract vocabulary.
- It never imports services, projections, apps, app configuration, or product
  policy.

**Proof topology**

Correct the shared packet before adoption:

```text
test/
  semantics/
    provider.test.ts
  execution/
    lifecycle.test.ts
  collaboration/           # optional
    provider.live.test.ts
```

Semantics proves public values and foreign-failure translation into only the
resource vocabulary. Execution proves acquire/use/release accounting,
reconnect, interruption cleanup, and once-only disposal. Collaboration is
admitted only for compatibility with a concrete external substrate. The
provider instance manifest carries a generic `live-collaboration` proof fact
when it declares an uncached live-substrate target. Selecting the fact requires
`collaboration/provider.live.test.ts`; omitting it forbids both leaf and target.
These fixed leaves plus the one declared selector are the provider kind's proof
membership authority; a provider cannot invent another suite or layer.
Fixtures are colocated. Workspace test configuration remains at the workspace
owner.

**Nx mapping**

- Shared graph metadata: `type:provider`.
- Required identities: matching `resource:<resource-id>` and
  `provider:<provider-id>`.
- Provider constraints allow only the matching resource, approved packages,
  and its exact external substrate dependencies.

**Generator**

Unsupported today. After law acceptance, require `--parent`; refuse unknown or
non-resource parents before writes. The generator creates no app composition or
resource contract.

## Service Source And Standalone Semantic Service

**State:** Proposed adoption of the accepted corrected successor service law.
The inspected baseline and existing Civ7 service are migration evidence, not
destination authority.

**Selected depth and root grammar**

- The reusable `service` source packet is selected at `src/service` only
  beneath an admitted standalone service or server API project.
- A standalone project rooted at `services/<service>` gives that packet
  semantic service authority and composes it with the service-project envelope
  and proof law.
- An API project may select the same construction packet at `src/service` for
  its own caller projection. That packet consumes public domain-service
  clients; it is not another domain service, facade, or contract-extraction
  owner.
- The private packet owns no `test/` interior. Its containing service or API
  project owns the kind-appropriate proof.

**Required standalone spine**

```text
habitat.toml
package.json
project.json
src/
  client.ts
  service/
    habitat.toml
    base.ts
    contract.ts
    impl.ts
    modules/
    router.ts
test/
tsconfig.json
```

Each module requires:

```text
AGENTS.md
contract/
  index.ts
module.ts
router/
  <operation>.router.ts
router.ts
```

The selected `modules/` collection is closed, nonempty, and admits only
lower-kebab module directories satisfying that module spine. Loose files,
invalid child names, and unselected directories are violations.

Contract directories admit direct kebab-case TypeScript leaves beside
`index.ts`. Router directories admit only named `*.router.ts` operation leaves;
the module-root `router.ts` composes them. Neither module `contract.ts` nor
`router/index.ts` is part of the target.

**Finite optional authored interiors**

At the standalone project root:

```text
AGENTS.md
tsconfig.build.json
tsconfig.test.json
vitest.config.ts
```

Inside `service/`:

```text
db/                         # standalone only; selects database law
middleware/
model/{actors,dto,entities,errors,policy,ports,prompts}/
modules/<module>/middleware/
modules/<module>/model/{actors,dto,entities,errors,policy,ports,prompts}/
```

Root middleware contains direct kebab-case `.ts` leaves with no index or nested
cabinet. Module middleware has one `index.ts` catalog plus direct `.ts` leaves.
Model categories contain direct `.ts` leaves and no index. `entities` remains
unconstructible until its pending shared TypeBox/platform-neutral source law is
accepted. The independently selected database kind owns the complete `db/`
interior and is available only to a standalone semantic service.

No generic `shared`, provider, transport, host, runtime, source-owned proof, or
additional service-root cabinet is admitted. Recognized outputs are `dist/`,
`node_modules/`, and `tsconfig.tsbuildinfo`.

**Public face and import direction**

- `src/client.ts` is the complete public caller and construction face. It
  accepts runtime-supplied ready capabilities and maps them into the
  service's private module ports.
- `src/service/**` is private implementation.
- Services consume runtime-supplied ports and public package types.
- A service may declare a named semantic capability in its public construction
  face without importing the project that realizes it. That public dependency
  descriptor owns the adapter operation signatures and typed failure
  vocabulary. A realizing app may implement the public descriptor through cold
  execution-descriptor references; a consuming app selects one adapter
  identity. Shared runtime validates exact-one compatible selection and lowers
  it into the ready private port supplied to the client constructor. This is
  neither a service-to-app project dependency nor provider selection.
- A service client may type its ready input through the matching public
  resource contract and may consume its own private port types. It imports no
  provider implementation.
- Services import no projection plugin, app, transport mount, ambient Civ7
  global, or generated output.
- Standalone-service-scoped actors, records, transition policy, cancellation
  state, retention policy, and finalizers remain private service model or
  module implementation. They do not become a resource unless they manage a
  foreign acquired capability through a provider-neutral contract. An
  API-selected projection packet admits none of that semantic service state.
- `base.ts` owns `Context` and authors a native `os.$context<Context>()` base
  only when context-authored middleware needs it.
- `impl.ts` owns the sole
  `implement(contract).$context<Context>()` lineage, its unconfigured `impl`,
  configured `service`, and the official Effect extension when procedures are
  Effect-authored.
- Each module descends from its exact configured service branch. Its
  module-root router composes plain operation leaves; the service-root
  `router.ts` performs the sole aggregate implementation through unconfigured
  `impl.router(...)`.
- Contracts use native `.errors(...)`; handlers use supplied `errors.*`
  constructors. The community `effect-orpc` bridge, `ORPCTaggedError`,
  compatibility status tables, and custom error tunnels are not target
  surfaces.

**Containing-project proof topology**

Standalone `test/` is required and closed to:

```text
contract/client.typecheck.ts
semantics/
  modules/<module>/<operation>.test.ts
  *.test.ts                       # optional cross-module invariants
execution/root.test.ts
```

The contract and execution leaves are fixed kind anchors. Semantics mirrors
every admitted module and every admitted operation contract leaf exactly;
there is one suite at
`test/semantics/modules/<module>/<operation>.test.ts` and no unmatched suite.
The instance manifest may select direct `test/semantics/<component>.test.ts`
leaves only for genuine cross-module or service-root invariants. Contract
proves the public client/contract consumer surface. Module semantics prove
public operation outcomes at the exact owner. Execution proves middleware
order, request isolation, cancellation, and once-only root execution.
Colocated fixtures are allowed; generic support, mechanics, database, and
integration cabinets are not. An independently selected database kind owns
database proof. Production never imports proof. The private service source
packet never admits proof; the containing standalone service owns this proof,
while a containing API owns its distinct contract/projection/execution axes.

**Nx mapping**

- Current: the control service uses `kind:control`.
- Target graph metadata after T1: `type:service` plus one
  product/capability/domain identity consistent with the shared taxonomy.
- Service projects may depend on packages and the matching resource contract,
  not providers or apps. The resource edge exists only for the public client
  input and cannot expose acquisition or provider construction.

**Generator**

No supported service generator exists today. Continue refusing service
scaffolds until the shared spine and source packets are imported, the oRPC 2
and Effect 4 dependency family is selected, and target fixtures pass. The law
is then promoted in the coupled burn-down branch so the live red corpus can be
observed and eliminated before that branch lands. The generator must not emit a
compatibility oRPC 1 spine.

## Plugin Specialization Selection

**State:** Settled Civ7 CLI law, proposed shared API adoption after upstream
closure, and unconstructible qualified mod destinations.

The shared broad `plugin` concept intentionally has no universal structural
packet: CLI commands, server APIs, workflows, and mod definitions do not share
one honest source interior. Civ7 does not introduce a `projection-plugin`
super-kind or another generic shell, and does not require a non-enforcing
parent blueprint as ceremony.

Each plugin can be admitted only by one qualified specialization that owns its
complete closed source, proof topology, and instance anchor. The broad `plugin`
concept is ontology, not an additional executable blueprint selection:

| Root grammar | Qualified selection | Public role |
| --- | --- | --- |
| `plugins/cli/topics/<topic>` | current Civ7 `cli-topic-plugin` packet | oclif topic package |
| `plugins/server/api/<plugin>` | `plugin-server-api` at root; shared `service` source packet at the API-owned `src/service` projection depth | caller-facing API |
| `plugins/mod/<family>/<plugin>` | pending qualified definition packet + family | authored mod definition |

An app imports a plugin through its specialization's declared public face. A
plugin may consume public packages, resource contracts, and service clients,
but imports no app, provider implementation, private service source, or
unrelated plugin interior. Provider selection remains runtime-profile-owned.

The selected specialization owns proof and generation. A bare `plugin`
generation request is refused because no universal source shape exists.

Civ7's CLI packets already express the accepted ownership law: `apps/cli` is a
commandless `cli-shell`, while each topic is a package and owns its commands
below `src/commands/**`. Their current path-selected instances remain legacy
enforcement roots until the packet defines and generates blueprint instance
anchors; they are not a manifest-admission exception or precedent for new
construction. The Template packets inspected for this frame incorrectly place
packages below `plugins/cli/commands/*` and require app-owned `src/commands`.
Those are upstream defects to correct, not destination law to import into Civ7.

The terminal CLI root independently selects the corrected shared `app` packet
and the qualified `cli-shell` specialization. Their composed root is closed to:

```text
apps/cli/
  habitat.toml
  package.json
  project.json
  rawr.civ7.ts
  civ7.ts
  bin/
    run.js
  runtime/
    config.ts
    processes.ts
    profiles/
      <profile>.ts
  test/
    definition.test.ts
    assembly/
      shell.test.ts
    profiles/
      <profile>.test.ts
    entrypoints/
      civ7.test.ts
    tsconfig.json
  tsconfig.json
```

`rawr.civ7.ts` owns app identity, plugin membership, and semantic-adapter
identities. `runtime/profiles/<profile>.ts` owns provider, configuration-root,
and process-default selection.
`civ7.ts` is the sole authored CLI role entrypoint. `bin/run.js` is the oclif
executable shim and delegates to that same entrypoint; it contains no second
startup plan. The profile proof mirror is derived from exact admitted profile
identities. Root optional documentation is limited to `AGENTS.md`,
`CHANGELOG.md`, and `README.md`; `TESTING.md` moves to canonical testing
documentation. Recognized output remains `node_modules`,
`oclif.manifest.json`, and `tsconfig.tsbuildinfo`.

This composed packet is proposed law, not an exception to the current
`cli-shell` structure. The burn-down branch replaces the old root structure
atomically after the shared app packet is accepted; it does not make
`runtime/` legal by weakening the current blueprint.

The CLI proof topology is likewise kind-owned. The shell test root is closed to
the `assembly/` layer within that app proof:

```text
assembly/
  shell.test.ts
```

Habitat topology owns commandless app source. A bounded source relation owns
the sole authored `package.json#oclif.plugins` registry and the absence of a
second topic enumeration. The shell suite observes only runtime assembly:
collision-free command discovery, help catalog, executable-shim equivalence,
and delegation to the shared harness. The app's `test/tsconfig.json` is the
sole compiler program. Generic app definition, profile, and entrypoint layers
retain their distinct oracles; the shell does not repeat static structure or
harness lifecycle proof.

A topic test root is closed to:

```text
test/
  commands/
  [adapters/]
  tsconfig.json
```

Command proof membership is derived from admitted production command leaves:
every `src/commands/<path>/<command>.ts` requires exactly
`test/commands/<path>/<command>.test.ts`, and no proof directory or suite may
exist without its matching source path. That layer proves command projection
semantics from flags and arguments through client calls to output and declared
errors using runtime-supplied fakes.

When `src/adapters/` is selected, `test/adapters/` mirrors its exact directory
and leaf identities in the same way. Every
`src/adapters/<path>/<adapter>.ts` requires exactly
`test/adapters/<path>/<adapter>.test.ts`. Adapter proof has one distinct
oracle: adapter-local translation, including any qualified host effect,
failure translation, and cleanup needed to produce the capability consumed by
commands. It never repeats command presentation, public capability behavior,
or pure package semantics. `test/tsconfig.json` is required. Both mirrors are
closed; `*.fixture.ts` may stay beside its sole consumer and carries no
membership authority. A Habitat source-relation rule owns exact mirror
membership; `structure.toml` owns the finite test-root and leaf grammar.

Each runtime-aware topic base carries cold, runtime-readable service/resource
requirement descriptors using the shared authoring helpers. A selected command
inherits exactly one approved semantic or diagnostic base. Native oclif class
loading discovers the selected class; `RuntimeCommand.init()` hands its static
requirements to the surrounding harness before binding. This metadata is not a
command registry or topic hook. Help, version, and unknown-command paths never
enter command initialization and therefore acquire no live capability.

Shared runtime owns Oclif harness execution proof for lazy requirement
discovery, scoped client access, exact-one session reuse, and once-only
finalization across success, command failure, binding failure, partial startup,
help, version, unknown command, interruption, and sequential invocation.
Shell and topic proof do not repeat those lifecycle guarantees or provider and
service behavior. The current recursive `test/support/**` allowance is a
same-kind law defect to remove before this cutover. It is not retained as a
generic helper cabinet.

## Server API Projection Plugin

**State:** Proposed upstream source and proof closure, then shared-law
adoption. The current Civ7 oRPC 1 shell and older Magic shells are migration
evidence rather than target authority.

**Selected depth and root grammar**

- Project root: `plugins/server/api/<plugin>`.
- `plugin-server-api` selects the project root.
- The reusable shared `service` source packet independently selects
  `plugins/server/api/<plugin>/src/service`. This is structural reuse for the
  API's own oRPC projection, not an embedded domain service.

**Required spine**

```text
habitat.toml
project.json
src/
  api.ts
  client.ts
  service/
    habitat.toml
    base.ts
    contract.ts
    impl.ts
    modules/
    router.ts
test/
tsconfig.json
```

The selected service-source packet supplies the same closed module spine and
contract/router relations as the shared service law. Its modules are
caller-facing projection groups. They consume context-supplied public clients
and adapters; they own no domain state, actors, provider lifecycle, database,
or semantic service policy. The API project root owns all proof.

**Finite optional authored interiors**

```text
AGENTS.md
```

No `package.json`, provider, runtime, process, transport, operation registry,
semantic service state, database, or nested proof interior is admitted.

**Public face and import direction**

- `client.ts` is the caller face derived from the complete API contract.
- `api.ts` is the server-side projection face over the selected service-source
  router.
- `src/service/base.ts` owns the exact public service-client and cold-adapter
  context required by projections.
- `src/service/contract.ts` aggregates module contracts and owns the sole
  caller-facing route tree.
- The application host owns Elysia and oRPC transport realization; neither API
  face chooses a transport, process, or provider.
- Module contracts own caller-facing API schemas and errors. Routers translate
  those calls to capabilities supplied through API context; they neither
  reconstruct a domain-service contract nor import service-private source.
- When an API preserves a complete public service-contract subtree at the same
  route identities, one module contract leaf may compose that subtree whole
  and delegate through its public client. It may not pick types or procedures
  from the contract, redeclare its schemas, or import the service router.
- Runtime realization materializes `Context` from public service clients and
  app-selected cold adapters before mounting `api.ts`. Browser callers import
  only `client.ts`.

**Proof topology**

The API kind requires and closes:

```text
test/
  contract/
    client.typecheck.ts
  projection/
    <component>.test.ts
  execution/                # optional; only for API-owned scoped behavior
    <component>.test.ts
```

The contract leaf is fixed. Projection is nonempty and proves mapping from API
input and context capabilities to exact caller output/error behavior without
choosing transport. Execution is selected only when the API itself owns a
scoped stream or projection lifecycle; cross-host startup, mounting, request
transport, service lifecycle, and shared-runtime disposal are never API proof.
Fixtures remain beside their sole consumer.

The API instance manifest selects a finite set of projection and optional
execution component ids from these blueprint-defined axes. Each selected id
requires exactly one matching suite and unselected leaves are forbidden. The
qualified Studio API selects projection components `authoring`, `control`,
`errors`, `router`, and `runs`, plus execution components
`live-game-watcher` and `studio-events`. Wildcards above are filename grammar
only.

**Nx mapping**

- Observed graph metadata may include `type:plugin`, `role:server`,
  `surface:api`, and one capability identity. These facts do not select the
  Habitat packet.
- `plugin-server-api` and the selected `service` source packet are Habitat
  selections, not Nx admission tags.
- The API imports public service clients and package types. It never imports a
  provider, app, private domain-service source, or generated output.

**Generator**

Unsupported today. The API generator must compose the package-less projection
root, public faces, and selected shared service-source packet atomically. It
refuses partial or source-only roots and never emits `server.ts`,
`package.json`, the community Effect bridge, or an oRPC 1 compatibility layer.

## Web Projection Authority

**State:** `UNCONSTRUCTIBLE` until a shared web-projection packet and the
qualified Studio additions are accepted.

The generic web projection owns accessible view projection and user
interaction mapping. Its required closed `test/views/` layer proves declared
state and results in the accessible DOM; optional closed `test/interactions/`
proves user intent maps to declared API or navigation calls.

MapGen Studio additionally owns deterministic browser-worker recipe execution,
mock-adapter collaboration, trace delivery, and visualization events. Its
qualified packet must select both a nonempty closed
`test/interactions/*.test.tsx` layer for its authored user-to-API/navigation
behavior and a nonempty closed `test/execution/*.test.tsx` layer for worker
startup, message ordering, cancellation/disposal, and emitted product evidence.
The app does not absorb this proof, and the web packet cannot become
constructible with only view suites while those capabilities remain in the
projection.

The web instance manifest selects exact `views`, `interactions`, and
`execution` proof-component identities. The qualified Studio packet records
the finite identities classified in `CORPUS.md`; a bounded source relation
requires exactly one matching suite per selected identity and forbids every
unselected suite. The layer wildcards describe only the legal suffix and do
not acquire tests by directory scan. Views, interactions, and execution remain
disjoint even when they observe the same user capability.

## App Realization Authority

**State:** Canonical shared runtime model; generic Habitat packet not yet
affirmed, so new app instances remain `UNCONSTRUCTIBLE`.

RAWR HQ Template's canonical runtime realization spec owns the app semantics:

```text
habitat.toml                 # blueprint-defined instance anchor
rawr.<app>.ts              # app identity and plugin membership
runtime/profiles/*         # provider selection, configuration roots, and defaults
runtime/config.ts          # app-owned config selection/default facts
runtime/processes.ts       # app-owned process defaults
[runtime/adapters/*]       # exact qualified cold effect bindings
<role>.ts / dev.ts         # one startApp(...) entrypoint selection
```

Apps and profiles select. They do not acquire resources, construct service
clients, build API context, mount plugins, or dispose runtime state. The shared
runtime compiler, provisioning kernel, process runtime, adapters, and harnesses
derive membership, acquire selected providers, bind service dependencies,
materialize contexts, mount roles, observe, and release.

An app may select one semantic adapter identity for each requirement declared
by its selected services and projections. The finite requirement set may
contain more than one adapter; each requirement still resolves to exactly one
compatible identity. The realizing app owns its opaque execution-descriptor
references. The runtime compiler rejects missing, ambiguous, or incompatible
selections and lowers each exact selection into the corresponding ready port.
The consuming app does not import service-private port types or target
implementation; the service does not import either app or realization project.
Semantic adapter selection does not make its targets a managed resource.
When a qualified app realizes a cold external effect, its manifest may select
an exact lower-kebab adapter identity at
`runtime/adapters/<adapter>.ts`. The app definition selects that identity;
profiles do not. The adapter may use its qualified host APIs and must implement
the public capability descriptor without exposing paths, prior bytes, handles,
or host-specific failures to the caller. `runtime/adapters/` is closed and
source-derived, never a generic integration cabinet.

For the CLI role, the shared harness surrounds native oclif `run(...)` with one
managed process scope and exposes bound clients through a scoped runtime command
context. A shared runtime-aware command base reads that context; topic-local
bases carry static cold requirement descriptors and narrow access to their
declared clients. Native oclif selection of the command class is the discovery
event; base initialization asks the already-surrounding harness to provision
and bind only those requirements. `apps/cli/package.json#oclif.plugins` remains
the sole authored topic registry. The app definition, runtime profile,
entrypoints, and topic packages must not recreate that list.
Construction-bound clients are process-cached; parsed flags and other
invocation facts remain outside binding identity. Oclif completion or failure
is reported only after the idempotent resource and service finalizer has run.

The full generic app Habitat packet is not affirmed at the Template audit
baseline. Civ7 therefore does not create a local `app`, `app-web-daemon`, or
equivalent super-kind. It first imports or accepts the shared generic app law
for the common definition/profile/entrypoint/proof grammar; a qualified product
packet may only narrow roles and membership facts after that parent kind is
constructible. Product specialization cannot compensate for a missing generic
app law:

| Root | Authority |
| --- | --- |
| `apps/cli` | commandless ownership is settled; terminal root composes the proposed shared `app` packet with the qualified `cli-shell` specialization and remains unconstructible until both are accepted |
| `apps/mapgen-studio` | semantic destination only; `UNCONSTRUCTIBLE` until the generic app law and its qualified role packet are accepted |
| `apps/mods/map/swooper-physics` | existing legacy product owner under a partial map envelope; not a manifest-admitted or constructible precedent |
| `apps/mods/civ/<mod>` | semantic destination only; `UNCONSTRUCTIBLE` until its qualified family packet is accepted |

The MapGen Studio browser is a web plugin projection, not a browser directory
that gives the app a second product truth. Its app definition selects that web
plugin and the Studio API plugin. Separate `server.ts` and `web.ts` entrypoints
may select production roles; `dev.ts` may cohost `server` and `web` without
creating an `app-web-daemon` kind.

Nx records project identity, edges, and tasks. `defineApp(...)`, runtime
profiles, and `startApp(...)` own membership, provider selection, and process
role selection. No Nx tag admits app kind or replaces those authorities.

No Studio browser extraction, app-definition rewrite, profile creation, or
entrypoint relocation begins until the generic app law, independently closed
web-projection law, and Studio role specialization define their manifest
anchors and are accepted. Generation remains refused until those packets and
the shared runtime realization substrate are accepted together.

The qualified Studio packet selects exactly these cold app adapters:

```text
runtime/adapters/
  civ7-official-data.ts
  civ7-save-files.ts
  fresh-log-files.ts
  studio-run-files.ts
  swooper-map-config-source.ts
```

The app definition owns those semantic adapter identities.
`runtime/profiles/local-civ7.ts` owns their concrete roots plus Tuner and
window-capture provider selection. `civ7-official-data.ts` satisfies the
Studio API's explicit `OfficialDataCatalog` requirement; it is separate from
saved-game configuration access. The other adapters implement bounded
filesystem effects for MapGen-runs or API context while importing pure
parser/planner packages and the Swooper definition's pure authoring surface.
No adapter identity or implementation is duplicated in a profile, service, or
API module.

Every qualified app packet owns a required closed proof interior:

```text
test/
  definition.test.ts
  profiles/
    *.test.ts
  entrypoints/
    *.test.ts
  execution/                # optional; only when app adapters are selected
    adapters/
      *.test.ts
  tsconfig.json
```

The definition suite proves exact plugin membership and every authored semantic
adapter identity. Profile suites prove provider, configuration-root, and
process-default selection only. Entrypoint suites prove one app, one profile,
one role, and delegation to the shared harness; they do not repeat startup,
mounting, disposal, or provider lifecycle. Profiles and entrypoints are
nonempty. Shared runtime compiler proof owns exact-one success and missing,
ambiguous, or incompatible semantic binding rejection; shared binding
execution proof proves the resolved adapter reaches the intended service port
once. Definition, profile, and entrypoint proof do not repeat adapter effects,
hosting, projection, provider behavior, or runtime lifecycle.

Proof membership is source-derived: every admitted
`runtime/profiles/<profile>.ts` requires exactly
`test/profiles/<profile>.test.ts`, and every authored `<role>.ts` or `dev.ts`
entrypoint requires exactly `test/entrypoints/<role>.test.ts`. Unmatched suites
are forbidden. `definition.test.ts` is the one fixed app-definition proof
leaf and `test/tsconfig.json` is the fixed compiler program. Every selected
`runtime/adapters/<adapter>.ts` requires exactly
`test/execution/adapters/<adapter>.test.ts`, which proves the adapter's host
effect, failure translation, and cleanup but not service policy. The app packet
never uses nonempty wildcards as membership authority.

## Mod Definition Project

**State:** `UNCONSTRUCTIBLE` for new families. The current
`map-mod-project` envelope preserves the existing Swooper product owner only as
legacy corpus; its open `scripts/`, `test/`, and source interiors and absent
instance anchor prevent it from being an admitted or constructible precedent.

**Candidate depth and root grammar, not admitted law**

```text
plugins/mod/<family>/<mod>
```

This initiative identifies `map` and `civ` as distinct family specializations
to qualify; it does not admit either through a generic packet. Family is a
specialization fact, not an instance-name inventory.

**Settled minimum, not constructible law**

```text
habitat.toml
package.json
project.json
src/
  mod-definition.ts
test/
  definition.test.ts
tsconfig.json
```

The definition face, project envelope, and qualified map proof topology are
settled. The complete finite `src/` interior still requires its separate
qualified family packet. An open recursive source tree or a list copied from
the current Swooper package cannot stand in for that law. `dist/` is recognized
output; `mod/`, deployment, runtime realization, and generated source are
structurally excluded from the definition owner.

The Swooper map definition selects one additional closed cold-authoring
interior:

```text
authoring/
  config.ts
  index.ts
  targets.ts
```

`authoring/config.ts` owns pure complete-config admission, canonical
serialization, and catalog membership projection. `authoring/targets.ts` owns
the two cold source-derived metadata targets consumed by the build graph:
recipe authoring metadata and map-catalog metadata. `authoring/index.ts` is the
sole deliberate `./authoring` face for those pure capabilities; it is not an
adapter registry. Filesystem selection, prior-byte capture, write, rollback,
and deletion belong to the Studio app's selected
`swooper-map-config-source` adapter. No generic `scripts/`, `tooling/`, or
recursive authoring cabinet is admitted.

**Public face and import direction**

- `src/mod-definition.ts` is the stable definition face.
- Additional public subpaths expose deliberate authored products, not generated
  realization output.
- Definitions consume public SDK, portable MapGen/core surfaces where
  qualified, static policy, and product-free packages.
- Definitions import no app, provider, live service, deployment capability,
  Node filesystem API, ambient host global, or generated `mod/` output.
- A qualified map definition may expose pure authored-config admission,
  serialization, and catalog projection through its closed `authoring/`
  interior. It never supplies the write/rollback adapter that consumes those
  functions.
- Portable `src/**` never imports `authoring/**`. The authoring table may import
  relative map catalog, configuration, and recipe-schema truth from `src/**`.
  Its sole deliberate package subpath is `./authoring`; the mod-definition face
  does not re-export it.

**Selected common and map-family proof law**

Every definition requires exact `test/definition.test.ts` for product identity,
matching public definition face, and finite authored capability membership.
The qualified map packet closes the test root to exactly:

```text
test/
  setup.ts
  definition.test.ts
  authoring/
    targets.test.ts
  domains/
  maps/
  recipes/
```

Membership below `domains/` and `recipes/` is projected from admitted
production-kind facts. It is never discovered by recursive path matching:

| Admitted production kind | Qualified proof destination | Distinct oracle |
| --- | --- | --- |
| Domain | `domains/<domain>/` | Optional direct `*.test.ts` for cross-module domain invariants only |
| Domain module | `domains/<domain>/<module>/` | Optional direct `*.test.ts` for cross-concern module invariants only |
| Domain artifact | `domains/<domain>/<module>/artifacts/<artifact>.test.ts` | Artifact-specific structural, cardinality, relational, or domain admission not already proved by the SDK |
| Domain model category | `domains/<domain>/<module>/model/<selected-category>/*.test.ts` | Behavior of the finite `atoms` or `policy` category selected by the domain-module law |
| Domain operation | `domains/<domain>/<module>/ops/<operation>/*.test.ts` | Shared operation-contract behavior across every strategy |
| Domain strategy | `domains/<domain>/<module>/ops/<operation>/strategies/<strategy>/*.test.ts` | Strategy-specific configuration and algorithm behavior |
| Recipe | `recipes/<recipe>/` | Optional exact `setup.ts` plus direct `*.test.ts` and `*.typecheck.ts` for whole-recipe configuration, composition, or orchestration |
| Recipe stage | `recipes/<recipe>/stages/<stage-path>/` | Optional direct `*.test.ts` for cross-step stage behavior |
| Recipe step | `recipes/<recipe>/stages/<stage-path>/steps/<step>/` | Direct `*.test.ts` for step-owned configuration, orchestration, projection, or materialization |
| Step artifact publication | `recipes/<recipe>/stages/<stage-path>/steps/<step>/artifacts/*.test.ts` | The declared publication behavior of that step |

`<domain>`, `<module>`, `<artifact>`, `<operation>`, `<strategy>`, `<recipe>`,
`<stage-path>`, and `<step>` are exact identities derived from their admitted
production manifests. `<stage-path>` is the exact relative path from the
recipe's selected `stages/` root to a directory admitted as a stage;
intermediate grouping directories admit no tests or loose files. The finite
domain-model categories are part of the domain-module blueprint rather than
free-form test names.

Every direct `*.test.ts` or `*.typecheck.ts` leaf in the table is selected by
an exact proof-component identity recorded in the nearest admitted domain,
module, operation, strategy, recipe, stage, or step manifest. Fixed artifact
publication leaves are derived directly from declared artifacts; all other
selected identities map one-to-one to their matching filename. A production
kind with no distinct behavioral oracle selects no direct proof leaf. This
keeps proof behavioral rather than file-count-driven without permitting
case-by-case directories or untracked suites.

`maps/` is not a free-form map-test cabinet. It is closed to exact proof
components selected by admitted production facts:

```text
maps/
  catalog/
    membership.test.ts
    [projection.test.ts]
  configs/
    admission.test.ts
    authoring.typecheck.ts
```

`catalog/` is selected only by an admitted `src/maps/catalog/index.ts`.
Membership proves ordered, unique, canonical shipped identities. Projection is
selected only when the definition declares a catalog projection target and
proves deterministic output from that same admitted order.

`configs/` is selected only by an admitted `src/maps/configs/index.ts`.
Admission proves exact envelope/schema admission, canonical serialization, and
immutable snapshots. Authoring typechecks the public map-construction surface.
The app adapter's execution proof owns ordered loading, prior-byte restoration,
new-source deletion, and path/prior-byte privacy. Whole-recipe compilation
belongs to the selected recipe proof, not `maps/`.

`authoring/targets.test.ts` is selected only when the definition admits
`authoring/targets.ts`. It proves the finite cold target table, deterministic
source-to-output projection, and generated-currentness contract for every
declared authoring target. It does not execute a CLI command, inspect generated
files as authored source, or repeat the catalog and recipe semantics proved by
their owners.

Swooper selects both map components and the catalog projection suite. Its current
`catalog-generation.test.ts`, `catalog-membership.test.ts`,
`create-map-config.type-test.ts`, and mixed `map-config-schema.test.ts` are
migration corpus for the exact destinations above; the recipe-compilation case
moves to Standard recipe proof. A Habitat source-relation rule binds component
selection to the exact production index and optional capability declaration.
`structure.toml` closes the finite names.

Recipe-qualified observational proof is part of the map-definition packet,
not an unowned optional interior. A recipe selects each capability explicitly
from its contract manifest. The selected capability then contributes one
closed, source-derived proof layer:

```text
recipes/<recipe>/
  [metrics/
    scenario.test.ts
    families/
      <metric-family>/
        <metric>.test.ts]
  [parity/
    <parity-surface>.test.ts
    <live-parity-surface>.live.test.ts]
  [trace/
    <trace-family>.test.ts]
  [viz/
    <visualization-path>.test.ts]
```

`<metric-family>/<metric>` is derived from the recipe's admitted metric bank,
not from directory discovery. `scenario.test.ts` proves that one admitted
scenario produces the complete bank consumed by targets and studies; family
suites prove only the named product metric or target relation. Metrics never
re-prove an individual operation or step.

`<parity-surface>` is derived from the recipe's admitted parity catalog.
Ordinary suites prove deterministic correlation, exact comparison, or report
projection from captured evidence. A surface requiring Civ7 must use the
`.live.test.ts` suffix and belongs to the realization-owned live Nx target even
though its semantic owner remains the recipe; local runs do not manufacture a
live pass.

`<trace-family>` is derived from the recipe's admitted trace expectation table
and proves causal emission identity, ordering, and payload projection. It does
not retest the generic trace recorder. `<visualization-path>` mirrors one
visualization entry selected by the recipe, stage, or step manifest and proves
only layer identity, legend/value projection, and deterministic visual data.
It does not test browser rendering.

The production manifest is the sole membership authority for all four layers.
A selected family or surface requires exactly one matching proof leaf; an
unselected leaf is forbidden. `structure.toml` owns the finite grammar and
source-relation rules own the manifest-to-proof correspondence. No generic
`metrics`, `parity`, `trace`, or `viz` wildcard is inferred from current files.

Every selected proof directory is closed and nonempty. A member with no
meaningful oracle has no proof directory; structure, wiring, and generic SDK
guarantees remain with Habitat, TypeScript, or the SDK's own tests. A fixture
used by one suite stays beside that suite. A bootstrap genuinely shared by
descendants is lifted to the nearest common admitted owner's exact `setup.ts`;
free-form `fixtures/` cabinets are forbidden. Root `test/setup.ts` owns only
suite-wide switchable Civ7 preset, map-seed, game-seed, and generic test-kit
composition. `recipes/<recipe>/setup.ts` may own one recipe-specific bootstrap
for its stages, steps, metrics, parity, trace, and visualization proof. Tests
whose subject is a shared default opt out explicitly rather than mutating it.

Generic `generated`, `scripts`, `support`, `runtime`, `operations`, and
`recipe` test cabinets are excluded. Generated output and command proof belong
to the realization or another independently selected kind. Live parity is
selected by the recipe but executes through the realization-owned live target;
live game loading remains realization proof. Structural SDK guarantees are not
repeated as product behavior. Civilization definitions require a separately
accepted finite content and proof grammar.

**Nx mapping**

- Observed graph metadata may describe `type:plugin`, a mod-definition
  role/surface, and one family identity. These facts do not admit the Habitat
  packet.
- Current `kind:mod` is replaced only when T1 and the definition/realization
  dependency directions are accepted.

**Generator**

Product authoring is explicitly refused today. The candidate shape is not
Engineer input. After a qualified family law is accepted, generation requires
that family and emits no authored product content beyond an empty typed
definition shell.

## Mod Realization Project

**State:** `UNCONSTRUCTIBLE` for new families. The current Swooper realization
is a legacy product owner under the partial map envelope and independently
enforced nested product laws; it is not a manifest-admitted realization kind.

**Candidate depth and root grammar, not admitted law**

```text
apps/mods/<family>/<mod>
```

A realization must identify one matching definition in `rawr.<mod>.ts`; Nx
corroborates the project edge and schedules its targets. Directory-name
equality is not the authority relation.

**Selected qualified realization spine**

The realization is an app that consumes exactly one matching definition and
has no reusable production export. It owns rendering, bundling,
generated-currentness, deployment, compatibility, and live proof. Recognized
outputs include `dist/`, `mod/`, and any explicitly protected generated-source
slot selected by the qualified family law; outputs are regenerated, never moved
as authored source or edited by hand.

The qualified packet composes the generic app law with additional finite
delivery law. It selects a cold typed app descriptor rather than a reusable
production module:

```text
habitat.toml
package.json
project.json
rawr.<mod>.ts
build.ts
deploy.ts
runtime/
  adapters/
    local-mod-install.ts
  config.ts
  processes.ts
  profiles/
    local-civ7.ts
  map-script/
    adapter.ts
    entrypoint.ts
    setup.ts
  file-plan.ts
  run-manifest.ts
  targets.ts
test/
  definition.test.ts
  profiles/
    local-civ7.test.ts
  entrypoints/
    build.test.ts
    deploy.test.ts
  execution/
    adapters/
      local-mod-install.test.ts
  artifact/
    *.test.ts
  deployment/
    *.test.ts
  [runtime/]
    *.test.ts
  live/
    *.live.test.ts
  tsconfig.json
tsconfig.json
```

`rawr.<mod>.ts` declares the exact matching definition identity and the
realization's cold semantic target bindings. The `local-civ7` profile selects
the local Civ7 installation/deployment configuration; `build.ts` and
`deploy.ts` are the selected app roles and delegate through the shared runtime
rather than constructing their own harness. `runtime/targets.ts` implements
the cold bindings with typed non-portable execution descriptor references;
`runtime/file-plan.ts` owns the deterministic rendered mod-tree plan and
`runtime/run-manifest.ts` owns transient run-manifest materialization. These
runtime leaves are compiler input, not reusable public exports.
`runtime/adapters/local-mod-install.ts` performs the host filesystem
installation selected by the app definition. `runtime/map-script/` contains
the only engine-global implementation: the concrete adapter, setup capture,
and generated Civ7 map entrypoint. It consumes the pure
`packages/civ7-adapter` port/static/mock package plus static Civ7 policy; the
package and portable definition import none of these engine-global leaves. The
map-family packet
may select finite generated-source slots, but generic `scripts/`, `src/`, and
open test interiors remain excluded.

**Qualified selection**

A future qualified realization packet must compose the accepted generic app
packet at this root, then add only the family relation, cold target interior,
and delivery proof layers. It cannot bypass or weaken generic
definition/profile/entrypoint law. Mod realization is not a subtype of mod
definition; it selects one definition as an app dependency.

**Public face and import direction**

- The realization app has no reusable production export.
- The app descriptor and target table are cold runtime-compiler inputs, not a
  callable library surface.
- It consumes exactly one matching mod definition plus public SDK/runtime,
  pure adapter contracts, packages, and qualified service/resource surfaces.
- It owns render, bundle, generated-currentness, deployment, compatibility,
  and live proof.
- Engine globals and Node filesystem effects are admitted only inside the
  exact qualified runtime leaves selected above.
- It never duplicates authored product definition or imports another app.

**Selected proof law**

The generic app layers remain intact. `definition.test.ts` proves exact
matching-definition identity, complete semantic target operation keys, and
cold descriptor compilation. `profiles/local-civ7.test.ts` proves the selected
installation/deployment configuration, while `entrypoints/{build,deploy}.test.ts`
prove role selection and shared-runtime delegation without repeating target
behavior. `test/execution/adapters/local-mod-install.test.ts` proves exact-tree
installation, failure translation, and cleanup against an isolated root.
`test/artifact/` proves deterministic rendered identity, content, and output
placement. `test/deployment/` proves correct operation distinction, target/root
selection, installation-receipt interpretation, cancellation, and release for
every selected deployment path; it does not repeat filesystem mechanics owned
by the app adapter. Optional `test/runtime/` proves only generated-module
collaboration, map-script adapter behavior, and engine compatibility under the
qualified runtime harness.
`test/live/` owns loader acceptance and runtime compatibility and runs only
through the explicit uncached live target.

`runtime/targets.ts` declares the exact artifact, deployment, optional runtime,
and live proof-component identities selected by the realization manifest. Each
identity requires one matching proof leaf in its layer; unselected leaves are
forbidden. The Swooper realization selects the finite identities classified in
`CORPUS.md`. Existing deterministic scripts cannot be relabeled as live proof:
its live leaves are newly authored against real loader/runtime observations.

All selected layers are closed and nonempty. Generation/currentness,
deployment verification, and live Civ7 acceptance remain distinct Nx targets.
Currentness reuses artifact identity rather than creating another suite.
Instance-specific product assertions do not enter generic realization law.

**Nx mapping**

- Observed graph metadata may describe `type:app`, `runtime:mod`, and the
  matching family identity. These facts do not admit the Habitat packet.
- Nx owns the exact definition-to-realization edge and build/deploy ordering.

**Generator**

Unsupported today. The candidate shape is not Engineer input. After a qualified
family law is accepted, generation requires that family and a matching admitted
definition. It emits authored scaffold files only; `dist/` and `mod/` remain
absent until their Nx generators run.

## Design Disagreement Inventory

These umbrella rows identify where exact cut-specific corpora will be derived.
They are design inventory, not one enforcing scan or Engineer handoff. Each
participating row must be narrowed to exact roots and one terminal disposition
before its law is promoted. Live red is migration evidence, not permission to
broaden a kind:

| Corpus root | Expected disagreement | Allowed disposition |
| --- | --- | --- |
| `.habitat/blueprints/service/**` | Civ7's oRPC 1/community-bridge topology and source relations differ from the selected shared substrate | Replace with the portable shared packets, adapt only repository path selectors, prove injected fixtures, promote in the burn-down branch, and eliminate the resulting live red before landing |
| `package.json`, `bun.lock`, `patches/**`, `services/**` | Old oRPC, Effect, TypeBox, and community-bridge dependency family remains reachable in Civ7 product code | Upgrade atomically after the independently sealed Habitat successor is pinned, migrate native construction/error relations, and delete the obsolete patch and bridge only after no product consumer remains |
| `tools/habitat/**` | The shared tool must migrate with the substrate it enforces | Consume the independently reviewed shared-Habitat result; do not treat tool source as Civ7 capability migration corpus |
| `packages/**` | Resource contract, provider, and service concerns are combined or proposed service-adapter/parallel-contract packages lack a native owner | Relocate to the qualified owner, retain only proven shared non-oRPC models, inline owner-local residue, combine duplicate public faces, or delete dead residue |
| `resources/**` | Selected resource/provider roots and their closed laws do not yet exist | Construct only after T1/T2; never move implementation into the resource contract |
| `services/**` | Existing service source and proof follow the legacy spine; the additional selected service root is absent | Migrate behavior into the shared spine or construct the missing service; do not baseline legacy files |
| `plugins/cli/topics/**` | Current topic packages are already governed by the accepted Civ7 `cli-topic-plugin` ownership law, but its recursive `test/support/**` allowance remains an open proof-cabinet defect | Preserve the topic boundary, correct proof to mirrored command/adapter layers with colocated fixtures, burn instance red down, and do not import Template's command-root defect |
| `plugins/server/api/**` | The selected package-less API composition is absent | Construct the complete API root plus its shared service-source projection depth atomically; do not create an independent domain service |
| `plugins/web/app/**` | The qualified Studio browser projection law is absent | Design and accept the closed qualified packet before creating the root or moving browser source |
| `apps/cli`, `apps/mapgen-studio` | Current app code manually owns provider, service, API-context, mounting, and lifecycle realization | Preserve the CLI law; keep Studio in place until its qualified packet is accepted, then move provisioning, binding, mounting, observation, and disposal into shared runtime |
| `plugins/mod/{map,civ}/**` | Swooper remains a legacy definition owner under a partial envelope; qualified map and civilization definition laws are absent | Preserve Swooper behavior and ownership in place until the qualified map definition packet closes its domain-shaped proof and manifest anchor; do not create or move Dacia until the civilization packet is accepted |
| `apps/mods/{map,civ}/**` | Swooper remains a legacy realization owner under partial law; qualified map and civilization realization laws are absent | Preserve Swooper behavior in place until its qualified realization proof and anchor close; do not construct Dacia realization until its packet is accepted; generated output is never moved as source |

Only qualified relocation, inlining, combination, or deletion is allowed.
Grandfather baselines, instance-path exceptions, broad source scanners, and
negative niche cabinets are rejected.

## Preserved Behavior And Exclusions

This slice changes structural ownership and service substrate, not product
behavior. Preserve:

- current public control lifecycle, readiness, cancellation, and failure
  observability;
- existing CLI command behavior and command-scoped acquisition;
- current Studio browser/daemon workflows and same-origin public behavior;
- map generation semantics, deterministic outputs, mod identity, build
  products, deployment behavior, and live-game proof;
- all currently accepted service operations and public error outcomes, even
  when their implementation moves to native oRPC 2 and Effect 4.

Outside this slice:

- new product operations, UI redesign, or changed control semantics;
- a public protocol package before a second real consumer proves it;
- a service-adapter package or parallel package-owned oRPC contract;
- content, documentation, example, or browser-only app kinds;
- a generic database or entity source law beyond composition of an already
  accepted shared kind;
- vendor-specific deployment design and instance inventories.

## Habitat Engineer Handoff Contract

**Rule owners**

- Habitat `structure.toml`: every required spine, finite optional interior,
  protected generated slot, and absent legacy interior.
- Habitat Grit: bounded public/private imports, provider/resource direction,
  native service construction and error lineage, projection/app direction, and
  definition/realization direction.
- TypeScript plus `package.json`: public faces, exact assignability, native
  router completeness, context/failure types, and package or package-less
  resolution.
- Instance manifests: blueprint identity/version, governed roots, selected
  capabilities, and accepted niche facts.
- Nx: primary and secondary facts, project dependency matrix, target ordering,
  generation/currentness, deployment, and uncached live proof.
- Tests: the kind-selected contract, semantics, execution, projection, assembly,
  or delivery layers and their observable oracles.

This matrix is not one broad Engineer corpus. Each selected law or product cut
receives a separate handoff containing:

- exact rule files or accepted upstream packet commit;
- exact blueprint-defined manifest anchor;
- exact source roots and exact destination roots;
- one terminal disposition for every source row: `relocate`, `inline`,
  `combine`, or `delete`;
- preserved behavior and focused proof owner;
- explicit adjacent roots excluded from acquisition.

Umbrella roots such as `packages/**`, `resources/**`, `services/**`,
`plugins/**`, or `apps/**` are forbidden handoff inputs. Alternative
destinations and “after semantic review” rows remain design corpus and cannot
reach an Engineer. A shared blueprint absent from Civ7 is corrected upstream,
accepted at a successor Template commit, then imported rather than re-authored
from current residue. Package, Studio web/app, civilization mod
definition/realization, and their proposed Habitat roots remain design inputs
until their own accepted closed packets and manifest anchors exist.

**Proof classes**

1. Positive and negative injected structure fixtures for every closed scope.
2. Grit match/no-match fixtures for each bounded source relationship.
3. TypeScript compile-only fixtures for public faces, router completeness,
   context, and exact failure channels.
4. Nx graph fixtures for every allowed and rejected primary/secondary edge.
5. Generator dry runs proving exact output and empty-write refusal.
6. Promotion/enforcement in the exact burn-down branch, followed by live-red
   observation and qualified disposition to zero.
7. The exact closed kind-owned proof layers selected in this matrix, with each
   suite mapped to one distinct oracle and no generic helper cabinet.
8. Final workspace formatting, type, boundary, reachability, build, and test
   graph, plus explicit live proof only where the realization law requires it.

Stop the burn-down and return `LAW_CORRECTION` if execution needs an instance
name in generic law, leaves a selected child open, reveals a missing
constructible kind, assigns an invariant to the wrong native owner, or produces
credible incompatibility evidence against the accepted successor shared service
substrate.

## Admission And Seal Order

Each reusable law packet proves its positive shape before product relocation,
then is promoted in the exact branch that burns its admitted corpus down:

```text
authority
  -> positive generic law
  -> closed topology
  -> bounded source relationships
  -> injected fixture proof
  -> promote and enforce in the burn-down branch
  -> exact live red corpus
  -> qualified relocation, inlining, combination, or deletion
  -> zero
  -> behavior proof
  -> seal and land
```

One product cut may keep several laws and instance migrations coupled where
lifecycle and consumer closure require it:

```text
fixture-proved destination laws
  -> Nx mapping and manifest-backed instance generation
  -> generator contract and refusal proof
  -> promote exact laws
  -> live red
  -> qualified dispositions
  -> zero and runtime proof
  -> seal and land
```

Provider waits for resource. API source waits for both the API-root and shared
service-source packets, while consuming domain capabilities only through
public clients. Qualified apps wait for shared runtime realization.
Mod realization waits for mod definition. No later row weakens or bypasses an
earlier row's public/private boundary.

## Stop Conditions

Stop and return to design if:

- T1 cannot describe a project with one structural type plus orthogonal graph
  facts without assigning kind admission, plugin membership, provider
  selection, or process-role selection to Nx;
- a kind requires an instance name or current file inventory in generic law;
- a new instance has no accepted blueprint anchor or generated
  `habitat.toml` facts;
- a required child remains structurally open after all selected
  specializations apply;
- a package role becomes a substitute for a missing resource, service,
  projection, or app kind;
- a standalone service or API-selected service source diverges from the selected shared oRPC 2 and
  Effect 4 substrate without the required incompatibility proof and explicit
  product decision;
- a definition law admits generated realization output;
- an app or plugin starts exporting reusable domain truth;
- a provider, service, or plugin selects its own runtime lifetime;
- a generator needs to write source before the accepted law and Nx mapping can
  validate the resulting tree.
