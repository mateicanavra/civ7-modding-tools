# Domino D1 Code/Topology Investigation

## Scope

Lane: D1 Receipt Contract Boundary investigation, code/topology only.

This file inventories current Habitat receipt/proof/check/diagnostic/apply/hook/verify surfaces as present-behavior evidence. Current names are not target authority. D1 must still consume D0 matrix rows before preserving, renaming, versioning, facading, or removing public surfaces.

No packet or source files were edited for this investigation.

## Current Surface Inventory

| Current name | Path | Kind | Public/internal guess pending D0 row | Target disposition candidate | Invalid states / smells | Tests | Write-set / protected-path recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `CheckReport` | `tools/habitat/src/lib/diagnostics.ts:36`; built in `tools/habitat/src/lib/command-engine.ts:273`; emitted by `tools/habitat/src/commands/check.ts:46` | Command JSON check result DTO | Public command JSON and package export. `CheckReport` is exported from `src/index.ts:45`; `check --json` advertises it in command help (`commands/check.ts:20`). Needs D0 rows for `command-json`, `package-export`, and docs examples. | Keep as `check` diagnostic/check result contract, not a handoff receipt. If renamed, require versioned compatibility wrapper. `ok` should be constructor-derived from rule statuses. | `ok: boolean` is correlated with `rules[].status` by convention, while `validateCheckReport` only checks shape (`diagnostics.ts:45`) and cannot reject `ok: true` with a failing rule. `rules` has no closed result family for selection failure vs executed check vs schema invalid report. | `test/commands/habitat-entrypoints.test.ts` validates JSON behavior; `hooks.ts` parses `CheckReport`; hook tests use check JSON. No current semantic test rejects contradictory `ok`/rule status. | D1 write set likely includes `tools/habitat/src/lib/diagnostics.ts`, `src/lib/command-engine.ts`, `src/commands/check.ts`, `test/commands/habitat-entrypoints.test.ts`, and hook parser tests if JSON semantics change. Protected until D0 rows exist: package export shape in `src/index.ts`, command JSON schema version, docs examples. |
| `HabitatDiagnostic` / `RuleReport` | `tools/habitat/src/lib/diagnostics.ts:8`, `:21` | Normalized diagnostic and rule report shapes | Public package export and command JSON subshape (`src/index.ts:45-51`). | Keep as diagnostic/check vocabulary. D1 should explicitly say diagnostics are observations, not receipts or proofs. | `RuleStatus` is a useful closed union, but `RuleReport` allows any combination of `lane`, `status`, and diagnostics. Advisory lane can carry diagnostics without failure; enforced fail depends on unbaselined error diagnostics. | Covered indirectly through check and hook tests. | Same write set as `CheckReport` if enforcing constructor validation. Protected path: broad export in `src/index.ts` until D0 classifies. |
| `validateCheckReport` | `tools/habitat/src/lib/diagnostics.ts:45`; used by `stringifyCheckReport` and hook parser | Structural validator | Package export (`src/index.ts:51`) and internal parser helper. | Convert or supplement with semantic validator/constructor for D1 contract. Keep structural validation if needed for compatibility. | Shape-only validation lets impossible check states through. It also casts `unknown` directly to `CheckReport` before validating, so the core type is trusted before full parse. | `stringifyCheckReport` throws on schema errors (`command-engine.ts:468`); hook parse uses it (`hooks.ts:802`). Need malformed-payload D1 test. | Likely D1 source write. Protected: external behavior of `check --json` until D0 disposition. |
| `VerifyProof` | `tools/habitat/src/lib/command-engine.ts:101`; created by `createVerifyProof` at `:646`; emitted by `commands/verify.ts:41` | Verify command JSON handoff/proof DTO | Public command JSON. Not currently exported from `src/index.ts`, but `verify --json` advertises `VerifyProof` in command help (`commands/verify.ts:24`). Needs D0 command-json and human-output rows. | Candidate target name: `VerifyHandoffReceipt` or `VerifyCommandReceipt`, with `VerifyProof` preserved only as schemaVersion 1 compatibility if D0 requires. Separate command receipt, check summary, Nx affected outcome, post-state, and non-claims. | Mixes command metadata, check summary, Nx affected streams/cache, post-state, and non-claims in one object. Good existing DU for `nxAffected.executed/skipped`, but no explicit top-level proof/receipt class, no scope field, and non-claims use human phrases such as "CI execution proof". `summarizeVerifyCheckReport` omits requested selectors (`command-engine.ts:732`) despite DTO field. | `test/lib/verify-proof.test.ts:24` pins bounded streams, cache-state-by-task, and skipped Nx truth. `test/commands/habitat-commands.test.ts` mocks `createVerifyProof` and checks `nonClaims`. | D1 likely writes `command-engine.ts`, `commands/verify.ts`, `test/lib/verify-proof.test.ts`, `test/commands/habitat-commands.test.ts`, docs. Protected: `verify --json` schemaVersion 1 and field names until D0 compatibility row decides preserve/version/rename. |
| `createVerifyProof` | `tools/habitat/src/lib/command-engine.ts:646` | Constructor/projection function | Internal helper for command JSON, possibly package-internal if later exported. | Rename or wrap as receipt constructor; make contradictory states unrepresentable at input boundary. | Accepts optional `affectedResult`; skipped/executed state derives from presence, not from an explicit check outcome. This is safe in current caller but admits misuse by direct callers. | `test/lib/verify-proof.test.ts` calls it directly and constructs inconsistent `CheckReport` freely. | D1 write candidate. Add tests for impossible input combinations or replace input with a DU: `checkFailed -> skippedNx`, `checkPassed -> executedNx/result`. |
| `HabitatCommandResult` | `tools/habitat/src/lib/habitat-process.ts:45`; exported from `src/index.ts:84` | Bounded command execution result substrate | Package export and internal process boundary. | Keep as command-result substrate, not proof. D1 should define how receipt DTOs project from it and what non-claims mean. | Current `kind` includes `"git-proof"` (`habitat-process.ts:11`) and `nonClaims` is free-form strings. `failureTag` is Grit-specific but lives on generic command result. | Used heavily by Grit/apply/proof artifact tests. | D1 may need a narrow write only if changing non-claim vocabulary or kind names. Protected path: `src/lib/habitat-process.ts` is broad substrate used by other dominoes; avoid D1 broad refactor unless D0 marks public/internal and D15 trigger is considered. |
| `AdapterProofArtifact`, `AdapterProofClass`, `ProofArtifactWriter` | `tools/habitat/src/lib/proof-artifact.ts:7`, `:19`, `:55`; exported from `src/index.ts:98-110` | Adapter command-result artifact writer | Public package export by current root index; writes old OpenSpec path. D0 must classify package-export and generated/workstream artifact surfaces. | Candidate target: adapter command result record / adapter evidence record. Preserve `Proof*` names only as compatibility wrappers if D0 requires. Hard-coded output path likely belongs to a historical adapter packet, not D1 target. | Broad `AdapterProofClass` mixes native sample/current-tree/raw acquisition/injected/dry-run/applied-diff/rollback/biome/nx/smoke. Generic artifact machinery risks becoming a cross-domain proof supertype. `normalizedSummary: Record<string, unknown>` is a property bag. Hard-coded `openspec/changes/habitat-effect-grit-adapter/workstream/proofs` path is stale/packet-specific (`proof-artifact.ts:127`). | `test/lib/proof-artifact.test.ts` pins path, safe ID, redaction, non-claim merge, write behavior. | D1 likely writes this only if compatibility row says rename/facade/version. Protected until D0: `src/index.ts` public exports and packet-local artifact path. If touched, tests need compatibility and migration assertions. |
| `adapterProofArtifactPath` | `tools/habitat/src/lib/proof-artifact.ts:127` | Artifact path constructor | Public package export. | D1 should recommend preserve/facade or deprecate. Path is not a D1 receipt contract unless retained as adapter compatibility. | Hard-coded to old `habitat-effect-grit-adapter` change path; D1 should not propagate this as current receipt authority. | `test/lib/proof-artifact.test.ts:14` pins exact path. | Protected unless D0 row authorizes change. If changed, update tests and add migration/deprecation note. |
| `HookTrace`, `PreCommitTrace`, `PrePushTrace`, `HookCommandRecord` | `tools/habitat/src/lib/hooks.ts:72`, `:91`, `:108`, `:119` | Hook local feedback trace | Internal today unless exported later; `createHookTrace` is imported in tests but not exported from root index. Hook human output is public. | Target as `HookLocalFeedbackTrace`, not proof. D1 should state it is local feedback telemetry with pre/post state and command provenance, not verification. | `HookTrace` top-level has optional `preCommit` and `prePush`, so an empty trace or both traces can exist; this may be fine for test/runtime injection but should not be mistaken for a command handoff receipt. Outcomes are closed unions and fairly strong. | `test/lib/hooks.test.ts:334` pins typed pre-commit trace; `:403` pins parse-failure outcome; `:537` pins pre-push provenance. | D1 likely writes `hooks.ts` and `hooks.test.ts` only for naming or non-claim output. Protected: hook command behavior and Husky semantics until D0 hook rows. |
| `localHookProofNotice` | `tools/habitat/src/lib/hooks.ts:154`; emitted at `:214` and pre-push path; tested at `test/lib/hooks.test.ts:24`, `:454`, `:471`, `:606` | User-facing human output line | Public human-output surface. Needs D0 `human-output` or `hook` row. | Rename/copy to "hook feedback: local only; CI remains authoritative" or equivalent. If public compatibility requires old wording, preserve through versioned/deprecated path. | Contains target-suspect term "proof" while correctly non-claiming CI authority. Because tests assert the string, this is a compatibility surface, not just prose. | Hook tests directly assert exact phrase in stdout and reporter events. | D1 likely write: `hooks.ts`, `test/lib/hooks.test.ts`, docs. Protected until D0: exact output text if public-stable. |
| `GritApplyTransactionProof` / `GritApplyTransactionResult` | `tools/habitat/src/lib/grit-apply.ts:71`, `:96`; exported from `src/index.ts:56-66` | Apply transaction record/result | Public package export and `fix` command result substrate. D0 rows needed for package-export, command behavior, and apply transaction. | Candidate target: `GritApplyTransactionRecord` or `ApplyTransactionReceipt`; keep separate from check/verify receipt. It should distinguish dry-run, live apply, rollback, formatter handoff, gate failure, and refusal as explicit variants. | `GritApplyTransactionResult` extends `SpawnResult` and adds `ok`, `failureTag`, `proof`. The `proof` fields are many nullable commands (`dryRunCommand`, `applyCommand`, `biomeCommand`, `rollbackCommand`, `transactionCopyCommand`) whose legal combinations are phase-dependent. `ok` is correlated with `failureTag` and embedded command states by convention. | `test/lib/grit-apply.test.ts` is strong: dirty live refusal (`:40`), dry-run allowed dirty (`:56`), mismatch fail closed (`:93`, `:105`), rollback/gate/biome failures (`:251`, `:356`, `:373`), isolated copy proof (`:401`, `:421`, `:445`). | D1 can recommend shape changes, but implementation likely belongs with D9 apply transaction. D1 write set should include type/contract definitions only if D9 consumes them. Protected paths: live apply behavior, Grit apply patterns, file writes, rollback behavior. |
| `proof()` helper in apply transaction | `tools/habitat/src/lib/grit-apply.ts:961` | Internal projection builder | Internal helper, but produces public/exported proof shape. | Rename after target contract chosen; keep projection-specific, no generic `Proof<T>`. | Uses defaults to fill missing phase records with null/empty arrays, allowing "successful-looking empty proof" if helper miscalled. | Covered indirectly by apply tests. | D1 should recommend a constructor DU or state-specific builders rather than broad helper. Implementation likely D9. |
| `runFix` / `fix` command | `tools/habitat/src/lib/command-engine.ts:606`; `tools/habitat/src/commands/fix.ts:18` | Command surface delegating apply transaction | Public CLI command; `runFix` is package export (`src/index.ts:39`). | D1 should classify output as apply transaction output, not proof. D9 should own detailed behavior. | Command writes stdout/stderr from `GritApplyTransactionResult` but no JSON contract flag. Exit code follows result. | Apply tests; command tests likely cover fix entrypoint. | D1 protected unless changing labels/docs. D9 likely owns implementation. |
| `ClassifiedTarget.proof` | `tools/habitat/src/lib/command-engine.ts:196`; classification command emits JSON at `commands/classify.ts:19` | Classify metadata subshape | Public command JSON and package type export (`src/index.ts:20-28`). Target owner likely D4/D3, not D1. | D1 should mark as adjacent proof-shaped current name outside D1 write set; D4/D3 should disposition. | Uses field `proof` for target source (`nx-project-graph` vs `habitat-owned`) but this is evidence/provenance, not receipt. | Classification tests under `test/lib/classify.test.ts` and D0 review mention classify. | Protected for D1. Add downstream note only; do not edit in D1 unless D0/D4 authorizes. |
| `Proof Coverage` and proof prose | `tools/habitat/docs/IMPLEMENTED-SURFACE.md:111`, `:143`, `:146`, `:162`, `:164`; `tools/habitat/docs/SCENARIOS.md:61`, `:76`, `:90`, `:170`, `:187`, `:190`, `:258`, `:267` | Durable docs/current-state examples | Public docs examples and current-state reference. D0 requires docs-example rows for examples. | D1 should classify each occurrence: compatibility/historical current-state prose vs target product language. Replace target guidance with receipts/checks/diagnostics/transactions/local feedback where needed. | Docs say "proof artifacts", "structured proof artifact", "diagnostic proof slices", "transaction proof", and "Graphite-aware pre-push affected proof"; some are current-state, some are public guidance. Ambiguous authority risk. | No direct docs tests found for these phrases. | D1 likely write set: `tools/habitat/docs/IMPLEMENTED-SURFACE.md`, `tools/habitat/docs/SCENARIOS.md` only after D0 docs-example rows. Protected: packet files and unrelated docs. |
| `PatternAuthorityManifest.proof` fields | `tools/habitat/src/rules/pattern-authority/manifest.ts` (found by search: `noWriteProof`, `appliedDiffProof`, `rollbackProof`, `typeAndTestProof`) | Pattern governance manifest fields | Public-ish generator/rule manifest surface, likely D8/D13/D9 owner. | D1 should not own unless D1 defines shared non-claim vocabulary consumed by pattern governance. | Proof terminology is embedded in manifest schema and validation messages; likely outside D1 immediate receipt/check/hook/verify/apply current lane. | `test/rules/pattern-authority-manifest.test.ts`, generator tests. | Protected for D1. Add downstream dependency note for D8/D9/D13 if D1 vocabulary must be consumed. |

## P1 Blockers

| Blocker | Why it blocks D1 | Concrete repair |
| --- | --- | --- |
| D1 cannot choose compatibility actions until D0 implementation matrix rows exist or are explicitly mocked as provisional rows. | Accepted D0 says later packets must cite `surface_id` before changing command JSON, package exports, hook output, docs examples, or public names. D1 currently needs rows for `CheckReport`, `VerifyProof`, `AdapterProofArtifact`, `ProofArtifactWriter`, `GritApplyTransactionProof`, `GritApplyTransactionResult`, `HookTrace`/hook output, `HabitatCommandResult`, and docs examples. | Add a D1 design-time table requiring D0 rows by plane: `command-json`, `package-export`, `human-output`, `hook`, `docs-example`. If D0 implementation is not complete, D1 must remain blocked for source edits and can only carry provisional recommendations. |
| Current D1 packet still lacks this row-by-row receipt surface inventory. | Without the inventory, implementation agents must decide whether current proof-shaped names are public compatibility wrappers, internal helpers, target language, or downstream-owned surfaces while coding. | Import this inventory into D1 design as `Receipt Surface Inventory`, with columns matching D0: current path, surface plane, D0 `surface_id`, compatibility handling, target owner, non-claims, tests, and downstream dominoes. |
| The broad "Command Receipt Contract" owner hides multiple authorities. | Check diagnostics, verify handoff, hook local feedback, apply transaction, adapter command records, Graphite/handoff docs, and pattern governance have different invalid states and owners. One broad owner recreates a shared proof super-domain. | Split D1 target into projection-specific contracts: `CheckResultContract`, `VerifyHandoffReceipt`, `HookLocalFeedbackTrace`, `ApplyTransactionRecord`, `AdapterCommandRecord`, and `HandoffDocNonClaims`. D1 owns vocabulary/compatibility boundary; D9 owns apply behavior, D11 hooks, D12 verify handoff, D4/D3 classify/graph metadata, D8/D13 pattern/generator manifest surfaces. |

## P2 Blockers

| Blocker | Why it matters | Concrete repair |
| --- | --- | --- |
| `CheckReport` has a real contradictory-state hole. | `ok` can disagree with `rules[].status` and structural validation will pass. This is the smallest D1 semantic repair because check results feed hooks and verify. | Add constructor/validator requirement: `ok === rules.every(status !== "fail")`; add malformed/contradictory payload test; preserve `schemaVersion: 1` unless D0 chooses versioning. |
| `VerifyProof` has useful internal state but target semantics are undernamed. | It already prevents executed Nx when check fails in current command flow, but the top-level DTO is still proof-shaped and combines receipt/check/Nx/post-state without `scope` or `proofClass`/receipt class. | Choose preserve/version/rename. If preserving, wrap target semantics in compatibility: `VerifyProof` schemaVersion 1 remains command JSON, but target docs call it verify handoff receipt. Add non-claim vocabulary and a test for failure projection that cannot be reported as passing proof. |
| Hook proof notice is public text pinned by tests. | The text is semantically careful but target-suspect. Changing it breaks tests and possibly users. | D0 row first. Then either preserve exact phrase as compatibility or rename to "hook feedback: local only; CI remains authoritative" with tests and docs updated. |
| Apply transaction shape is state-space heavy but likely D9-owned. | Nullable command slots represent phase-dependent state. D1 should define vocabulary, but D9 should own the behavior/transaction refactor. | D1 should recommend write set split: D1 may define shared labels/non-claims and compatibility disposition; D9 writes `grit-apply.ts` behavior and transaction DU if needed. |
| Docs contain mixed proof language. | Some is historical/current-state, some is user guidance. D1 must not overcorrect by erasing useful non-claims or undercorrect by leaving target proof language. | Add docs-example rows to D0; D1 classifies each doc occurrence as `current compatibility prose`, `target replacement`, or `downstream-owned`. |

## Recommended D1 Implementation Write Set

Only after D0 rows exist and D1 packet repairs are accepted:

- `tools/habitat/src/lib/diagnostics.ts` for `CheckReport` semantic validation/constructor and diagnostic non-claim language.
- `tools/habitat/src/lib/command-engine.ts` for `VerifyProof` compatibility wrapper or `VerifyHandoffReceipt` projection, `createVerifyProof` input-state tightening, and check report construction.
- `tools/habitat/src/commands/check.ts` and `tools/habitat/src/commands/verify.ts` only if help text or JSON output names change.
- `tools/habitat/src/lib/hooks.ts` only for hook local-feedback wording and trace naming if D0 authorizes.
- `tools/habitat/src/lib/proof-artifact.ts` only if D0 classifies exported proof artifact APIs as D1-owned compatibility surfaces; otherwise defer to adapter/process substrate owner.
- `tools/habitat/test/lib/verify-proof.test.ts`, `test/lib/proof-artifact.test.ts`, `test/lib/hooks.test.ts`, `test/commands/habitat-commands.test.ts`, and `test/commands/habitat-entrypoints.test.ts` for semantic/non-claim tests.
- `tools/habitat/docs/IMPLEMENTED-SURFACE.md` and `tools/habitat/docs/SCENARIOS.md` only for D0-cited docs-example realignment.

## Protected Paths For D1

- `openspec/changes/deep-habitat-d1-receipt-contract-boundary/**` and packet files during this investigation lane: do not edit from code/topology lane.
- `openspec/changes/deep-habitat-d0-command-surface-inventory/**`: accepted design/spec authority, not a D1 write target.
- `tools/habitat/src/index.ts`: protected until D0 package-export rows authorize export preserve/version/facade/deprecate actions.
- `tools/habitat/src/lib/grit-apply.ts`: protected for behavior unless D1 only applies compatibility naming and D9 agrees; transaction behavior belongs to D9.
- `tools/habitat/src/lib/habitat-process.ts`: protected unless D1 narrows a very specific non-claim/projection contract; broad command-result substrate changes risk D15/D9/D7 overlap.
- `tools/habitat/src/lib/grit.ts`, `src/lib/grit-injected-probe.ts`, and pattern-authority manifest files: adjacent proof terminology exists but belongs to D6/D7/D8/D13 unless D1 explicitly includes downstream vocabulary migration.
- Generated artifacts: `dist/**`, `oclif.manifest.json`, Nx cache/output, and generated project outputs.
- Root `package.json`, `nx.json`, `tools/habitat/package.json`: D0/D3/package-surface territory unless D1 has a specific D0-cited row.

## Validation Recommendations

- Add/keep `bun run --cwd tools/habitat test -- test/lib/proof-artifact.test.ts test/lib/verify-proof.test.ts test/lib/hooks.test.ts test/lib/grit-apply.test.ts` as the focused semantic suite, but do not treat it as enough for D1 closure.
- Add a malformed or contradictory `CheckReport` payload test: `ok: true` with a `fail` rule must be rejected or normalized.
- Add a verify command failure projection test: failing Habitat check must produce skipped Nx receipt/non-claim state and must not claim passing proof.
- Add a hook local-feedback test if wording changes; it must keep the CI non-claim.
- Add an apply transaction non-claim compatibility test only if D1 touches exported apply proof/transaction names; otherwise defer to D9.
- Run `bun run habitat verify --json` and `bun run habitat check --json` after implementation to capture command JSON behavior with expected status and non-claims.
- Run `git diff --check` and `git status --short --branch` to prove D1 did not write generated artifacts or packet-protected paths.

## Non-Claims

- This investigation did not run tests or validation commands.
- This investigation did not accept D1.
- This investigation did not accept or implement the future D0 matrix.
- Current code names are treated as current-surface evidence only, not target domain language.

Skills used: domain-design, information-design, solution-design, ontology-design, typescript-refactoring.
