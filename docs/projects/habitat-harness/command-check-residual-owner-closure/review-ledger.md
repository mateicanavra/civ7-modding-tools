# Review Ledger

Status: closed

## Findings

| Priority | Finding | Disposition |
| --- | --- | --- |
| P1 | Do not weaken ecology quality validation to make the new package validator green. | Resolved. The validator was narrowed to the old residual branch scope, then actual JSDoc gaps were fixed. |
| P1 | Do not leave `.habitat` importing Habitat Toolkit internals for boundary taxonomy. | Resolved. The executable adapter moved to `tools/habitat/scripts`. |
| P2 | Docs anchor validation should not be relocated while still red. | Resolved. Stale anchors were updated and the rule is green. |
| P2 | Studio artifact currentness should not remain in Habitat after Nx dependency proof. | Resolved. The Habitat packet was deleted, the package preflight remains in package/Nx ownership, and both Studio-focused Nx proofs passed. |
| P2 | Root CI must not keep invoking the deleted aggregate profile packet. | Resolved. `ci:architecture-strict-core` now runs explicit Habitat owner checks instead of `enforce_domain_refactor_boundary_profile`. |

## Review Notes

- Remaining command-check packets are not automatically wrong. They are acceptable only when the category metadata names the real owner class.
- Historical project ledgers may mention deleted packets as prior evidence; current authority ledgers and execution analytics must not treat them as live packets.
