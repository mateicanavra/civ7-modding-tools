# Planning Admission Review 04

Status: invalidated and changes requested; repaired for a fresh terminal pass

| Lens | Reviewer | Outcome |
| --- | --- | --- |
| information/closed-loop | Hegel the 2nd (`019f4c87-86f8-7b42-bfa0-660ec0284a5f`) | no terminal verdict before corpus change |
| Graphite/state-space | Huygens the 2nd (`019f4c87-8b70-7043-b0e7-6541f84fb194`) | two provisional P2 command-contract findings |
| semantic supervisor | Beauvoir the 2nd (`019f4c87-8d96-7020-910e-bd05d3a9a15d`) | review invalidated by the accepted source-first Habitat planning contract |

The Graphite findings were accepted: an `rg -v` execution error could make the
staged-path filter fail open, and first-parent/distance checks did not reject a
merge-shaped commit. The mutation block now uses an `awk` validator under
`pipefail` and asserts an exact single parent in addition to parent identity.

The accepted Habitat contract records why planning checks use the merged
`#2058` source implementation against the unchanged opening worktree cwd. The
opening harness attempt remains a failed stale-source record; the same enforced
rule passed through refreshed-main source with zero diagnostics.

No digest from this wave is accepted. The post-change candidate reported by the
invalidated supervisor was
`e012902d734a2d5d7eefb5d40b1f18e37ffd43758400c86cdfc5943dfa2b45bd`;
the next supervisor must compute the current post-command-repair digest anew.
