# Takeover Control Reconciliation Wave 01

Status: closed-passed; four accepted P2 findings repaired; three fresh affected-review roles, closing gates, and the exact Graphite seal passed; provider reconstruction next

Record target:
`docs/projects/mapgen-studio-runtime-transition/verification-ledger.md`

## Packet

Objective: make the takeover frame, canonical workstream, live control state,
source/sink accounting, A.2 continuation, agent/process accounting, and next
execution boundary agree before any inherited implementation resumes.

Authority order:

1. current direct user direction;
2. root `AGENTS.md` and repository process;
3. accepted product, architecture, and Habitat authority;
4. reviewed `TAKEOVER-FRAME.md@8ec2a96e3319` as the Stage 2 scope supplement;
5. repaired `WORKSTREAM.md` as the execution method;
6. `verification-ledger.md` as sole live resume state;
7. source/sink, continuation, packet, gate, and cleanup records;
8. Git, tests, processes, and inherited dirt as evidence;
9. historical claims only at their recorded evidence class.

Allowed write scope for the control layer:

- `WORKSTREAM.md`;
- `verification-ledger.md`;
- `NEXT-PACKET.md`;
- `stack-recut-manifest.md`;
- `packet-a2-domain-operation-topology.md`;
- `cleanup-register.jsonl`;
- `gate-register.jsonl`;
- this Wave Packet.

No provider, A.2 Authority, product source, generated artifact, readiness stack,
or inherited dirty-worktree path belongs to this layer.

The initial frozen candidate contained seven paths: the six modified tracked
Markdown/JSONL records plus this untracked Wave Packet. `gate-register.jsonl`
was allowed but unchanged, so it was not part of initial digest
`70e305ca20dbd337c885dceb15f4195c1ddeecd27dc4bc44272e3d64dcfd39da`.
It enters the repaired candidate only to record the real initial gate/review
attempt; no no-op edit is permitted to satisfy a path count.

## Research Wave

All research lanes were read-only, used the shared filesystem, wrote no
repository scratch, and were forbidden from staging, committing, stashing,
resetting, checking out, mutating Graphite/worktrees, or starting persistent
processes.

| Lane | Agent | State | Returned decision input |
| --- | --- | --- | --- |
| control truth and canonical record audit | `/root/control_truth_audit` | closed-passed | maximum eight-path control scope; stale dual-DRA, Stage 0/1/2, lineage, transition, and nonexistent-corpus claims; A.3a admitted and A.5 kept proposed |
| provider source attribution | `/root/provider_source_manifest` | closed-passed | mixed dirt is not a patch source; exact provider-owned corpus, mixed hunk boundaries, missing repairs, clean reconstruction order, and acceptance gates |
| pinned Grit wire and hermeticity | `/root/grit_wire_contract` | closed-passed | pinned package/native identity, scoped catalog/user config, download-disabled execution, check JSON, compact JSONL union, terminal/count laws, root-pair planning, and parse/incomplete state model |

## Integrated Decisions

- One takeover DRA owns Product/Development and Supervisor/Enforcer
  obligations. Independent review remains mandatory.
- Stable review roles are responsibilities, not persistent sessions. Three fresh
  agents fill them for each frozen changeset.
- The reviewed takeover frame is the current Stage 2 scope supplement. The
  workstream folds that scope into canonical execution language now rather than
  leaving contradictory control records.
- The accepted transition is represented directly by the workstream amendment
  and takeover frame; no empty `TRANSITION.md` is created to satisfy an old
  filename.
- The uninstantiated `obligation-corpus.jsonl` plan is retired as an executable
  prerequisite. Existing packet, source/sink, gate, cleanup, and authority
  records remain the corpus surfaces.
- The inherited worktree remains read-only evidence. Provider code is
  reconstructed on a clean child; A.2 Authority remains preserved until both
  generic Grit capabilities seal.
- Atomic reroll is an independently confirmed defect and is sequenced after the
  two capability prerequisites but before the long A.2 domain train unless
  source mapping proves a real dependency.
- Lazy config payload and worker readiness remain proposed concerns whose
  cohesion must be decided before admission.
- C is the Stage 5-8 certification/matrix/archive/merge umbrella, not a
  pre-recut implementation packet.

## Initial Review Outcome

All three roles reviewed exact seven-path digest
`70e305ca20dbd337c885dceb15f4195c1ddeecd27dc4bc44272e3d64dcfd39da`.

| Finding | Role | Severity | Disposition | Repair |
| --- | --- | --- | --- | --- |
| `CTRL-AUTH-001` | architecture/authority | P2 | accepted | distinguish allowed eight-path scope from the actual seven-path initial candidate; add the gate path only for a real attempt receipt |
| `CTRL-TS-001` | TypeScript/state-space | P2 | accepted | add reviewer cleanup/fleet/gate receipts, freeze the complete repaired candidate, and rerun all three roles |
| `CTRL-TS-002` | TypeScript/state-space | P2 | accepted | place independently owned A.3a explicitly between A.2 Authority and Ecology on the one continuation spine |
| `CTRL-TS-003` | TypeScript/state-space | P2 | accepted | terminalize stale planning states and fence the old planning/design-lock procedure as a historical, non-replayable receipt |

The product/runtime/library role passed with no P0-P3. Its pass does not survive
the shared repairs above; all three roles rerun on the complete repaired digest.

## Affected Re-review Outcome

Three new agents reviewed the exact repaired eight-path digest
`78877c67f7b5f45adb772c75717634053d7483638a7f80822e8067a85af092a5`.
Each independently reproduced every path/object row in
`/tmp/mapgen-takeover-control-rereview-objects.tsv`.

| Role | Agent | Verdict |
| --- | --- | --- |
| TypeScript/state-space simplifier | `/root/control_rereview_state` | passed; no P0-P3 |
| architecture/authority simplifier | `/root/control_rereview_authority` | passed; no P0-P3 |
| product/runtime/library verifier | `/root/control_rereview_product` | passed; no P0-P3 |

Terminal reviewer, gate, fleet, and seal receipts may be added after this digest
only as mechanical records of already-observed facts. They may not alter the
reviewed authority, state machine, source accounting, sequencing, product
contract, or provider boundary. Any substantive change reopens affected roles.

## Review Contract

After accepted repairs and real attempt receipts are integrated, freeze the
actual complete candidate and dispatch three new read-only agents in these
stable roles:

1. TypeScript/state-space simplifier, narrowed here to control-state closure,
   impossible/stale states, vocabulary, and evidence-label consistency;
2. architecture/authority simplifier, focused on source order, DRA ownership,
   Habitat/product boundaries, stage admission, and no new tracking apparatus;
3. product/runtime/library verifier, focused on preserved product obligations,
   provider/Grit claims, A.3a/A.5 sequencing, and proof-class honesty.

Every finding must state severity, evidence, impact, smallest repair, residual
limits, and recommended disposition. Accepted P1/P2 findings block the control
seal. A shared authority, stage, source-accounting, or evidence contract repair
reopens all three roles.

## Closing Gates

- fresh clean/inherited worktree, branch, HEAD, staging, operation, readiness,
  agent, and process census;
- line-by-line JSON parse of both JSONL registers;
- `bun habitat classify` for every changed path and every reported docs gate;
- strict OpenSpec validation;
- repository lint and `git diff --check`;
- searches proving no live record assigns current ownership to the standalone
  A.2 DRA or a continuous supervisor, and no executable record requires the
  nonexistent obligation corpus;
- three fresh exact-tree review roles, repair, and affected re-review;
- one exact-set Graphite changeset above `8ec2a96e3319`, protected refs
  unchanged, no remote/PR mutation, and a clean ending worktree.

## Seal Receipt

- Branch: `codex/mapgen-runtime-closeout-control-reconciliation`.
- Sole parent: takeover frame `8ec2a96e3319`.
- Initial Graphite create: `74ab73a08731`; message-corrected pre-receipt
  identity: `f309e96ecfe9`; the terminal amended branch ref is authoritative
  because a commit cannot contain its own hash.
- Range: the same exact eight control paths; terminal deltas are mechanical
  gate, cleanup, phase, lineage, and wave receipts only.
- Ending state: clean worktree, no Git operation, unchanged
  `main@46943c5f1165`, `origin/main@46943c5f1165`, readiness
  `f325250d0878`, and inherited `62/31/75` evidence census; no remote or PR
  mutation.
- Next container: generic Grit diagnostic acquisition reconstructed on this
  clean tip from the bounded source corpus, never transplanted from mixed dirt.

## Non-Goals

- no provider or A.2 Authority implementation;
- no inherited-tree staging or cleanup;
- no final sink graph or recut design;
- no rewrite of historical receipts into present-tense claims;
- no product, packet, live-runtime, or matrix closure claim;
- no readiness, unrelated process, sibling worktree, remote, or PR mutation.
