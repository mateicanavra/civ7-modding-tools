# Planning Prerequisite External Closure

Status: prerequisite cohort closed; planning admission reviewed separately

This packet closes the prerequisite work that remained open after
`planning-prerequisite-terminal-review-01.md`. It is a terminal supplement, not
a rewrite of that historical wave.

## Refreshed Main Checkpoint

The accepted prerequisite chain is:

| Order | PR | Merged identity | Role |
| --- | --- | --- | --- |
| 1 | `#2056` | `ada321597b98` | clean-worktree environment and Studio lifecycle handoff |
| 2 | `#2057` | `2eea5f7dedec` | sole main-root Foundry authority sink |
| 3 | `#2058` | `fab37f842728` | authority-correct Habitat execution and formatting/import harness |
| 4 | `#2059` | `3735af2ada6f` | semantic Studio UI recipe-layout fixture comparison |
| 5 | `#2052` | `46943c5f1165` | pixel-preserving OKLCH value form and DesignSync closure |

`main` and `origin/main` both resolved to `46943c5f1165` after the final
`gt sync --force --no-restack`. None of these commits is a Studio recut source.

## Token External Closure

- DesignSync project `531d158d-a7f6-41cb-87a4-f0f8a5e521b0` received an atomic
  full-content write of 326 managed paths with `_ds_sync.json` alone and last,
  no deletes, and no project-owned exploration removal.
- The final remote hashes were style
  `038266570a82e6c01882e9d80cedc7b1d1c565acb23380c432119dbf7eaeef49`,
  auxiliary `5e30399d30f24acf`, and bundle `26fc014c4139`.
- All 47 components remained unchanged and no grades reopened. Authored
  semantic tokens remained classified as `color`; only the accepted `--tw-*`
  framework-noise class remained.
- The OpenSpec change was archived as
  `2026-07-10-studio-ui-token-oklch`; canonical spec and memory records were
  reconciled.
- Final source `f86f03453802` passed 206 package tests, 38 focused tests,
  TypeScript checks, the real browser collector, seven-row forced-light canary,
  app build, DesignSync check, 350 strict OpenSpec validations, workspace lint,
  classify-reported Habitat checks, and diff checks.
- Final TypeScript/refactoring, structural quality, and Node/Playwright library
  lanes approved. The final approving agents were Herschel the 2nd
  (`019f4c4a-110b-7a83-b38e-b14d5c235f50`), Kepler the 2nd
  (`019f4c4a-140b-7881-ac1a-09ed3b062ffb`), and Avicenna the 2nd
  (`019f4c4a-16fb-7982-b369-d9997c38be8d`).
- PR `#2052` merged as `46943c5f1165`. The token branch and dedicated worktree
  were removed.

## Habitat And Fixture Closure

- PR `#2058@fab37f842728` closed the failed formatting/import attempt through
  Habitat-owned execution rather than a second authority tree. The retained
  harness terminates, uses the accepted repository root/resource topology, and
  keeps non-owned source-reference/generated surfaces outside native Biome
  ownership without hiding repository-owned diagnostics.
- PR `#2059@3735af2ada6f` repaired the recipe-layout guard at its semantic
  boundary: imported JSON is normalized as data and compared structurally,
  rather than excluding the file or pinning formatting bytes.
- The final classify-reported formatting/import, workspace boundary, public
  test-surface, direct-control owner, package-manager, and protected-surface
  rules passed on the accepted trunk.

## Cleanup

- The environment source branch was retired after the primary checkout returned
  to `main`.
- The token source branch/worktree and temporary semantic-fixture worktree were
  removed.
- Removal of the clean Foundry main worktree deregistered it but initially left
  a nonempty filesystem directory. A temporary-index comparison against
  `main@46943c5f1165` found only matching surviving tracked files, partial
  tracked-file deletion from the removal attempt, ignored dependency/build
  output, and no nonignored unique file or live tmux owner. The exact residue
  directory was then removed.
- The detached `wt-ds-sync-token-noise@197957184` worktree is distinct from the
  merged token prerequisite and remains Stage 0 census input only.
- The readiness stack and `studio_runner_parked` remain untouched.

## Close Condition

The prerequisite cohort itself is terminal. Planning admission remains a
separate loop: the live ledger, registers, manifest, normative workstream, and
Next Packet must bind this five-PR checkpoint, and fresh planning reviews must
accept the resulting corpus before the planning layer is committed.
