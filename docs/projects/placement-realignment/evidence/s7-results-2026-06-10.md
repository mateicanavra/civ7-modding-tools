# Placement Realignment — S7 Results (2026-06-10)

Slice: S7 viz + knob surface (OpenSpec change
`placement-realignment-s7-viz`). Verification: full mod + studio test
suites, studio build, and the canonical metrics harness
(`bun run verify:placement-metrics -- --seed 1337 --seeds 5 --size standard
--json /tmp/pm-s7.json`).

**Headline:** every placement step except `assign-advanced-starts`
(recorded exception, no per-plot data exists) now emits decision-substance
viz — 7 layers before → 29 layers after, all artifact-backed; the
audit-cited presentation defects are fixed; placement knobs were confirmed
to reach studio through the generated schema path and the hand-maintained
shadow config was deleted; and metrics are **bit-identical** to S6
(recursive diff of `/tmp/pm-s7.json` vs `/tmp/pm-s6.json`: 0 differing
fields) — viz emission changed no behavior.

## Per-step layer inventory (before → after)

Visibility "default" unless noted. All layers under group
"Gameplay / Placement" (now single-sourced from `stages/placement/viz.ts`).

| Step | Before (audit studio-viz lane) | After |
| --- | --- | --- |
| derive-placement-inputs | — (blank canvas) | `placement.wonders.plannedSites` (points, plan anchors colored by priority 0..1); `placement.discoveries.plannedSites` (points, priority) |
| plot-landmass-regions | `placement.landmassRegions.regionSlot` (grid; opaque None wash) | same grid, None category now transparent (alpha 0) |
| place-natural-wonders | — | `placement.wonders.outcome` (points: Placed / Placed-Relocated / Rejected, from coordinateRows) |
| prepare-placement-surface | — | `map.placement.surface.lakeDrift` (grid, debug: stable/dried/declassified accepted-lake tiles); `map.placement.surface.terrainValidationDrift` (grid, debug: terrain/water changed by engine maintenance) |
| plan-resources | — | `placement.resources.intents` (points, category = resource type with policy-table identity labels); `placement.resources.habitat.{aquatic,cultivated,terrestrial,geological}` (f32 grids, unit valueSpec); `placement.resources.eligibleTypeCount` (u16 grid, types passing policy∧habitat per tile); `placement.resources.legalTypeCount` (u16 grid, debug) |
| assign-starts | `placement.starts.viabilityScore` (no valueSpec), `placement.starts.viabilityTier`, `placement.starts.startPosition` (grid+points) — emitted only AFTER successful assignment; `placement.starts.sectorId` already removed in S4 | viabilityScore (now unit valueSpec) + viabilityTier + NEW `placement.starts.component.{freshwater,fertility,expansion,climate,resource,roughness}` (f32 grids from retained candidate vectors) + NEW `placement.starts.seatRung` (points: regional/open-pool/quality-relaxed/spacing-relaxed) — ALL emitted from the PLAN output before materialization; `placement.starts.startPosition` (grid+points) kept post-stamp |
| adjust-resources | — | `placement.resources.supportAdjustment` (points: move origin / move destination (floor/equity) / added (floor/equity)); `placement.starts.supportRadius` (grid: zone within `settings.supportRadiusTiles` of each seated start) |
| place-resources | — | `placement.resources.outcome` (points: placed / rejected per typed reason) |
| place-discoveries | — | `placement.discoveries.outcome` (points: placed / rejected per typed reason) |
| assign-advanced-starts | — | — (recorded exception: artifact is two engine-effect booleans; no per-plot readback surface exists — see OpenSpec decision log) |
| placement (terminal) | `map.placement.engine.landMask` (grid) | landMask kept + `map.placement.engine.waterDrift` (grid, debug: engine-vs-physics land mask disagreement, the per-tile surface behind waterDriftCount) |

Count: 3/11 steps with layers (7 layers) → 10/11 steps (29 layers; 5 of
them debug-visibility evidence).

## E4.2 / E4.3 verdict

- **E4.2:** 10/11 current steps emit ≥1 decision-substance layer;
  `assign-advanced-starts` is a decision-logged no-content exception (its
  only product is `{fertilityRecalculated, advancedStartsAssigned}`
  booleans — engine effects with no adapter readback). Durable guard:
  `mods/mod-swooper-maps/test/placement/viz-coverage.test.ts` pins the
  per-step expected dataTypeKey sets, meta validity (label + shared
  group), transparent zero categories, overlay-suggestion key existence,
  and sectorId absence (5 tests, green).
- **E4.3:** every layer is backed by a published artifact or op output
  (resource intents/eligibility ← resourcePlan/resourceEligibility inputs;
  components/seats ← plan-starts op output retained since S4; outcomes ←
  typed reconcile artifacts; drift grids ← the snapshots behind already-
  published aggregate counters). No viz-only truth was computed; the only
  derivations at emit sites are scatters/joins of existing rows.

## Presentation fixes (audit studio-viz P3 a/b/c)

- (a) `regionSlot` None category: alpha 210 → 0 (transparent), matching
  startPosition's convention; regression-asserted for every zero-means-none
  categorical layer in viz-coverage.
- (b) `sectorId`: removed by S4; S7 verified zero dangling references
  repo-wide and added a regression test that the key is never emitted.
- (c) `viabilityScore` (and all new 0..1 score layers incl. components and
  habitat intensities): explicit `valueSpec { scale: "linear", domain:
  unit }` so the studio legend domain no longer derives from per-run stats
  (presentation.ts:524-536 fallback no longer applies to these layers).

## Overlay suggestions

9 placement pairs added to
`apps/mapgen-studio/src/recipes/overlaySuggestions.ts` (standard recipe;
browser-test has no placement stage): seats over viabilityScore/Tier, seat
rungs over viabilityScore, intents over each of the 4 habitat families and
over eligibleTypeCount, supportAdjustment over supportRadius. All pairs are
same-step (studio gates candidates on the selected step's dataTypeModel);
every referenced key's emission is pinned by the coverage test — the cited
silent-failure drift mechanism is closed from the emitting side.

## Knob surface outcome

- **Mechanism (investigated):** the audit-cited
  `apps/mapgen-studio/src/ui/data/defaultConfig.ts` + `getStageOrder()`
  path is dead code — zero app importers; the studio panel is fully
  schema-driven from generated artifacts
  (`mod-swooper-maps/recipes/standard-artifacts`: `STANDARD_RECIPE_CONFIG`,
  `STANDARD_RECIPE_CONFIG_SCHEMA`, `studioRecipeUiMeta`; regenerated via
  `bun run --cwd mods/mod-swooper-maps build:studio-recipes`).
- **Placement is present in that surface** (verified post-regeneration):
  schema/defaults carry `placement.{knobs, naturalWonders, discoveries,
  resources, starts, support}` (the S3 resources, S4 starts, S5 support
  knob groups, all derived from op config schemas — no hand-shadowing) and
  uiMeta lists all 11 steps.
- **Cutover shipped:** deleted the hand-maintained shadow file (the drift
  surface itself); updated the stale studio guard tests, which were still
  pinned to the pre-S5 surface (no `support` group; old 9-step order) and
  failing — they now assert the current 6 public groups and the 11-step
  S5/D3 order against the generated artifacts. No hand-authored schema, no
  parallel path, no recorded follow-up needed for placement knobs.

## Hygiene + stretch goal

- "Gameplay / Placement" single-sourced from `stages/placement/viz.ts`
  (was re-declared inline in assign-starts/materialize.ts,
  plot-landmass-regions/index.ts, placement/apply.ts).
- Emitted-key registry stretch goal: OUT (cross-package API design beyond
  slice scope); the per-step coverage test is the shipped cheap guard.
  Decision-logged in the OpenSpec change; S8 deferral candidate.

## Verification runs

| Check | Result |
| --- | --- |
| `bun run --cwd mods/mod-swooper-maps check` | clean |
| `bun --cwd mods/mod-swooper-maps test` | 511 pass / 0 fail (139 files; includes new viz-coverage.test.ts) |
| `bun run --cwd apps/mapgen-studio check` | clean |
| `bun run --cwd apps/mapgen-studio test` | 139 pass / 0 fail (32 files) — was 2 failing pre-S7 (stale placement guards vs regenerated artifacts) |
| `bun run --cwd apps/mapgen-studio build` | vite build + worker bundle check passed |
| `verify:placement-metrics` (seeds 1337-1341, standard 84x54) | `/tmp/pm-s7.json` vs `/tmp/pm-s6.json`: **0 differing fields** (runs + aggregate) |

Browser-runner smoke: the headless equivalents run in CI-shape here — the
mod's pipeline viz tests execute the full standard recipe with a mock
adapter + viz collector (the same emission path the studio worker uses),
and the studio worker-bundle check passed in the build. Interactive studio
visual QA (layer colors/composites in the browser) remains the Milestone B
review item per the proof-class ladder.
