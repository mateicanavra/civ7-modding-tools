# Placement Realignment — S6 Results (2026-06-10)

Slice: S6 inputs/artifact hygiene (OpenSpec change
`placement-realignment-s6-hygiene`). Harness:
`bun run verify:placement-metrics -- --seed 1337 --seeds 5 --size standard
--json /tmp/pm-s6.json` (mock adapter, swooper-earthlike config, 84x54,
8 players, seeds 1337–1341). Before = S5 HEAD (11f9c213d), recomputed in
this worktree as `/tmp/pm-s5-baseline.json` immediately before the slice.

**Headline: every metric is BIT-IDENTICAL to S5** — a recursive diff of
both the per-run records and the aggregates between
`/tmp/pm-s5-baseline.json` and `/tmp/pm-s6.json` reports **0 differing
fields**. This slice changed plumbing (declared inputs, single-publish,
validators, contract layout), not behavior; in particular the wonder
planning surface reconstruction (biome from the `biomeBindings` artifact,
feature from the reified `field:featureType`) produced byte-identical
plans, so no wonder-placement delta needed quantifying.

## Per-item disposition table

| # | Cited bypass (audit-register) | Disposition | Where |
| --- | --- | --- | --- |
| 1a | Wonder planning biome from per-tile `adapter.getBiomeType` readback (inputs.ts) | **FIXED — reconstructed** from `ecology.biomeBindings.engineBiomeId` (the plot-biomes projection artifact; nothing rebinds biomes before placement) | derive-placement-inputs contract + inputs.ts `buildEngineBiomeSurface` |
| 1b | Wonder planning feature from per-tile `adapter.getFeatureType` readback | **FIXED — declared field** `field:featureType` (features-apply reifies the engine feature surface, now unconditionally) | derive contract requires + inputs.ts `readDeclaredFeatureField`; map-ecology features-apply |
| 1c | Wonder planning terrain from per-tile `adapter.getTerrainType` readback | **DECLARED readback (kept)** — `validateAndFixTerrain` runs inside the features projection step with engine-only terrain side effects (mock: coast materialization; live: engine maintenance), so no artifact carries the post-maintenance terrain surface; consistent with the S5-declared plan-resources legality surface read (ADR-009) | derive contract docstring + `readDeclaredEngineTerrainSurface` |
| 2a | Elevation via undeclared `context.buffers.heightfield` (inputs.ts:186,207) | **FIXED** — wonder/discovery planning + wonder plan-input telemetry consume `topography.elevation` (published artifact handle; same buffer, zero data delta) | inputs.ts, natural-wonder-plan-input-telemetry.ts |
| 2b | Terminal placement physics-buffer read (apply.ts:109-117) | **DECLARED (kept)** — the physics-vs-engine landMask comparison IS the step's product (waterDriftCount parity evidence); declared in the placement step contract docstring | placement/contract.ts, apply.ts |
| 3 | `globalThis.GameInfo.Resources` in recipe layer (`readRuntimeResourceCatalog`) | **FIXED** — S3 had NOT fixed it (it survived the refactor as telemetry enrichment); new `EngineAdapter.getResourceCatalog()` (live: GameInfo behind the adapter; mock: policy-table catalog); recipe-layer GameInfo read deleted. Adjacent `resolveActiveResourceAge` GameInfo.Ages read in domain policy left in place (out of cited scope, safe offline default; decision-logged) | civ7-adapter types/live/mock; place-resources materialize + index |
| 4a | placementInputs double-publish (embedded + standalone wonder/discovery plans) | **FIXED** — `PlacementInputsV1` slimmed to mapInfo/starts/wonders/config; each plan single-published by derive-placement-inputs | placement-inputs.ts, derive index.ts |
| 4b | `buildPlacementPlanInput` cross-step import (assign-starts, place-natural-wonders) | **FIXED — deleted**; consumers read `placementInputs.starts`/`.wonders` from the artifact | assign-starts/index.ts, place-natural-wonders/index.ts |
| 4c | `normalizeNaturalWonderStampingStats` cross-step import (prepare-placement-surface, terminal placement) | **FIXED — deleted from consumers**; the artifact is validated at its publish site and consumed as-is | prepare-placement-surface/index.ts, placement/apply.ts |
| 4d | Ordering-only artifact reads (read-and-discard) | **FIXED — deleted 5**: place-discoveries→startAssignment, assign-advanced-starts→discoveryPlacementOutcomes, assign-starts→placementSurfacePreparation, place-resources→placementSurfacePreparation, prepare-placement-surface→naturalWonderPlacement; ordering carried by the existing effect-tag chain (unchanged) | respective contracts + steps |
| 5 | Zero validators on remaining placement artifacts | **FIXED** — validate hooks added for placementInputs, naturalWonderPlan, discoveryPlan, naturalWonderPlacement, discoveryPlacementOutcomes, advancedStartAssignment, placementSurfacePreparation, placementSurfaceValidationBoundary, projectionMeta, landmassRegionSlotByTile, placementOutputs, engineState, placementEngineTerrainSnapshot (cheap invariants: count reconciliation, digest↔row agreement, grid partitions, full-grid buffer lengths) | per-step validate.ts files |
| 6 | Multi-artifact stage `artifacts.ts` (per-file normalization deferred from S5) | **FIXED — applied** — convention text generalizes ("applies to every artifact-contract collection") and S5 explicitly deferred this collection here; 15 contracts split into `stages/placement/artifacts/contract/*.contract.ts`, `artifacts.ts` is assembly-only | placement/artifacts/ |
| 7 | placementOutputs fake fields (floodplainsCount/snowTilesCount hardcoded 0; methodCalls never produced) gate-checked ≥0 | **FIXED — removed** fields from schema + publish site + `isPlacementOutputSatisfied` gate rows (no other consumers; removal preferred over fake measurement) | placement-outputs.ts, apply.ts, tags.ts |

## Metric comparison (5-seed canonical, seeds 1337–1341)

Recursive JSON diff S5-baseline vs S6: **0 differences** (runs and
aggregate). Values for the record:

```
E1.1 invalid 0          E1.3 freshwater 0.925 [0.75..1]   E1.4 ratio 1.1363 [1.0827..1.1829]
E1.5 spacing 7.2 [6..9] pairsBelow6 0                     E1.6 worstPairGap 0.2278, balanced 5/5
E1.7 fallbackRate 0.025 [0..0.125] regionReassigned 0     E1.8 extreme 0.0%
E2.1 Spearman -1.0  min/max per type 2/19.2  CV 0.558     E2.2 shortfall 4 [0..8] recorded 5/5
E2.3 fidelity 0.9695 [0.9484..0.9859]                     E2.4 marine 26 / 6 types
E2.5 pairCorr 1.1226 [1.0123..1.1951] (basePlan 1.1282)   E2.6 0 below floor, min dist 3
E2.7 inRange 0.9765 nearTarget 0.9706 belowMinNoShortfall 0
E2.8 spread 1.0555 [1..1.1401]                            E2.9 0 / 1.0 / 216==216==216
E3.1 minPerStart 3.2 [3..4] belowFloor 0 moves 6.8 adds 0 shortfalls 0
E3.2 maxMinGap 2.0 plannedGapBefore 7.0 → after 2.0       E3.3 5/5
```

E1/E2/E3 gates all hold at their S5 verdicts (HOLD/PASS unchanged).

## What was deleted

- `readEngineSurface` (silent 3-field engine readback) — replaced per the
  disposition table.
- `buildPlacementPlanInput` + `PlacementPlanBundle` (cross-step reach-in).
- `readRuntimeResourceCatalog` + its `globalThis.GameInfo` access.
- 5 read-and-discard artifact requirements (contracts + reads).
- `normalizeNaturalWonderStampingStats` call sites outside the owning step
  (the helper itself remains for the owning step's telemetry path).
- `floodplainsCount`, `snowTilesCount`, `methodCalls` from
  `PlacementOutputsV1` + their gate rows in `isPlacementOutputSatisfied`.

## Verification gates run

- `bun run verify:placement-metrics -- --seed 1337 --seeds 5 --size
  standard --json /tmp/pm-s6.json` — bit-identical to the S5 baseline
  recompute (`/tmp/pm-s5-baseline.json`).
- `bun --cwd mods/mod-swooper-maps test` — **506 pass / 0 fail**. Three
  test updates were required: derive-placement-inputs tests supply the new
  biomeBindings/elevation/field inputs; placement-contracts encodes the
  tag-only ordering and the per-file start-assignment contract path;
  vegetation-plan-apply overrides the mock feature-legality oracle — the
  unconditional `field:featureType` reify exposed that the test's synthetic
  context applied 0 features while the old all-zeros field counted every
  tile as applied (fake-green fixed, not papered over).
- `bun run --cwd mods/mod-swooper-maps check` — clean.
- `bun run verify:placement-catalogs` — green.
- `bun run --cwd mods/mod-swooper-maps gen:maps` and `build:studio-recipes`
  — zero generated drift (no knob/config schema changed this slice).
- `openspec validate --all` — 87 passed, 0 failed.

## Notes

- The wonder plan-input telemetry (`NATURAL_WONDER_PLAN_INPUT_V1`) keeps
  its per-tile engine digests — that readback is the evidence surface the
  earthlike live-parity proofs compare against (readback-as-evidence stays;
  readback-as-planning-truth is what S6 removed).
- RESOURCE_PLACEMENT_V1 telemetry now also logs on mock runs (the mock
  adapter serves a non-empty policy-table catalog); previously the
  GameInfo gate made it live-only. Evidence-only output.
- Milestone B (live) is NOT claimed: the declared terrain readback and the
  biome/feature reconstruction equivalence are proven on the mock
  maintenance model; the live `validateAndFixTerrain` drift window is the
  Milestone B measurement.
