# Status: Superseded Seam Note

The old foundation-only recipe config override seam has been retired. MapGen Studio now treats recipe config through the active Standard recipe artifacts in `mod-swooper-maps/recipes/standard-artifacts`.

Current implementation anchors:

- `apps/mapgen-studio/src/recipes/catalog.ts`
- `apps/mapgen-studio/src/browser-runner/recipeRuntime.ts`
- `mods/mod-swooper-maps/src/recipes/standard/recipe.ts`
