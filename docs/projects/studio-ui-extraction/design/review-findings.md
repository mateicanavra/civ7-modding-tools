# Adversarial design review — 3 lenses (2026-07-01)

All findings below were folded back into DESIGN.md + the OpenSpec change set in the same session (see DESIGN.md status header). Recorded for the audit trail.

## Lens: DESIGN-SYNC CONTRACT + FIDELITY ORACLE — converter demands (resolveDistEntry/exportedNames/cssEntry/fonts/provider), anchor-survival mechanics, title/fork/upload discipline, dual-theme CSS equivalence
Survives: True

### [MAJOR] openspec/changes/studio-ui-extraction/design.md §2.5 (line 71) and DESIGN.md §1 sync-repoint bullet (line 38)
- **Defect:** The committed decision to DROP `entry` from .design-sync/config.json is mechanically under-specified for this repo and silently overrides Designer 3's opposite, evidence-backed recommendation without an adjudication. `resolveDistEntry` reading `module`/`exports` only works after PKG_DIR is found, and without `entry` (config or --entry flag) PKG_DIR = join(NODE_MODULES, pkg) (package-build.mjs:158-176). Under this repo's bun ISOLATED linker (bunfig.toml `linker = "isolated"`) the root node_modules has no @civ7 scope at all and a package never self-installs, so the skill-default choice of --node-modules ("the DS package's own node_modules", storybook SKILL.md §2.5 — which also states 'In the DS's own source repo node_modules/<pkg> doesn't exist, hence --entry') makes PKG_DIR nonexistent → [NO_DIST] hard exit, and cssEntry/docsMap/readmeHeader/tsconfig all PKG_DIR-resolve wrong. Designer 3 kept `entry: "dist/index.js"` precisely to keep PKG_DIR 'deterministic ... independent of node_modules topology' (storybook-sync.md M5, §4); Designers 2/4 dropped it assuming a workspace symlink that only exists inside a DEPENDENT's node_modules (e.g. the app's). DESIGN.md hedges '(or dropped)' — an unresolved either/or — while the openspec design commits the fragile branch, and the B7 runbook (tasks.md 7.2, design.md §3) never pins --node-modules nor mentions --entry.
- **Evidence:** package-build.mjs:152 (ENTRY_OVERRIDE = flag('entry', cfg.entry)), :158-176 (PKG_DIR walk-up vs join(NODE_MODULES, PKG)); bundle.mjs:13-42 (resolveDistEntry needs pkgDir/pkgJson first); dts.mjs:270-283 + package-build.mjs:243,690-702 (exportedNames/surface filter also keyed off PKG_DIR); verified: `ls node_modules/@civ7` at repo root is empty, bunfig.toml linker=isolated; design/storybook-sync.md §0 M5 + §4 config keeps entry; design/build-css.md §9 drops it; DESIGN.md §2 adjudications do not list this conflict.
- **Fix (applied):** Adjudicate explicitly: keep `entry: "dist/index.js"` in the repointed config (Designer 3's branch — deterministic, zero cost), or, if dropping it, pin the runbook to `--node-modules apps/mapgen-studio/node_modules` AND add `--entry dist/index.js` to every converter/driver invocation in design.md §3 and tasks.md 7.2. Record the resolution in DESIGN.md §2.

### [MAJOR] DESIGN.md §3 (lines 95-113) and openspec tasks.md §7 / specs/studio-ui-package/spec.md theme scenario
- **Defect:** The FRAME §2 DoD item 'the fidelity gate can exercise both theme modes (a light-render canary), or the team has recorded a deliberate decision not to add one' is dropped. The synthesis ships only the STATIC half (theme-invariant vitest on dist/styles.css + CI verify block assertions — token presence/values), and two designers explicitly handed the render half upstream: build-css.md §4d ('the render-time half (a `.light` card render) belongs to the verification designer') and structure-rewire.md §7 hook row 'Light-mode canary ... coordinate with Designer-sync's canary design' — but storybook-sync.md designs no canary, DESIGN.md §3 verification omits it, tasks.md 7.2 has no light-mode step, and no deliberate skip decision is recorded anywhere.
- **Evidence:** FRAME.md §2 bullet 7; design/build-css.md §4d last paragraph; design/structure-rewire.md §7 table (Light-mode canary row, B7); DESIGN.md §3 lists only 'dark `:root` AND `.light` blocks in styles.css' as the theme check; spec.md 'Theme flip is invisible to users' scenario is computed-token-level only. Failure scenario: a `.light`-scoped rule or cascade-order regression that keeps token NAMES intact ships while the oracle (initialGlobals: dark) and CI both render/inspect dark-only — the exact FRAME §4 blindspot the workstream exists to close, still silent.
- **Fix (applied):** Either add a B7 light-render step (e.g. one scoped compare/capture pass with the reference and preview forced `.light`, or a small light-mode render smoke over 2-3 token-heavy stories) to DESIGN.md §3 + tasks.md 7.2, or record in DESIGN.md the deliberate decision that the static theme-invariant test is the accepted light guard and a render canary is declined — the FRAME accepts either, but not silence.

### [MINOR] DESIGN.md §1 CSS bullet (line 29) + package Storybook preview design (design/storybook-sync.md §1 preview.tsx importing the source CSS entry)
- **Defect:** The claim 'authored `@font-face` with `url(./fonts/…)` correct at authoring time' is only true in the DIST layout: build-css.md §6 copies woff2s exclusively to dist/fonts/, so src/styles/fonts.css's relative URLs dangle at source. The @tailwindcss/cli compile never resolves url() (fine), but the package Storybook preview imports the SOURCE CSS entry (storybook-sync preview.tsx `import "../src/styles.css"`; DESIGN.md 'preview = fonts + package CSS'), and the Vite/SB pipeline DOES resolve CSS url() from src/styles/ — the sb-reference build (the oracle's reference side, needed at every B2-B7 smoke and the B7 run) either fails on the unresolved assets or ships inert dangling @font-face rules. The seam between 'fonts.css is dist-relative' and 'Storybook compiles source' is reconciled by neither designer nor the synthesis.
- **Evidence:** design/build-css.md §4b (fonts.css authored with url(./fonts/…)), §6 (woff2s copied to dist/fonts/ only; 'same-dir @import means url survives untouched' — true only for the CLI path); design/storybook-sync.md preview.tsx line importing ../src/styles.css plus six direct @fontsource imports (which mask the visual symptom — faces still load via @fontsource, so a silent strip leaves the oracle rendering correctly by accident).
- **Fix (applied):** Pick one at B1: (a) commit/copy the woff2 set under src/styles/fonts/ so the authored URLs resolve in BOTH layouts (dist copy becomes a plain dir copy), or (b) have the Storybook preview import theme.css (tokens/globals) rather than the fonts-including compile entry, and document that fonts.css is dist-layout-relative. Amend DESIGN.md §2.1 accordingly.

### [MINOR] openspec/changes/studio-ui-extraction/design.md §1 tree (line 15, exports comment) vs the committed single-entry tsup build (line 18 / DESIGN.md §1 Build bullet)
- **Defect:** The `./types` subpath is internally inconsistent with the adjudicated build: structure-rewire §3.6 maps `"./types".import` → `./dist/types/index.js` (and identity-wiring wants a second tsup entry to emit a runtime stub), but the synthesis committed SINGLE-entry tsup (`entry: src/index.ts`, build-css §2) which never emits dist/types/index.js — the exports map would name a nonexistent runtime target. Type-only imports (`import type ... from "@civ7/studio-ui/types"`, the app rewire's stated form) survive via the types condition, but any value-position resolution of the subpath (vitest/node resolution, a future value re-export) hits a missing file. Also unadjudicated: Designer 1 targets ./dist/types.{d.ts,js} while Designer 4 targets ./dist/types/index.{d.ts,js}.
- **Evidence:** design/structure-rewire.md §3.6 exports block; design/identity-wiring.md §3.2 ('Emit a real (near-empty) dist/types.js ... tsup emits this for free from a type-only entry'); design/build-css.md §2 tsup config `entry: { index: "src/index.ts" }`; DESIGN.md §1 'tsup (single entry, ESM, browser)'.
- **Fix (applied):** Decide at B1: make `./types` a types-only export (`{ "types": "./dist/types/index.d.ts" }`, no `import` condition) since the tsc tree emit produces the .d.ts for free, or add the second tsup entry and amend 'single entry' in DESIGN.md §1 / design.md §1.

### [MINOR] DESIGN.md §1 (lines 18-20, 'every dep pinned from today's lockfile resolutions ... @fontsource/inter + jetbrains-mono in package deps') vs design/build-css.md §6 and identity-wiring.md §3.4
- **Defect:** Both designer manifests pin @fontsource/inter + @fontsource/jetbrains-mono at 5.1.1 (build-css: exact '5.1.1', 'filenames are the contract'; identity-wiring: '^5.1.1'), but today's resolved version is 5.2.8 — copying the designer JSON verbatim DOWNGRADES the fonts, which can change woff2 filenames/subsets and the shipped font bytes, the exact hazard Designer 2's own 'filenames are the contract' rationale names. The synthesis states the correct rule (pin from lockfile resolutions) but never corrects the stale designer numbers, and tasks 1.1 says only 'pinned deps incl. ... @fontsource pair'.
- **Evidence:** apps/mapgen-studio/package.json declares ^5.1.1; verified resolved apps/mapgen-studio/node_modules/@fontsource/inter/package.json version = 5.2.8 (jetbrains-mono likewise 5.2.8). All other spot-checked pins are correct (cva 0.7.1, clsx 2.1.1, tailwind-merge 3.6.0, tailwindcss 4.3.0, sonner 2.0.7, rjsf 6.2.5, lucide-react 0.522.0, typebox 1.0.80).
- **Fix (applied):** Add an erratum to DESIGN.md §5 (or a note in tasks 1.1): @fontsource pair pins are 5.2.8 per today's resolutions; do not copy the 5.1.1 figures from the designer manifests.

### [MINOR] openspec/changes/studio-ui-extraction/tasks.md 3.1 (line 22) — AppFooter move item
- **Defect:** Binding LEDGER adjudication 6 ('One TooltipProvider policy — ambient provider ... strip AppFooter's inline provider', AppFooter.tsx:151) is absent from the execution checklist: tasks 3.1 carries the seedMin/seedMax inject-prop but not the provider strip, and it is also missing from the B8+ cleanup list (8.1) and from DESIGN.md — an implementer following tasks.md verbatim moves AppFooter with its self-provided TooltipProvider intact, violating the frozen one-provider adjudication.
- **Evidence:** LEDGER.md §3 adjudication 6 and row 18 ('self-provides TooltipProvider (strip per policy)'); ledger/coherence.md §2.6 (strip + 'verify hover manually'); tasks.md 3.1 and 8.1 contain no strip item.
- **Fix (applied):** Add the strip (plus the coherence-recorded manual hover verification note) to tasks 3.1.

### [MINOR] openspec/changes/studio-ui-extraction/design.md §2.6 (lines 75-79) / DESIGN.md §3 CI bullet
- **Defect:** The FRAME §2 DoD item 'the sync is CI-runnable from the package build (in this repo that means an Nx target)' is mutually deferred by the designers and never landed: identity-wiring §5.4 reserves a custom target name and says 'exact target composition belongs to the Q6 designer'; storybook-sync §5 says the target 'lands on the package project' but designs only manual runbook commands; the synthesis and tasks.md define the `verify` assertions and correctly keep Chromium out of CI, but no design or task ever creates the sync-invocation target itself.
- **Evidence:** FRAME.md §2 bullet 6; design/identity-wiring.md §5.4 ('names reserved ... final composition is Q6's'); design/storybook-sync.md §5 last bullet; tasks.md contains no target-creation item; DESIGN.md §3 covers only the five-target `verify` assertions.
- **Fix (applied):** Add one line to tasks 7.1 (or B1): define the package-project custom target (e.g. `design-sync:check` wrapping the driver invocation with its flags) outside the CI five, satisfying the DoD's 'Nx target' wording — or record that buildCmd-as-nx-invocation is the accepted reading.

### [MINOR] DESIGN.md §3 / §6 — absence of any incrementality statement
- **Defect:** FRAME §2's final DoD item ('Incrementality has improved or has a designed path: today one shared bundle + one shared styleSha de-incrementalizes all 46 ...') is unaddressed in the synthesis and the openspec change. Designer 3's bare-name import strategy IS a real incrementality improvement (story bytes never mention internal paths → srcSha stability → grades carry forward across future internal refactors), but it is never surfaced against this DoD item, and the shared-bundle/styleSha structure question (FRAME §5 investigation item) gets no recorded verdict.
- **Evidence:** FRAME.md §2 last bullet + §5 last bullet; design/storybook-sync.md §2 Option C ('grades carry forward forever after this one re-key'); DESIGN.md and openspec design.md contain no incrementality section; tasks 9.2's DoD walk would hit this cold.
- **Fix (applied):** Add a short recorded position to DESIGN.md (e.g. §3 or §6): bare-name story imports + carried grades are the bought improvement; bundle partition/per-group styleSha investigated-and-declined (or deferred with a pointer), so the DoD walk at close-out has an answer.

## Lens: AUTHORITY CONSISTENCY — LEDGER §3 (12 adjudications) walked one-by-one; coherence §3 ownership table walked row-by-row against the target tree/tasks; DESIGN.md §2 adjudications spot-checked against al
Survives: False

### [BLOCKER] openspec/changes/studio-ui-extraction/tasks.md §3.1/§8.1 + docs/projects/studio-ui-extraction/DESIGN.md (whole file) — vs LEDGER.md §3 adjudication 6
- **Defect:** Binding LEDGER adjudication 6 is only half carried: the ambient-TooltipProvider policy and its documentation are present, but the second binding instruction — 'strip AppFooter's inline provider' (coherence.md §2.6: 'strip AppFooter's inline provider (behavior-only at rest; oracle-safe for static capture, verify hover manually)') — appears nowhere in DESIGN.md, proposal.md, design.md, or tasks.md. Executed as written, B3 moves AppFooter verbatim-plus-seed-props and the package ships a component that self-provides TooltipProvider (AppFooter.tsx:151, no delayDuration), violating the one-provider-policy adjudication and silently changing tooltip delay semantics relative to the ambient 300ms provider.
- **Evidence:** grep -rn -i 'strip|inline provider' over DESIGN.md + openspec/changes/studio-ui-extraction/ returns zero hits (excluding stripSchemaMetadataRoot). AppFooter.tsx:145-151 confirms the inline provider ('The footer carries its own TooltipProvider…'). tasks.md:22 (task 3.1) lists only seed props for AppFooter; tasks.md:52 (task 8.1, E3 wave) does not list the strip. LEDGER.md:117-118 makes the strip binding.
- **Fix (applied):** Add the AppFooter inline-provider strip to the plan explicitly — either in B3 (with the coherence-mandated manual hover verification) or as a named E3-wave item — and reference adjudication 6 in tasks.md and DESIGN.md §1.

### [MAJOR] DESIGN.md §3 + design.md §3 + tasks.md §7 — vs FRAME.md §2 (line 30, light-render canary)
- **Defect:** The FRAME DoD requires 'a light-render canary, or … a recorded deliberate decision not to add one'. The synthesis ships only the static half (theme-invariant vitest asserting `.light` token blocks) and records neither a render-time light canary nor a decision to skip one — so the dark-only-gate blindspot (FRAME §4: 'a gate that only looks at one rendering mode can only verify that mode') survives as a silent failure mode. Designer 2 explicitly deferred this ('the render-time half (a .light card render) belongs to the verification designer' — build-css.md:242) and Designer 4 listed a B7 'Light-mode canary' hook (structure-rewire.md:527); the synthesized verification design (DESIGN.md §3) dropped it without adjudication.
- **Evidence:** grep -rn -i 'canary' over DESIGN.md + openspec/changes/studio-ui-extraction/ returns zero hits. tasks.md:46 (7.2) runs 'full render-check → grade all 46' with no light-mode pass; the render-check grades against the dark-default preview only.
- **Fix (applied):** Either add the B7 light-render canary (a `.light`-state capture/grade pass or a scripted `.light` computed-style probe, per Designer 4's hook) to DESIGN.md §3 and tasks 7.2, or record an explicit deliberate decision not to add one (a Matei-checkpoint line item).

### [MAJOR] DESIGN.md (whole file) + proposal.md + design.md — vs FRAME.md §2 (line 31, incrementality)
- **Defect:** The FRAME DoD requires 'Incrementality has improved or has a designed path' (today one shared bundle + one shared styleSha de-incrementalizes all 46 on any shared CSS change). The synthesis is completely silent: it keeps the single-bundle/single-styleSha shape, prices the one-time O(46) re-key, and neither designs an incrementality improvement, records a path, nor records a decision that none is buyable. FRAME §5 made this a named investigation item ('do not fill these by assumption'); it was dropped, not investigated or adjudicated.
- **Evidence:** grep -rn -i 'incremental' over DESIGN.md + openspec/changes/studio-ui-extraction/ returns zero hits. FRAME.md:31 and FRAME.md:84 state the requirement and the investigation mandate. The close-out task (tasks.md:57, 9.2 'Frame §2 DoD walk with evidence') would fail on this bullet as the design stands.
- **Fix (applied):** Add an incrementality section to DESIGN.md: either a designed path (verified against resync.mjs mechanics — e.g. what the same-shape repoint does/doesn't buy) or an explicit recorded finding that no improvement is buyable under the converter's contract, presented at the Matei checkpoint.

### [MAJOR] design.md §2.6 + tasks.md — vs FRAME.md §2 (line 29, 'sync is CI-runnable … an Nx target')
- **Defect:** The DoD requires the sync be CI-runnable from the package build via an Nx target. Both designers planned this: identity-wiring.md §5.4 reserves a manually-invoked custom target ('e.g. design-sync:check, deliberately outside the CI five … exact target composition belongs to the Q6 designer') and storybook-sync.md §5 states 'The Nx design-sync target (FRAME §2 CI-runnable) lands on the package project'. The synthesis dropped it: design.md §2.6 defines only build/check/test/clean/verify and the render-check-never-in-CI rule; no task creates any sync target — the sync remains a hand-run `node .ds-sync/resync.mjs` invocation (tasks.md:46).
- **Evidence:** grep -rn -i 'design-sync:check|nx.*design-sync|CI-runnable' over DESIGN.md + openspec/changes/studio-ui-extraction/ returns zero hits.
- **Fix (applied):** Commit the custom Nx target (name + composition, per the two designer reports) on the package project in design.md §2.6 and add its creation to tasks (B7 is the natural home), keeping it outside the CI five per the weight rule.

### [MAJOR] design.md §1 (line 18 'single entry src/index.ts' vs line 15 exports './types') + DESIGN.md §1 (lines 21-22 vs 15-16)
- **Defect:** Internally contradictory build spec: the committed design adopts Designer 2's tsup single-entry recipe (build-css.md §2, which notably has NO './types' export at all) while also committing a './types' subpath export taken from Designers 1/4 — both of whom said that subpath needs a second emitted JS entry (identity-wiring.md §3.2: 'Emit a real (near-empty) dist/types.js beside the .d.ts … tsup emits this for free from a type-only entry'; structure-rewire.md:90: tsup 'entries: src/index.ts, src/types/index.ts'). Under the committed single-entry build + tsc --emitDeclarationOnly, no dist/types.js exists, so the './types' export map either points at a nonexistent import target or must be types-only — nowhere specified. The 19 app repoints target this subpath (structure-rewire §4.1; tasks.md:17).
- **Evidence:** design.md:18 ('single entry src/index.ts'); design.md:15 (exports list includes './types'); build-css.md:52-63 + 315-325 (single entry, exports without './types'); identity-wiring.md:84 + 139; structure-rewire.md:263-264.
- **Fix (applied):** Resolve in design.md: either add the second tsup entry (src/types/index.ts) with a real dist import target, or specify './types' as a types-only export condition and verify every consumer stays `import type` (vitest/Vite resolution included). Record which designer's exports map won, like adjudication 1 did for CSS.

### [MAJOR] tasks.md §5 (and §2-§6 generally) — missing the E1-B parity-test drift fence
- **Defect:** The app-side type-level parity test is the load-bearing mitigation that makes the recommended E1-B defensible (DESIGN.md §2.2: reverse-drift 'mechanically fenced by an app-side type-level parity test that fails mapgen-studio:check on drift'; design.md §2.8; identity-wiring.md §4 E1-B: 'test/types/contractParity.test-d.ts with mutual-satisfies assertions for the three shapes'). No task creates it: task 5.1 executes the splits 'type-only over contract types per E1 outcome' with no fence, and no other task or spec scenario mentions it. Under the recommended default, execution per tasks.md ships the re-homed DAG/status shapes with the exact unfenced reverse-drift risk the LEDGER flagged as HEAVY.
- **Evidence:** grep -rn 'parity' over tasks.md matches only the CSS token-count parity in 1.3. LEDGER.md §5 E1 names the reverse-drift risk; coherence.md §4 E1 calls the DAG re-home HEAVY.
- **Fix (applied):** Add the parity test to B5 (status/phase unions + RecipeDagResult) as an explicit task deliverable in the app's test project, and add a spec scenario asserting drift fails mapgen-studio:check.

### [MAJOR] design.md §2.5 ('entry dropped') vs DESIGN.md §1 ('entry → dist entry (or dropped …)') vs storybook-sync.md §4 — unadjudicated Designer 2/Designer 3 conflict
- **Defect:** The two sync-adjacent designers directly disagree on cfg.entry: Designer 2 deletes it (build-css.md §9 table: 'entry: deleted — resolveDistEntry reads module/exports'), while Designer 3 — the Q6 sync owner — deliberately keeps `entry: "dist/index.js"` because it 'keeps PKG_DIR anchoring deterministic via the named-package.json walk-up (M5) independent of node_modules topology' (storybook-sync.md:243). Without entry, PKG_DIR = join(NODE_MODULES, pkg) (verified: package-build.mjs:158-176), making cssEntry/docsMap/readmeHeader resolution depend on node_modules layout under bun's isolated linker. DESIGN.md §2's adjudication list (which exists precisely to record cross-designer conflicts) omits this one entirely; DESIGN.md §1 hedges both ways while design.md commits 'dropped' — the two synthesis artifacts disagree in firmness and the sync designer's mechanically grounded rationale is silently overridden.
- **Evidence:** design.md:71; DESIGN.md:38-39; build-css.md:365 (table row); storybook-sync.md:243 + M5 (storybook-sync.md:24); package-build.mjs:158-176 read directly.
- **Fix (applied):** Adjudicate explicitly in DESIGN.md §2 (keep-entry is the lower-risk branch per the Q6 owner's M5 analysis; dropping it requires verifying node_modules/<pkg> realpath-consistent containment under the isolated linker) and make design.md §2.5 match the adjudicated outcome.

### [MINOR] tasks.md §8.1 + DESIGN.md §4.4 — E3 wave enumeration drops two adjudicated items
- **Defect:** The E3 cleanup-wave list omits (a) the panels/forms manual-join normalization that is part of binding adjudication 4 (LEDGER §3.4 'manual joins normalized'; coherence §2.8 'panels/forms join normalization (oracle-gated batch)') and (b) the `${id}__error` shared id-builder extraction (LEDGER row 37 remedy 'extract shared id builder'; LEDGER §7 contract; structure-rewire B8+ 'forms/fieldIds.ts'). Both were in Designer 4's B8+ list; the synthesized lists read as exhaustive and drop them.
- **Evidence:** grep -rn -i 'join normal|manual join|fieldIds|id-builder' over DESIGN.md + openspec change returns zero relevant hits; tasks.md:52 enumerates the wave without either item; structure-rewire.md:506 includes both.
- **Fix (applied):** Add both items to tasks 8.1 and DESIGN.md §4.4.

### [MINOR] specs/studio-ui-package/spec.md line 18 ('the 25 adjudicated shared-kernel modules') — vs coherence.md §3
- **Defect:** The spec requirement (inheriting LEDGER §4's 'Full 25-module table') binds execution to '25 adjudicated shared-kernel modules', but the coherence §3 ownership table contains 24 rows (counted mechanically). An executor walking the checklist cannot reconcile the count; the ownership walk itself is otherwise complete — every table row maps to exactly one owner in the target tree/tasks.
- **Evidence:** awk over coherence.md §3 counts 24 '| `' table rows; LEDGER.md:137 and spec.md:18 both say 25.
- **Fix (applied):** Record the count correction in DESIGN.md §5 errata (the section built for exactly this) and fix the spec wording to '24' or 'the coherence §3 ownership table'.

### [MINOR] proposal.md line 13 (deletions include 'storybook/{storeReset,queryStub}.ts') — vs LEDGER.md §3.12
- **Defect:** The frozen LEDGER's literal binding text says 'storeReset/queryStub stay app-side'; the synthesis deletes them. The retire branch is legitimized only by coherence §2.12's parenthetical '(or retire with the old preview)', which the LEDGER digest dropped — so the change set diverges from the frozen LEDGER's letter without recording the divergence in the errata section (§5), unlike the other two corrections which were dutifully recorded.
- **Evidence:** LEDGER.md:132-133 vs coherence.md:30 vs proposal.md:13 and tasks.md:45 (7.1).
- **Fix (applied):** Add one line to DESIGN.md §5 errata noting the retire branch of coherence §2.12 is taken (app has zero stories post-move, so 'stay app-side' would keep dead code).

### [MINOR] tasks.md 1.3 + design.md §2.6 + DESIGN.md §3 — server-specifier dist fence labeled 'under E1-B'
- **Defect:** Designer 1 designed the 'no @civ7/studio-server specifier in dist/*.js' verify assertion as the E1-A runtime-leak fence (identity-wiring.md §4: 'enforceable with a one-line dist grep in verify'; §5.4: 'E1-A runtime-leak fence'). The synthesis attaches it 'under E1-B' in all three artifacts — meaning if Matei picks E1-A (where type-only imports exist and the fence is load-bearing), the letter of the plan drops the assertion exactly where it matters.
- **Evidence:** tasks.md:10 ('no server specifier in dist JS under E1-B'); design.md:77-78; DESIGN.md:106-107; identity-wiring.md §4/§5.4.
- **Fix (applied):** Make the fence unconditional (it is meaningful under both branches) or condition it on E1-A.

### [MINOR] specs/studio-ui-package/spec.md lines 12-14 (theme-artifact scenario) — vs the designed test (build-css.md §4d, tasks.md 1.3)
- **Defect:** The spec scenario promises 'classless (dark-default) and .light computed token values match today's artifact', but the designed theme-invariant test asserts token-NAME-set identity + count parity + one spot value (tasks 1.3: 'token count parity'); value-level equivalence is carried by the verbatim-move discipline and the §4c cascade proof, not by the test the scenario names. The spec overstates what the gate mechanically proves.
- **Evidence:** spec.md:12-14 vs build-css.md:236-240 vs tasks.md:10.
- **Fix (applied):** Either strengthen the test to assert all token values against the extracted blocks of today's _ds-compiled.css, or soften the scenario to token-set/spot-value parity plus the cutover computed-style probe (build-css.md §10).

## Lens: EXECUTABILITY OF THE 8-BRANCH STACK — branch-by-branch CI-green claims, transitive import traps between move branches, falsifier set (two owners / forms split), reality of per-branch verification hook
Survives: False

### [BLOCKER] openspec/changes/studio-ui-extraction/tasks.md §2.3/§3.2/§4/§5 (lines 15-36) + docs/projects/studio-ui-extraction/design/structure-rewire.md §4
- **Defect:** B2–B5 are not CI-green as written: tasks.md bins structure-rewire's END-STATE app-edit lists into branches without the interim repoints for still-app-side consumers of modules each branch moves-and-deletes, and it schedules barrel trims one branch after the underlying files are deleted.
- **Evidence:** Grep-verified at the worktree (all under app tsconfig include:["src"], so `mapgen-studio:check` and the app vitest gate go red): (a) B2 (tasks.md:15-17) moves ui/types, LAYOUT (layout.ts), useResolvedTheme.ts, the 15 primitives, and FieldRow, but lists only "19 ui/types repoints, 2 constants repoints, useToast, StudioProviders/DeckCanvas"; not repointed and still app-side until B3/B5/B6: AppFooter/AppHeader/ExplorePanel/RecipePanel/ViewControls import `../types`; AppFooter.stories/RecipePanel.stories import `@/ui/types`; AppFooter.tsx:12/ExplorePanel.tsx:25/RecipePanel.tsx:34 import LAYOUT from `../constants`; PipelineStage.tsx:5 imports `../../ui/hooks/useResolvedTheme`; ~12 files import `components/ui` primitives (rjsfWidgets.tsx, PresetDialogs.tsx, mockWidgetProps.tsx, BrowserConfigFieldTemplate.stories.tsx, 8 surface components); rjsfTemplates.tsx + mockWidgetProps.tsx import FieldRow. Staying tests break too: test/controllers/{usePresetLifecycle,useRunInGame,useSaveDeploy}.test.tsx, test/runInGame/clientState.test.ts, test/studioState/persistence.test.ts import `@/ui/types`; test/runInGame/GameConsole.test.tsx:4 imports `../../src/components/ui/tooltip` (its relocation is B5). (b) Ordering: tasks.md:15 (2.1) deletes layout.ts/useResolvedTheme.ts at B2 but the `ui/hooks`/`ui/constants` barrel trims are scheduled at 3.2 (B3); src/ui/hooks/index.ts:13 and src/ui/constants/index.ts:14 re-export the deleted files → B2 check fails on its own text. (c) B3 deletes EmptyState's app copy while PipelineStage.tsx:4 imports it until B5 — structure-rewire.md §4.4 explicitly defers that repoint to "its move". (d) B4 (tasks.md:27-29) has NO app-rewire item, but RecipePanel.tsx:31-32 (app-side until B5) imports SchemaConfigForm + useConfigCollapse, deleted at B4. (e) B5 (tasks.md:33-34) omits StudioShell's ExplorePanel/GameConsole/RecipePanel/PipelineStage import repoints — all binned into B3's "StudioShell component repoints" (structure-rewire §4.4 writes StudioShell as ONE edit).
- **Fix (applied):** Derive per-branch repoint lists mechanically: in the branch that moves a module, repoint EVERY not-yet-moved consumer (src/, stories still in src/, and test/) to the package, and trim the ui/hooks + ui/constants barrels in B2. Add an explicit B4 app-rewire item (RecipePanel's two configOverrides imports) and split the StudioShell edit across B3/B5/B6. Encode the rule in tasks.md (e.g. "each branch: grep for the moved module's old path must return empty AND check+vitest green") rather than fixed file counts.

### [MAJOR] openspec/changes/studio-ui-extraction/tasks.md line 22 (task 3.1, AppFooter)
- **Defect:** The E2-A consequence is dropped from the task plan: under the recommended default (options data stays app-side), AppFooter cannot compile inside the package at B3 without the three options props, but 3.1 carries only the seedMin/seedMax inject-props.
- **Evidence:** AppFooter.tsx:12 imports MAP_SIZE_OPTIONS, MAP_SIZE_SHORT, PLAYER_COUNT_OPTIONS from `../constants`; under E2-A those stay app-side (LEDGER §4 ownership split, DESIGN §4.3 Rec (A)). structure-rewire.md §4.5 specifies the required remedy — `mapSizeOptions`/`mapSizeShortLabels`/`playerCountOptions` props mirroring today's shapes, StudioShell passing the app constants, story args updated — and warns "E2 changes AppFooter's public API — surface this at the Matei checkpoint before B3". tasks.md 3.1 says only "(+ required seedMin/seedMax props per adjudication 2; story args = today's exact values)" and 3.2 only "seed props"; no branch implements the E2-A options-via-props contract anywhere.
- **Fix (applied):** Add the E2-A options-props work to 3.1/3.2 (AppFooter props + StudioShell wiring + story args), conditioned on the checkpoint outcome, with the E2-B alternative (options.ts/defaults.ts move) as the recorded redirect path.

### [MAJOR] openspec/changes/studio-ui-extraction/tasks.md §5 (line 33) vs DESIGN.md §2.2 (line ~82) and openspec design.md §2.8
- **Defect:** The app-side type-level parity test — the drift fence that makes E1-B's reverse-drift risk acceptable in the synthesizer's own adjudication — is created by no task in any branch.
- **Evidence:** DESIGN.md §2.2: "E1-B's reverse-drift risk … is mechanically fenced by an app-side type-level parity test that fails `mapgen-studio:check` on drift"; openspec design.md §2.8 commits to "E1-B … + app-side parity-test drift fence". tasks.md 5.1 implements statusLabels "type-only over contract types per E1 outcome" and 1.3's verify covers only "no server specifier in dist JS", which proves independence, not parity — nothing anywhere creates the parity test. Without it, a server-side phase-union change silently diverges from the package's re-homed copies, the exact failure mode E1-A was rejected for having zero risk of.
- **Fix (applied):** Add a task (B5, where server-contract.ts and the re-homed unions land) creating the app-side type-level parity test (e.g. mutual-assignability assertions between `@civ7/studio-server/contract` unions and the package's re-homed types, in a file under app src/ so `tsc --noEmit` fails on drift).

### [MAJOR] DESIGN.md §6 first bullet (line 153-154) vs design/storybook-sync.md §1 (exact main.ts + rationale)
- **Defect:** The synthesis contradicts the designer report it claims to carry on how the package .storybook resolves the package name: DESIGN mandates the source alias be dev-gated "so the oracle/sync always sees real dist artifacts", while the designer's exact main.ts makes the bare-name→src/index.ts alias UNCONDITIONAL ("renders and HMRs source directly and never depends on a prebuilt dist"), which means the sb-reference (the capture side of the B7 oracle) renders from source, not dist.
- **Evidence:** DESIGN.md §6: "dual-maintained dev-only source alias (app vite.config.ts + package .storybook) — MUST stay dev-gated so the oracle/sync always sees real dist artifacts." storybook-sync.md main.ts comment: "The exact-match alias below points that name at the source barrel so the workbench renders and HMRs source directly and never depends on a prebuilt dist. The converter side is alias-independent." Both statements are about the same alias and cannot both be executed; the choice changes what B2's build-storybook smoke and B7's sb-reference rebuild actually verify (dist render vs source render), and the dev-gated variant additionally requires a working production-mode resolution of the self-referential bare import that no artifact specifies.
- **Fix (applied):** Adjudicate explicitly in DESIGN.md: either adopt the designer's unconditional source alias (and reword §6 — the converter's bundle side still comes from dist via resolveDistEntry, so state that the DIST guarantee applies to package-build.mjs, not the storybook), or spec the dev-gate mechanism plus how build-storybook resolves the package name in production mode.

### [MINOR] openspec/changes/studio-ui-extraction/tasks.md lines 24 and 29 (tasks 3.3, 4.2)
- **Defect:** The per-branch story-count gate numbers are internally inconsistent: B3 moves 12 composites PLUS LeftDock/RightDock (14 stories), so cumulative counts are 30 (B3) and 41 (B4), but the tasks state 28 and 39 — only 5.4's "45" and 6.2's "46" match the actual move sets.
- **Evidence:** 3.1 explicitly moves "…PresetDialogs (3 components), LeftDock, RightDock + their stories" = 14 story files on top of B2's 16 → 30; B4 adds 11 → 41; B5 adds 4 → 45 (matches 5.4); B6 adds 1 → 46 (matches 6.2). 3.3's "(28 stories)" and 4.2's "(39 stories)" are arithmetic carried from a 12-component B3 that ignores the two layout components 3.1 itself lists — a correct execution would 'fail' the stated smoke expectation.
- **Fix (applied):** Correct 3.3 to 30 and 4.2 to 41 (or restate the gates as "all moved stories render" with counts derived from the move set).

### [MINOR] openspec/changes/studio-ui-extraction/tasks.md §1-§2 (lines 8-18)
- **Defect:** Creation of the package .storybook/ (main.ts + preview.tsx) is assigned to no branch: B1's enumeration omits it and B1 is "no component code" (the specified preview imports Toaster/TooltipProvider from the barrel, impossible at B1), yet B2's gate (2.4) requires a package `build-storybook` smoke of 16 stories.
- **Evidence:** tasks.md 1.1 enumerates "package.json …, tsconfig.json, tsup.config.ts, components.json, README stub" — no .storybook; 1.2 covers CSS/fonts/build/test wiring; 2.1-2.3 cover moves and app rewire; 2.4 then invokes "package `build-storybook` smoke (16 stories)". The .storybook exists only in design.md §1's tree and storybook-sync.md's exact file contents; no task instantiates it (nor the Storybook devDeps move noted in structure-rewire §4.8).
- **Fix (applied):** Add "create package .storybook (main.ts/preview.tsx per storybook-sync §1) + SB devDeps" to task 2.2, and move the app's storybook/build-storybook script+devDep removal to an explicit line (B2 or B7).

### [MINOR] openspec/changes/studio-ui-extraction/tasks.md line 10 (task 1.3) + specs/studio-ui-package/spec.md scenario "theme artifact preserves today's rendered semantics"
- **Defect:** The B1 theme-invariant test is specified against "today's `_ds-compiled.css`", which is an untracked build artifact — the comparison is not executable on a clean checkout/CI unless a token-contract snapshot is captured and committed first, a step no task performs.
- **Evidence:** `git ls-files` in apps/mapgen-studio shows no dist/assets/_ds-compiled.css (it is produced by build-inputs.sh into the gitignored dist/); tasks.md 1.3 says "token count parity with today's `_ds-compiled.css`" and spec.md's scenario says the test "compares dist/styles.css against the token contract shipped by today's `_ds-compiled.css`" without saying where that contract lives after build-inputs.sh is deleted at B7.
- **Fix (applied):** Add to 1.3: capture the token contract once (run build-inputs.sh at B1-time, extract the token names/values into a committed fixture in the package test dir) and assert dist/styles.css against the fixture.
