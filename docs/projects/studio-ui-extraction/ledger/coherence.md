# Coherence verdict — whole-ledger judge over the 46-component classification

Judge scope: cross-group consistency over `build-{primitives,composites,forms,panels-layout}.md` + `verify-*.md` (all 8 read in full). Per-row correctness is NOT re-litigated (all four groups verified=true); contested points below were re-adjudicated with fresh greps against `/Users/mateicanavra/Documents/.nosync/DEV/worktrees/wt-studio-ui-extraction/apps/mapgen-studio` @ `c4ebaf1e1`.

## 1. Final tier distribution (46/46 accounted)

| Tier | Count | Components |
|---|---|---|
| clean | **38** | all 16 primitives; all 11 forms; composites: AppBrand, ViewControls, OptionSelect, DisclosureHeader, EmptyState, ErrorBanner, PresetErrorDialog, PresetSaveDialog, PresetConfirmDialog; layout: LeftDock, RightDock |
| moderate | **6** | AppFooter, StageViewTabs, WaterStatsSection (composites); ExplorePanel, GameConsole, RecipePanel (panels) |
| app-shaped | **2** | AppHeader (runtime server-contract reach 2 hops + domain precedence rules in-component → designed prop-contract split); PipelineStage (~980-line contract-typed sibling value graph + contract-typed public prop → designed directory split) |

Note vs the planning memory ("~38 clean / 5-7 moderate / 1 app-shaped"): the ledger lands 38/6/2 — the second app-shaped (AppHeader) is real and verified (verify-composites: "app-shaped grade is earned").

Tier rubric is applied consistently: runtime contract *reach* alone never set the tier (GameConsole/RecipePanel reach the contract at runtime through severable formatters → moderate); *entanglement needing a designed split* did (AppHeader, PipelineStage). No identical pattern received different tiers: all 7 rjsf widgets clean; all 14 cn-consuming shadcn primitives clean+move-with; both status-formatter consumers (GameConsole, RecipePanel) moderate with the SAME corrected remedy; both zero-import dock twins clean; both preset-adjacent directory splits (presets, configOverrides) leave their components clean.

## 2. Cross-group inconsistencies (adjudicated)

1. **Crossing-accounting convention divergence.** Composites tiering treats `src/ui/{types,constants,utils}` as intra-surface (crossings recorded only in shared findings); panels-layout counts the same imports as per-row boundary crossings (ExplorePanel crossings 1-2, RecipePanel crossings 3-4). No tier flips under either convention (re-checked: ExplorePanel/RecipePanel stay moderate on their other crossings; ViewControls/AppFooter stay as graded), but **per-row crossing counts are not comparable across groups** — the shared-kernel decision must be read from §3 below, not from row counts.
2. **The "co-owned by 15+ app files" lump figure must be split per module.** Fresh grep: `ui/types` has **17 app-side importer files** (app/, features/, stores/, ui/hooks, ui/constants, ui/utils/config) + 5 surface components + 2 stories — the composites claim holds for *types*. `ui/constants` has only **2 outside-surface consumers** (`app/hooks/useViewportLayout.ts`, `ui/hooks/useViewState.ts`) — verify-panels C2 correct, composites' lump ~3x overstated for *constants*. `ui/utils` barrel: 3 surface composites + 4 app files (useBrowserRun, useVizSelection, liveSource, useGeneration), and all `config.ts`/`formatting.ts` named consumers are app-side.
3. **useConfigCollapse ownership contradiction — adjudicated: PACKAGE.** build-forms shared finding 2 puts it in the move-with set; panels' RecipePanel row treats it intra-surface; verify-forms' "missed crossing" frames it as "app-side ... NOT in the extraction slice". Grep: its ONLY consumers are rjsfTemplates (type source), itself, and RecipePanel (which moves); zero `data-config-*` queries exist outside configOverrides. It moves with the package; the `data-config-header`/`data-config-pointer` + `configContentId` DOM contract becomes **intra-package** (document it in the package as a versioned string convention anyway — it is queried by string, not by import).
4. **Stale remedies superseded by verify passes — must NOT be executed as originally written:** (a) GameConsole/RecipePanel `features/{mapConfigSave,runInGame}/status.ts` "move-with" → corrected to **split-formatters** (whole-module move relocates app operation-domain constructors consumed by 6 app files into the published package — dependency-direction defect; delete the dead PHASES import/re-export lines, formatters go type-only over the contract); (b) AppFooter seedPolicy "inline" → corrected to **inject-prop** (inline creates a second owner of the Civ7 seed-range policy; 3 app hooks validate the same range).
5. **PipelineStage app-shaped rationale is wrong in verify-panels even though the tier is right.** "No other row requires splitting a directory" is false — `features/presets/` and `features/configOverrides/` splits are required by composites/forms rows whose components stayed clean. The tier holds on different grounds: PipelineStage's OWN value graph (~980 lines of contract-typed siblings executing at render) and its contract-typed public prop, not directory hygiene.
6. **TooltipProvider policy is inconsistent across the surface** (three postures): AppFooter self-provides (AppFooter.tsx:151); AppHeader/ViewControls/WaterStatsSection/ExplorePanel/GameConsole/RecipePanel rely on the ambient decorator; the Tooltip primitive's own story self-provides. Package must pick ONE policy. Recommended: ambient provider (Radix/shadcn convention), documented loudly (absence = silent blank, NOTES.md:25), and strip AppFooter's inline provider (behavior-only at rest; oracle-safe for static capture, verify hover manually).
7. **externalDeps under-reporting is one categorical rule, flagged piecemeal in three verifies** (primitives note, forms C1, panels C3): any `components/ui` barrel import drags the FULL primitive closure at runtime — sonner (toast value re-export, index.ts:13), every @radix-ui package, class-variance-authority, clsx + tailwind-merge (via cn), lucide-react. Fix once at the package boundary: value-free (or curated) barrel + no external toast re-export; then per-component dep lists become honest.
8. **cn: three variants converge on one owner.** Extended cn (`src/lib/utils.ts`, 14 primitives) vs plain cn (`src/ui/utils/cn.ts`, 3 composites) vs manual string-joins (forms home + all 4 panels). Every group independently lands on: package ships ONE extended cn. Render-affecting flips enumerated once: TagSelectWidget active pill (real class-set change — rjsfWidgets.tsx:242); DisclosureHeader/EmptyState/ViewControls plain→extended (expected no-op, verified inputs non-conflicting); panels/forms join normalization (oracle-gated batch).
9. **Duplicate 3-value relation union crosses the split boundary:** `runInGame/status.ts:23 RunInGameActionRelation` ≡ `clientState.ts:41 RunInGameCurrentRelation`. The status split (item 4a) moves formatter code to the package while clientState stays app-side — the type re-home for GameConsole must COLLAPSE the twins (package owns one union; app aliases), not mint a third copy.
10. **Duplicate theme hook resolved consistently:** primitives (Toaster's private `useThemeFromClass`) and panels (PipelineStage's `useResolvedTheme` consumption marked CORRECT) agree — package ships `useResolvedTheme` as the one theme-read API; sonner.tsx consumes it; `test/ui/sonnerTheme.test.tsx` repoints; app consumers (StudioProviders, DeckCanvas) import from the package.
11. **Story-side fixture-type chases are four, not one** (verify-composites correction, generalized): AppFooter.stories + RecipePanel.stories (`@/ui/types`), AppHeader.stories (`@/features/civ7Setup/setupConfig`), WaterStatsSection.stories (`@/features/viz/riverLakeInspector` ×4). All four must follow the type re-homes; plus every story's `@` alias must be recreated or rewritten in the package Storybook.
12. **Unclaimed module — the Storybook preview stack.** `.storybook/preview.tsx` uses `src/storybook/storeReset.ts` (imports `stores/viewStore` — app) and `queryStub.ts`; no slice assigned ownership. Adjudication: the package preview needs only TooltipProvider + Toaster (all 46 components are props-driven post-extraction; no store/query to stub); storeReset + queryStub stay app-side (or retire with the old preview). `mockWidgetProps.tsx` + `recipeDagFixture.ts` move with the package Storybook.
13. **CSS-pipeline requirements recorded on some rows only — categorical:** the package must (a) re-declare `tw-animate-css` as a real dependency (devDeps-only today, package.json:189), (b) ship the `@theme inline` token block + tw-animate import + the unlayered globals (`.custom-scrollbar`, native select/input resets — used by all 4 panels), and (c) scan story files AND sonner.tsx in its Tailwind content config (arbitrary-variant classnames + Demo scaffolding). Pin `class-variance-authority`/`clsx`/`tailwind-merge` (all "latest") before publishing.

## 3. Shared-kernel ownership ledger (package vs app; every module claimed by ≥1 row's remedy)

| Module | Owner | Basis |
|---|---|---|
| `src/lib/utils.ts` (extended cn) | **package** | zero consumers outside components/ui (grep-verified); the ONE cn; app copy deleted |
| `src/ui/utils/cn.ts` (plain cn) | **delete** | unify 3 consumers onto extended cn; render-adjacent, oracle-verify |
| `src/ui/utils/` config.ts + formatting.ts + barrel | **app** | all named consumers app-side (StudioShell, useBrowserRun, useVizSelection, liveSource, useGeneration); package stops importing through this barrel |
| `src/ui/types` | **package** | prop contracts of 5 surface components; 17 app importers repoint |
| `src/ui/constants` | **split** | LAYOUT geometry (incl. FOOTER_HEIGHT source) → package; options.ts/defaults.ts (Civ7 engine ids) → app — pending escalation E2 |
| `src/ui/hooks/useResolvedTheme.ts` | **package** | the theme-read API; sonner unifies onto it; StudioProviders/DeckCanvas repoint |
| `src/ui/hooks/` useTheme/useViewState/useGeneration | **app** | app orchestration; in no component graph |
| `features/civ7Setup/seedPolicy.ts` | **app** | AppFooter inject-prop; 3 app hooks co-own the policy |
| `features/civ7Setup/setupConfig.ts` | **app** | AppHeader inject-prop redesign; type re-home per E1 |
| `features/mapConfigSave/status.ts` | **split** | pure formatter → package (type-only after deleting dead PHASES re-export); constructors stay app |
| `features/runInGame/status.ts` | **split** | same; collapse the relation-union twin during the split |
| `features/runInGame/clientState.ts` | **app** | 3-value union re-homes (collapsed with RunInGameActionRelation) |
| `features/viz/riverLakeInspector.ts` | **app** | narrow structural summary/layer-ref types re-home into package |
| `features/presets/` | **split** | PresetDialogs.tsx → package; dialogState/usePresets/storage/importFlow → app |
| `features/configOverrides/` | **split** | everything → package (incl. SchemaForm, pathUtils, schemaPresentation, typeboxRjsfValidator+test, useConfigCollapse) EXCEPT configBuilders.ts → app |
| `features/recipeDag/` | **split** | PipelineStage + layout + domainPresentation + artifactPresentation → package; useRecipeDagQuery + prunePipelineExpansion → app; RecipeDagLoadStatus re-homes to view |
| `stores/viewStore.ts` | **app** | StageView union re-homes to package; store imports it back |
| `src/components/ui/` + barrel | **package** | drop `toast` value re-export (useToast.ts repoints to sonner); all 15 primitive files |
| `src/ui/components/fields/` (FieldRow) | **package** | one package barrel dissolves the ds-entry collision; forms templates keep consuming |
| `src/index.css` tokens + tw-animate + component globals | **split** | package CSS entry becomes the ONE owner of tokens/@theme + tw-animate + `.custom-scrollbar`/select resets; app imports package CSS and keeps app-only globals |
| `src/storybook/` mockWidgetProps + recipeDagFixture | **package** | story fixtures of the moving corpus |
| `src/storybook/` storeReset + queryStub | **app** | preview stack for app-coupled stories; package preview = TooltipProvider + Toaster only |
| `components.json` | **app** | package adds its OWN generator config (or documented no-generator policy) or the `@/lib/utils` crossing re-grows |
| `.design-sync/ds-entry.tsx` | **delete** | extraction retires it; package barrel is the export surface |

No module ends up claimed by both sides after adjudication; the two formerly ambiguous ones (useConfigCollapse — §2.3; storybook preview helpers — §2.12) are resolved above.

## 4. Escalations (decisions only Matei can make)

- **E1 — Published `.d.ts` policy for `@civ7/studio-server` contract types.** One decision, three sites: GameConsole/RecipePanel (`MapConfigSaveDeployStatus`, `RunInGameOperationStatus`) and PipelineStage (`RecipeDagResult`). Types-only peer dependency (package .d.ts references the server workspace package — dependency-direction smell per the directive, but zero drift risk) vs re-homing structural types (clean direction; trivial for the status/phase unions, HEAVY for the DAG shape — re-homing a shape the server owns creates reverse drift risk, verify-panels).
- **E2 — Civ7 domain data in the published package.** `ui/constants/options.ts`/`defaults.ts` embed engine ids (MAPSIZE_*, player counts) and AppFooter/AppHeader hardcode-import them. Ship domain data in the package (defect per directive, zero API churn) vs options-via-props (clean, but public API change → story args churn against the oracle for 2+ components).
- **E3 — The oracle-gated visual-cleanup batch: with the move or deferred?** Every render-affecting cleanup is flagged (ErrorBanner text-xs→text-data 1px; AppBrand placeholder-content propification; positioning-as-chrome lift for AppFooter/AppHeader/StageViewTabs/ErrorBanner; ExplorePanel native selects→Radix; TagSelectWidget cn flip; React 19 forwardRef sweep). Recommended: verbatim move first (changed:[] fidelity proof), one designed cleanup wave after — but batching is a fidelity-risk tradeoff only Matei holds.
- **E4 — AppHeader is the one component that cannot move verbatim.** The inject-prop redesign changes its public API (intent callbacks replace setupConfig update helpers), so the "changed:[] proves fidelity" shortcut is unavailable. Approve the designed prop-contract split for v1, or defer AppHeader from the package's first release and extract 45.
