# FRAME §2 DoD walk — close-out evidence (2026-07-02)

Run: 8 refute-biased clause walkers + 8 independent verifiers + 1 completeness critic (all Opus), read-only against `studio-ui-cleanup` tip `e73877294`. Each walker was instructed to REFUTE its clause; each verifier spot-checked cited sources and tried one fresh refutation angle. Full structured result lives in the session workflow journal (wf_b83386a4-04a); this file is the durable distillation.

**Verdicts: 7 clauses MET (all upheld by independent verifiers), 1 PARTIAL by design** — `project-resync`, whose only open half is the deliberately Matei-gated 7.4 upload (tasks.md 7.4). No verifier overturned any walker verdict.

An **independent Codex second pass** (tasks §9.1) ran in parallel over the full stack diff (`packages/mapgen-studio-ui/src+test`, `packages/studio-contract`, `apps/mapgen-studio/src+test`), with deeper passes on GameConsole, React 19 ref/memo changes, package exports/d.ts, AppHeader/setup controls, RJSF widgets/templates, dialogs, statusLabels, theming, and app import wiring: **no findings**.

## Per-clause record

### package-real-build — **MET** (upheld)

> The design-synced component surface and the token/theme layer live in one standalone workspace package with a real build: dist/, accurate generated .d.ts (the TS7056 tolerance is gone), an exports map, a CSS/token entry.

The package-real-build clause holds on every sub-claim: the standalone @swooper/mapgen-studio-ui package ships a real dist/ (index.js with 100+ exports, compiled styles.css, fonts, and a strict-tsc dist/types/ tree with zero TS7056 suppression and zero `: any` in the barrel), an exports map whose six targets all resolve to real files, and a token/theme entry (theme.css) genuinely consumed by both the app and the package Storybook — with no design-synced component surface or token definition left app-side. Refutation failed: every angle (app-side surface, TS7056/dts workaround, lying exports entry, unused CSS entry) was defeated by the tree and the recorded gate evidence, and the only open item (7.4 upload) is a deliberately-gated part of a different DoD clause.

Declared gaps: (1) Task 7.4 (upload of the re-synced design project to 531d158d) is deliberately BLOCKED on Matei's explicit go-ahead (tasks.md:58) — but this belongs to the design-sync half of the DoD, NOT the package-real-build clause under audit; it does not affect any of dist/, .d.ts, exports map, or CSS/token entry. (2) Gate results are cited from recorded evidence (verify.mjs contents, commit/PR records, WORKSTREAM line 3) rather than re-run, per the read-only mandate; the dist/ artifacts inspected are the committed build outputs in the tip worktree, which I confirmed exist and match the exports map by direct stat/find.

### app-identical — **MET** (upheld)

> apps/mapgen-studio imports the package; the app renders and behaves identically, gated by the existing verification machinery (FRAME §6), not by assertion.

The "app-identical" clause holds on both halves: apps/mapgen-studio consumes the extracted surface only through the @swooper/mapgen-studio-ui package barrel (no local bypass, extracted homes deleted, no app-local redefinitions), and every identity claim is backed by a live gate — the 88/88 post-fix design-sync grade oracle, the byte-exact AppHeader markup pin, the recorded vitest known-failure set + dev-serve smoke, and the verify.mjs export/import-discipline floor — not by prose. The one real threat (grade-carry over moved renders) was caught in B8 review and closed with a --force full re-grade; the only open task is the outward DesignSync upload (7.4), deliberately reserved for Matei and orthogonal to app-identity.

Declared gaps: (1) Grade sheet mtimes read 2026-07-02 14:59:22 — ~19 min BEFORE the e73877294 record commit at 15:18:46. This is consistent (the resync driver wrote the gitignored sheets during the run; the tracked NOTES/record commit was authored after), and the sheet notes + 88/88 'match' are the freshness proof, but the mtime<committime ordering rests on that reading of the workflow rather than a re-executed gate (gates are recorded-evidence-only per audit rules — I did not and could not re-run the driver). (2) The vitest '7 known failures' and 'dev-serve 200/200' are read from the B8 PR body and WORKSTREAM record, not re-executed here; the audit is scoped to recorded evidence, so this is expected, not a defect. The 4 portal dialogs and the Toaster-blank/TagSelectWidget-crop false positives ride the recorded manual full-page path (declared allowed noise), so the oracle's 'match' for those 6-ish pairs is manual-verification-backed rather than automated-screenshot-backed — by design per FRAME §3, not a bypass of the clause.

### sync-real-artifacts — **MET** (upheld)

> The design sync builds from the package's real artifacts. build-inputs.sh, tsconfig.dts.json, and the rest of the reverse-engineering scaffolding are deleted — not bypassed, not kept as fallback.

The refutation fails: every artifact the clause names as reverse-engineering scaffolding — build-inputs.sh, tsconfig.dts.json, ds-entry.tsx, the synthEntry/source-storybook fork, and the app .storybook stubs — lived under apps/mapgen-studio/.design-sync/ and was deleted (not bypassed, not kept as fallback) in B7 (3a71bf94f), with the sync config repointed to real built artifacts (dist/index.js, dist/styles.css, buildCmd=nx build). The one same-named survivor, packages/mapgen-studio-ui/tsconfig.dts.json, is a different file at a different path: the package's forward build config added at B1, invoked by the real build script and failing on any type error — the antithesis of the retired tolerant reverse-engineering emit.

Declared gaps: (1) Deliberately gated, not a refutation of this clause: tasks.md 7.4 (the upload to project 531d158d) is unticked [ ] and BLOCKED on Matei's explicit go-ahead. That gates whether the real-artifact bundle has been PUSHED to Claude Design, not whether the sync BUILDS from real artifacts or whether the scaffolding was deleted — both of which are complete. The sync-real-artifacts clause is about the build inputs and the deletion, and those are met. (2) Not independently re-executed (per read-only mandate): the recorded B7 oracle (resync verdict changed:46/added:0/removed:0, anchor ok, 46/46 grades, forced-.light 7/7 zero drift) proving the repointed sync actually produces equivalent output from the real dist was cited from the WORKSTREAM/PR record, not re-run here.

### project-resync — **PARTIAL** (upheld)

> Design project 531d158d is re-synced from the new shape and all 46 components re-verify — the four portal dialogs via the manual-verification path used for PR #1992, not by forcing recapture. The design agent's explorations/ and scraps/ survive untouched.

Refutation failed on every part that is not deliberately gated: the local re-verify is real (B7 resync verdict changed:46/added:0/removed:0 with anchor ok, recorded in PR #2004 and in the committed .sync-diff.json; all 46 grade sheets on disk show 88/88 story-verdicts 'match'; the B8 grade-carry gap was caught and closed by a full --force re-grade in the tip commit; the portal-4 carry the manual PR#1992 note and only those four; zero B8 story-file diffs keep the sync surface byte-frozen; the forced-.light canary passed 7/7 zero-drift), and the upload plan verifiably preserves explorations/scraps by construction (deletePaths verbatim from .sync-diff.json = [], anchor-invisible non-components paths, fresh-fetch _ds_sync.json + driver re-run mandated). The verdict is partial only because the actual upload (task 7.4) is intentionally unticked and blocked on Matei's go-ahead — the single open half, exactly as the frame anticipated.

Verifier notes: Minor citation-wording imprecision (does not change the verdict): the walker's evidence item #4 calls `.sync-diff.json` 'The committed .sync-diff.json artifact,' but `git check-ignore` returns exit 0 and `git ls-files --error-unmatch` confirms it is gitignored and NOT tracked in git. It is a real, on-disk artifact whose verdict fields I confirmed directly (anchorReason:ok, changed:46, added:[], removed:[], deletePaths:[]), but 'committed' is inaccurate. The walker itself already disclosed this in its declared gaps ('.sync-diff.json are gitignored... not tracked in git history'), so this is a local contradiction with its own gap statement, not a concealed fact, and it does not affect the on-disk-verified verdict.

Declared gaps: (1) THE UPLOAD ITSELF IS UNDONE (deliberately gated): the actual push of the 46 re-keyed cards + bundle + _ds_sync.json to project 531d158d has not happened — task 7.4 is the sole unticked box, blocked on Matei's explicit go-ahead. So 'is re-synced' in the present tense is not literally true of the remote project; only the local re-verify + the fully-specified atomic upload plan exist. This is the expected open half per the audit framing, not a defect. (2) The local re-verify anchor was a STALE local sidecar (2026-06-30 byte-copy), not a live fetch — because no DesignSync tool ran in the executor session. The record correctly scopes this as an upload-session precondition (fresh-fetch _ds_sync.json + re-run driver) and it does NOT undermine the changed:46 verdict for the current surface, but it means the deletePaths=[] preservation guarantee is only proven against the 2026-06-30 anchor; the live-anchor re-run at upload time is unexecuted (by design, since it needs the tool + Matei). (3) The grade sheets (.cache/compare/*.grade.json) and .sync-diff.json are gitignored, so a fresh clone cannot reproduce the 88/88-match evidence without re-running compare.mjs --force. Durability rests on the re-runnable --force + the NOTES.md mechanism bullet + cross-machine carry from the uploaded project. I verified the artifacts in THIS worktree directly; they are real and consistent, but not tracked in git history.

### theme-single-source — **MET** (upheld)

> The theme layer is single-sourced: one token entry consumed by the app, Storybook, and the sync, with the light/dark :root inversion owned as a deliberate, tested design decision instead of manufactured by text surgery.

The theme layer is genuinely single-sourced: packages/mapgen-studio-ui/src/styles/theme.css is the sole authored palette (one `:root,.dark` dark-default block + one `.light` re-skin), consumed identically by the app (index.css @import), Storybook (preview.tsx via index.css), and the sync (dist/styles.css compiled from index.css) — the app's own build carries the exact same token values, and the retired text-surgery (build-inputs.sh, tsconfig.dts.json) is deleted from the tree, not bypassed. The light/dark inversion is owned as a deliberate tested decision by themeTokens.test.ts (token-for-token fixture pin, the assert_theme_block replacement) plus the light-canary render gate (7/7 zero drift), so no second token source, no surviving CSS-rewrite mechanism, and no absent inversion test could be found — refutation fails and the clause is met (with only the deliberately Matei-gated upload, task 7.4, still open downstream).

Declared gaps: (1) Task 7.4 (upload the re-synced shape to design project 531d158d) is deliberately BLOCKED on Matei's explicit go-ahead (tasks.md:58, unticked). This is downstream of single-sourcing, not part of it: the clause is about the token layer having one owner consumed by app/Storybook/sync, which is fully satisfied in the tree. The clause is met independent of whether the artifact has yet been uploaded. (2) themeTokens.test.ts and light-canary.mjs read build products (dist/styles.css, sb-reference/ds-bundle) rather than theme.css directly, so their green status depends on a fresh build having run. This is by design (they pin the artifact the sync actually ships) and the transitive chain dist←index.css←theme.css was verified in-tree; I relied on recorded gate evidence (B1 commit 60f209fc8, B8 canary 7/7) rather than re-running the gates, per the read-only mandate.

### ci-runnable — **MET** (upheld)

> The sync is CI-runnable from the package build — in this repo an Nx target (FRAME §8 covers the project.json collision).

The DoD clause claims the sync is CI-runnable from the package build via an Nx target that composes with (rather than races) the project.json collision; every leg holds against the recorded evidence. The refutation fails: the target is real and builds dist via the Nx dependsOn edge, the collision is resolved by keeping the target inline under the package.json nx key (no project.json for either project), and the only qualifications (deliberate exclusion from the CI five-target graph plus the anchor/chromium prerequisites) are recorded as intentional, not hidden defects.

Declared gaps: (1) CI-graph membership vs CI-runnability: the clause says 'CI-runnable', and the target IS a single Nx-invocable command proven green cold — but it is deliberately NOT in the repo's actual CI graph (the five-target run-many). If an auditor reads 'CI-runnable' as 'actually executes in CI on every PR', that stronger reading is not met (by design, for chromium-weight reasons recorded in NOTES.md/tasks.md 7.2b). Under the FRAME author's own scoping ('deliberately outside the CI five'), the weaker property-level reading is met. (2) The steady-state green (anchored) run depends on remote-sync.json, which is gitignored and produced by a DesignSync get_file step outside nx; a fresh CI clone with no anchor runs unanchored (full first-sync scope) — still a valid mechanical check per the script's own comment, but not the anchored-diff path. Both the Chromium and the anchor prerequisites are external to the Nx target itself. (3) The oracle's fidelity grades (the 46 grade.json sheets) are gitignored and never tracked; cross-machine/CI carry derives from the uploaded design project, so a clean CI runner would have no committed grades to certify against — the automated target verifies the mechanical stages, and the human grade-carry lives outside the tracked tree (recorded in the tip commit body).

### both-modes-gate — **MET** (upheld)

> The fidelity gate can exercise both theme modes (a light-render canary), or the team has recorded a deliberate decision not to add one — either way the dark-only-gate blindspot stops being a silent failure mode.

The refutation fails on all three angles: the light canary is a genuinely committed capability (scripts/light-canary.mjs + light-canary-tokens.json, both tracked and added in B7 09c28536a), and its recorded run demonstrably forced light on both the reference and converter sides (all 7 picks sbClass=light, authentic light --background 240 20% 97% distinct from dark 240 14% 6%, 7/7 zero drift). The blindspot no longer fails silently because the honest dark-only scope of design-sync:check and the canary's role as the both-modes leg are written into NOTES.md:198-205 and enumerated in the re-sync ledger (tasks.md 7.2), satisfying the clause's explicit OR which names a light-render canary as sufficient.

Declared gaps: (1) The canary is a MANUAL capability — it is NOT wired into the automated design-sync:check gate (grep confirms no reference in package.json/verify.mjs; design-sync:check itself 'renders dark-only' per NOTES.md:205). So a future re-sync that skips the manual step would not auto-fail. This does not open the clause: the clause is an explicit OR that names 'a light-render canary' as satisfying (not 'an auto-wired one'), and its bar is the SILENT failure mode — which is now removed because the capability is committed, a run is durably recorded (tokens.json 7/7 zero drift), the honest dark-only scope of design-sync:check is written down (NOTES.md:205), and running the canary is an enumerated step in the re-sync ledger (tasks.md 7.2). The blindspot is documented and instrumented, not silent. (2) The canary tests light-mode FIDELITY (reference-light vs converter-light), not dark-vs-light of the same component on one gate pass. That is exactly what the clause asks for ('a light-render canary' to close the dark-only gate), so it is not a shortfall against the clause — noted only for precision.

### incrementality — **MET** (upheld)

> Incrementality has improved or has a designed path: one shared bundle + one shared styleSha de-incrementalizes all 46 components on any shared CSS/dep change, which makes every re-sync expensive.

The refutation fails: the clause is a disjunction ("improved OR has a designed path / deliberate recorded decision"), and the workstream landed both a deliberate recorded decision (the Q10 reframe: the frame's "de-incrementalizes all 46 / every re-sync expensive" premise is false for grading — styleSha partitions uploads only, grades/captures carry — adjudicated at the Matei checkpoint) and a real designed path (bare-name story imports making gradeKeys story-source-keyed so grades carry across future internal refactors, plus resync.mjs's skip/sample/full render-check tiering documented as the path). The literal "one shared bundle + one shared styleSha" structure was NOT changed — no bundle partition or per-group styleSha was built — but that was the investigated-and-declined half, not a dropped requirement; and the B8 grade-carry lesson shows the "grades carry" improvement is honest only when paired with the mandatory --force re-grade for internals-editing waves.

Declared gaps: (1) The clause's literal example ('one shared bundle + one shared styleSha de-incrementalizes all 46 … makes every re-sync expensive') was answered by REFRAMING it as false-for-grading rather than by the mechanical improvement the example gestures at (bundle partition / per-group styleSha). A strict reading that demands the shared-bundle/styleSha structure itself change would score this 'not_met on the literal, met on the disjunction' — but the clause explicitly offers 'improved OR has a designed path', and 'has a designed path' plus 'deliberate recorded decision' are both satisfied, so the DoD as written is met. (2) The 'grades carry → cheaper re-syncs' improvement is real but CONDITIONAL and hazard-bearing: it is only honest when a component-internals-editing wave force-re-grades (the B8 lesson). The cheapness is not unconditional; NOTES.md now encodes the mandatory --force discipline as the guardrail. This is a caveat on how much 'improved' means, not a failure of the clause. (3) Task 7.4 (the live upload) is deliberately BLOCKED on Matei's go-ahead, so the reframed incrementality claim ('post-extraction syncs stay incremental via the surviving anchor') has been proven locally (changed:46/added:0/removed:0, anchor ok) but not yet exercised through a second real upload round-trip. This is a deliberately-gated leg, not an open defect.

## Completeness critic — three close-out gaps, all dispositioned

Critic assessment (verbatim): The 8-clause DoD walk is substantively sound: every FRAME §2 clause is genuinely met/partial, the 7.4 upload gate is correctly identified as the one KNOWN-expected open thread, and the walk's evidence chains (verify.mjs floor=100, dist artifacts, the B8 grade-carry force-re-grade fix, the light canary 7/7, the deletion of build-inputs.sh/tsconfig.dts.json/ds-entry.tsx) hold against the tree and the recorded gates. I found NO clause that is actually unmet or read so narrowly that its verdict flips. The three missing items are close-out completeness gaps, not clause failures.\n\nThe sharpest is item 1: task 9.1 mandates an 'independent Codex second pass on substantial diffs' and it was never run — the box is unticked and no artifact anywhere references it. Because the walk scopes itself to §2 DoD clauses, this self-imposed review-rigor commitment slid entirely past it. It is the one gap that touches whether the identity/correctness claims were checked with the independence the workstream itself promised, so before honest close it must be run or explicitly dropped by decision.\n\nItems 2 and 3 are FRAME-lever/task-ledger gaps the §2 walk structurally cannot catch: three FRAME §9 falsifiers (most importantly 'sync slower/less verified post-extraction') carry no verdict in the live §8 tally, and task 9.2's runner move-up half is unaddressed. Neither reverses a DoD clause; both are 'stamp the verdict / list the action' close-out hygiene. None of the three touches the 7.4 upload gate (excluded per instructions). If these three are closed — Codex pass run-or-waived, §9 falsifiers stamped, runner move-up scheduled — the workstream can honestly close on everything except the deliberately Matei-gated upload.

Dispositions:

1. **Codex second pass** — ran this same close-out session (in parallel with the walk, so the critic could not see it): full stack diff, no findings, scope recorded above. tasks §9.1 ticked on that evidence.
2. **FRAME §9 falsifiers unstamped** — the three open falsifiers (re-verify drift, wrapper/shim accretion, sync slower/less verified) are now stamped with verdicts in WORKSTREAM.md §8, citing B7 anchor verdict, the dist import-graph audit, and the light-canary/NOTES.md ritual.
3. **Runner move-up (tasks §9.2 second half)** — done this session: runner worktree detached at `e73877294`, deps installed, studio recipes rebuilt via Nx, relaunched with `restart-mapgen-studio.sh --no-build`; frontend 5173 answered 200 and the daemon RPC responded live on 5174.
