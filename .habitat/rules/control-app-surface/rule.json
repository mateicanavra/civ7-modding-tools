{
  "id": "control-app-surface",
  "ownerTool": "pattern-check",
  "ownerProject": "@internal/habitat-harness",
  "lane": "enforced",
  "scope": "apps/**/*.ts, apps/**/*.tsx, packages/**/*.ts, packages/**/*.tsx",
  "forbids": "caller-local Civ7DirectControlSession construction outside sanctioned session owner files and tests",
  "why": "Studio and tooling code use canonical game-wire doors; direct-control session lifecycle belongs to @civ7/direct-control and the Studio server session service.",
  "detect": [
    "habitat",
    "check",
    "--tool",
    "pattern-check"
  ],
  "remediate": null,
  "message": "Use Civ7TunerSession or withCiv7DirectControlSession instead of constructing Civ7DirectControlSession locally.",
  "exceptionPath": "none",
  "patternName": "control_app_surface",
  "hookCheck": true,
  "scanRoots": [
    "packages",
    "apps/mapgen-studio/src",
    "mods/mod-swooper-maps/src/recipes",
    "mods/mod-swooper-maps/src/maps",
    "mods/mod-swooper-maps/src/domain"
  ],
  "pathCoverage": [
    {
      "kind": "exact-path",
      "patterns": [
        "apps/**/*.ts",
        "apps/**/*.tsx",
        "packages/**/*.ts",
        "packages/**/*.tsx"
      ]
    }
  ]
}
