{
  "id": "arch-test-cutover",
  "ownerTool": "target-check",
  "ownerProject": "mod-swooper-maps",
  "lane": "enforced",
  "scope": "mods/mod-swooper-maps (4 cutover scans)",
  "forbids": "op-calls-op in tectonics, dual contract paths, shim surfaces, foundation topology drift",
  "why": "M-cutover structural invariants (coarse wrap of test:architecture-cutover).",
  "detect": [
    "target-check",
    "run",
    "mod-swooper-maps:test:architecture-cutover",
    "--outputStyle=static"
  ],
  "remediate": null,
  "message": "See the failing cutover test output for the violated invariant.",
  "exceptionPath": "none",
  "pathCoverage": [
    {
      "kind": "exact-path",
      "patterns": ["mods/mod-swooper-maps"]
    }
  ],
  "graphTarget": {
    "project": "mod-swooper-maps",
    "target": "test:architecture-cutover"
  }
}
