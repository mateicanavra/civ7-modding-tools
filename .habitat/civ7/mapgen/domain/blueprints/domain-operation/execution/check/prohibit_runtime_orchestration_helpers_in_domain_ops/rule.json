{
  "ownerProject": "mod-swooper-maps",
  "lane": "enforced",
  "forbids": "domain op runtime entrypoints calling ops.bind or runValidated",
  "why": "Domain ops are atomic runtime units; orchestration belongs in step/stage composition, not inside op entrypoints.",
  "remediate": null,
  "message": "Domain op runtime entrypoints must not call ops.bind or runValidated.",
  "hookCheck": true,
  "scanRoots": ["mods/mod-swooper-maps/src/domain"],
  "pathCoverage": [
    {
      "kind": "exact-path",
      "patterns": ["mods/mod-swooper-maps/src/domain/**/ops/*/index.ts"]
    }
  ]
}
