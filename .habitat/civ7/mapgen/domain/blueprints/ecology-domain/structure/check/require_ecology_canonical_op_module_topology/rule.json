{
  "ownerProject": "mod-swooper-maps",
  "lane": "enforced",
  "forbids": "ecology op modules missing canonical topology direct children",
  "why": "Ecology is the canonical exemplar for decomposed op module topology; support directories, schema quality, and JSDoc quality remain outside structure-check.",
  "remediate": "Restore contract.ts, index.ts, strategies/index.ts, and rules/index.ts where a rules directory exists for each ecology op module.",
  "message": "An ecology op module is missing canonical topology.",
  "pathCoverage": [
    {
      "kind": "exact-path",
      "patterns": ["mods/mod-swooper-maps/src/domain/ecology/ops/*"]
    }
  ]
}
