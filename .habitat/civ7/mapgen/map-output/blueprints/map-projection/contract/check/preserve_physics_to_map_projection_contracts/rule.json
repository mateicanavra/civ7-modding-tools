{
  "ownerProject": "mod-swooper-maps",
  "lane": "enforced",
  "forbids": "physics contracts requiring map artifacts/effects, realized-map artifact tags, or non-canonical map effect names",
  "why": "The physics-to-map projection seam is a contract boundary, not package-local behavior.",
  "remediate": null,
  "message": "Keep map projection dependencies on the map side of the contract seam.",
  "pathCoverage": [
    {
      "kind": "exact-path",
      "patterns": [
        "mods/mod-swooper-maps/src/recipes/standard/stages/**/*.contract.ts",
        "mods/mod-swooper-maps/src/**/*.ts",
        "packages/mapgen-core/src/**/*.ts"
      ]
    }
  ]
}
