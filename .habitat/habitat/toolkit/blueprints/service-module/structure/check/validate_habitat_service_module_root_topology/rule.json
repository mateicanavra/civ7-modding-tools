{
  "ownerProject": "habitat",
  "lane": "enforced",
  "forbids": "service module and service-model root children outside the admitted topology",
  "why": "The Habitat Toolkit service layer should keep a predictable root file tree before deeper suffix validation runs.",
  "remediate": "Move service files into the admitted module/model root tree.",
  "message": "Habitat service module root topology validation failed.",
  "pathCoverage": [
    {
      "kind": "exact-path",
      "patterns": ["tools/habitat/src/service/modules/*", "tools/habitat/src/service/model/*"]
    },
    {
      "kind": "project-owner"
    }
  ]
}
