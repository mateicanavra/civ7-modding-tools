{
  "$comment": "Root registry metadata for Habitat rule discovery. Habitat discovers .habitat/**/rule.json manifests. Each manifest owns stable rule identity, current placement inventory facts, operation kind, explicit runner file references, and support file references. Physical location is not rule identity. ownerProject = the workspace project whose check:policy target runs this rule. lane: enforced rules fail `habitat check`; advisory rules report only.",
  "schemaVersion": 2,
  "ownerRoots": {
    "control-orpc": "packages/civ7-control-orpc",
    "civ7-adapter": "packages/civ7-adapter",
    "civ7-map-policy": "packages/civ7-map-policy",
    "civ7-docs": "apps/docs",
    "habitat": "tools/habitat",
    "civ7-sdk": "packages/sdk",
    "mapgen-core": "packages/mapgen-core",
    "mapgen-studio": "apps/mapgen-studio",
    "mod-intelligence-bridge": "mods/mod-civ7-intelligence-bridge",
    "mod-swooper-maps": "mods/mod-swooper-maps"
  }
}
