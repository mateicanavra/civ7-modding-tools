{
  "ownerTool": "command-check",
  "ownerProject": "civ7-docs",
  "lane": "enforced",
  "scope": "apps/docs/docs.json",
  "forbids": "docs site configuration without a structured navigation object",
  "why": "The docs app must keep the Mintlify navigation input semantically structured.",
  "detect": [
    "bun",
    ".habitat/docs/blueprints/docs-site/contract/check/validate_docs_site_config_inputs/check.ts"
  ],
  "remediate": "Restore a docs.json name and navigation object. Root file presence is owned by require_docs_site_root_inputs.",
  "message": "Docs site configuration has invalid navigation shape.",
  "pathCoverage": [
    {
      "kind": "exact-path",
      "patterns": [
        "apps/docs/docs.json"
      ]
    },
    {
      "kind": "project-owner"
    }
  ]
}
