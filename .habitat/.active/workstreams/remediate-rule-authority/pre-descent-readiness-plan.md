# Pre-Descent Readiness Plan

Status: split boundary sealed; no A.2 execution authorized

Owner: DRA Habitat authority-tree workstream steward

Purpose: define the finite prerequisites for opening domain-operation descent
work from current repository authority. This is a readiness gate and a compact
historical disposition, not a second work tracker.

## Authority Order

1. Current code and Habitat authority own behavior, capabilities,
   `RuleDiagnostics`, `RuleFixPreview`, Nx output ownership, and all
   non-descent concerns.
2. The readiness corpus owns descent intent, workspace semantics, and the
   frame -> ledger -> decisions -> execution -> receipts -> ascent model.
3. The later D1-D4 rulings in the selected descent own its exact target.
4. Earlier candidate refinements are evidence only.

When these sources disagree, apply this order rather than reconciling prose by
accumulation.

## Current Readiness State

| Slice | Current disposition | Split-boundary condition |
| --- | --- | --- |
| R1 helper-surface consolidation | parked, non-gating | no action required |
| R2 config-facade consolidation | adopted on current authority | generic rule covers root and domain config facades; op-local config stays admitted; Foundation-specific proxy removed after survivor proof |
| R3 aggregate runnability | behaviorally superseded by current architecture plus bounded determinism and cache-input repairs | focused provider tests pass; repeated local execution has identical normalized semantic output; one native Nx graph owns all ten owner-local batches and both exact graph leaves with truthful task exit |
| R4 stale-blocker refresh | adopted on the active cleanup ledger | zero operation children without `contract.ts`; stale support-pocket blocker removed |
| R5 workspace shape | adopted here | roadmap, frame, ledger, and separate D1-D4 records are present; later phase homes remain first-use |
| R6 current-tree reconciliation | refreshed from the current source tree | `ledger.md` carries the current counts and identifies historical artifacts rather than treating them as red |

Current prerequisites also include:

- one immutable rule-catalog snapshot and stable `RuleDiagnostics` demand and
  result boundary;
- `RuleFixPreview`, with Habitat-owned admission and vendor-neutral file
  impacts while all writes remain refused;
- Nx-owned ordering and deduplication for output-materializing targets;
- 121 live rule manifests with exact active cleanup-ledger id parity. The
  unqualified Habitat command owns 119 local-runner rules; the two `runner:nx`
  manifests resolve to `habitat:check:hygiene` and `habitat:check:boundaries`
  through the native Nx graph.

These capabilities are foundations for the split. They are not reopened by
the descent.

## Finite Readiness Disposition

This table is the only readiness-commit disposition needed. Prefixes identify
the historical readiness commits; they are not ancestry instructions.

| Readiness commit | Disposition |
| --- | --- |
| `ac734`, `bae`, `dbcef`, `e0de` | semantic documents adopted and intentionally adapted |
| `8fb` | active R4 ledger correction adopted |
| `004` | behavior re-proved; historical implementation not adopted; the one live nondeterministic provider detail received a bounded current-architecture repair |
| `3cf` | R2 semantics adopted on current authority |
| `61d` | parked/non-gating disposition retained without historical receipt churn |
| `1e38` | census re-run; historical receipt not adopted |
| `396` | old Grit implementation and `.ds-sync` content excluded |
| `f325` overlap ledger | historical/reference-only unless review proves a still-live unresolved fact |

No other readiness ancestry or receipt is required to begin from the current
split base.

## R2 Survivor Contract

The surviving authority is
`.habitat/blueprints/domain-operation/prohibit_root_config_facade_imports_in_domain_ops/`.
It rejects both root and domain-specific config facade imports from operation
source while preserving operation-local config modules. The retired
Foundation-only rule contributes no independent concern.

Proof at the split boundary:

- focused bad samples fail for both facade import forms;
- a clean operation-local config sample passes;
- the retired operation rule id is absent and domain-operation source has no
  `FoundationConfigSchema` residue; the recipe-step rule that still names the
  token is an exterior authority;
- live manifest ids equal active cleanup-ledger ids exactly.

## R3 Current-Architecture Proof

The old aggregate-concurrency repair is unnecessary. The current provider and
service supply:

- bounded selected-rule work;
- immutable root planning;
- timeouts and distinct acquisition, parse-failed, incomplete, and complete
  states;
- closed TypeBox decoding and exhaustive state dispatch;
- one terminal result per selected rule in deterministic order;
- report-derived command status and truthful CLI exit.

Normal owning-worktree execution found two bounded determinism gaps. Pinned Grit
could emit multiple relevant `AnalysisLog` events in different orders, and
complete findings could arrive in a different event order. The current repair
canonically selects one relevant failure and orders public findings without
discarding evidence. The focused provider suite passes, and repeated 119-row
local reports match after removing command and timing metadata.

Complete authority proof is
`nx run-many -t check:policy --outputStyle=static`: ten owner-local batches plus
the exact hygiene and boundaries leaves execute under one scheduler. Its current
result is three failing executed rules with five product diagnostics, one docs
advisory, and no selector, dependency, execution, provider, hygiene, or boundary
refusal. Every source-executed Habitat target hashes the root `habitatRuntime`
named input, so provider and service changes invalidate non-Habitat owner caches;
an explicit miss, population, and hit reproduced the same passing artifact-rule
result. Do not construct a second dependency environment merely to rerun the
graph.

## R4 And R6 Current Evidence

The current census establishes:

- 101 operation roots and zero missing `contract.ts` files;
- 60 operation-local `types.ts` files;
- seven operation-local `policy/` directories;
- 57 `rules/` directories, four without `index.ts`;
- 92 `strategies/` directories, 83 exactly `default.ts` plus `index.ts`;
- nine operations without `strategies/`;
- zero operation-local `artifacts.ts` files;
- six contract description gaps, owned outside A.2;
- 506 strategy import specifiers and three real private-boundary anomalies;
- the bare `mountain` token is a prose/comment parsing artifact;
- the Standard recipe `__type_tests__/` and `artifacts.ts` surfaces are
  exterior to domain-operation topology.

The selected descent ledger carries the actionable grouping. A fresh advisory
run on the A.2 sibling must re-derive exact file/clause rows before mutation.

## Split Gate

The common parent is ready for ordinary sibling Graphite children only when:

1. the sealed R2 bad and clean fixture proof remains intact and its current-tree
   owner rule passes;
2. live manifest and active-ledger ids have exact parity;
3. all ten owner-local batches and both exact graph leaves terminate through one
   native Nx graph, and every red task is classified from task identity and
   typed Habitat disposition;
4. current Habitat checks, tests, build, and boundaries run through correctly
   owned Nx graphs;
5. generated execution-surface artifacts are current;
6. the nine descent documents preserve the state model and sealed D1-D4;
7. the tree and Graphite stack are clean.

The split base contains no domain-operation authority implementation, source
migration, live violation corpus, execution authorization, or unresolved
Habitat capability decision.

After the gate, create A.2 and independent product work as sibling Graphite
children. Move a continuation branch only when its code gains an actual A.2
dependency.

## Non-Claims

This plan does not authorize the descent, choose implementation slices, define
agent prompts, or prove product runtime behavior. It guards the clean split
transition from current capability authority into the already-settled descent
frame.
