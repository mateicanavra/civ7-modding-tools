# Domain-Operation Interior Row Ledger Seed

Status: refreshed current-tree seed; not an execution corpus

Owner: domain-operation interior descent

Purpose: preserve the bounded evidence that selected this descent and make the
first advisory census falsifiable. Counts and pointers below are dated source
facts, not authority. The asserted target and D1-D4 rulings live in `frame.md`
and `decisions/`.

## Staleness Rule

The A.2 sibling re-derives every class after positive authority is installed in
advisory mode. A pointer that no longer reproduces closes as stale. A new class
without a destination under the settled law stops that class for authority
judgment; it is not silently normalized.

## Refreshed Terrain

| Fact | Current value | Meaning |
| --- | ---: | --- |
| operation roots | 101 | all have `contract.ts` |
| operation-local `types.ts` | 60 | outside the target topology |
| operation-local `policy/` | 7 | outside the target topology |
| `rules/` directories | 57 | four lack `index.ts` |
| `strategies/` directories | 92 | 83 are exactly `default.ts` plus `index.ts` |
| operations without `strategies/` | 9 | red under sealed D1 |
| operation-local `artifacts.ts` | 0 | historical sentry pressure only |
| strategy import specifiers | 506 | three confirmed private-boundary anomalies |
| contract description gaps | 6 | package-owned and exterior to A.2 |

The bare `mountain` token from an earlier import census was prose/comment
parsing, not a module specifier. Standard recipe `__type_tests__/` and
`artifacts.ts` are recipe-owned exterior surfaces and do not affect this
ledger.

## A. Proxy-Rule Rows

| Row | Existing rule | Settled authority disposition |
| --- | --- | --- |
| A1 | `require_ecology_canonical_op_module_topology` | generic operation topology replaces the ecology-only open proxy after survivor proof |
| A2 | `prohibit_domain_artifacts_modules` | closed operation and nested-directory grammar absorbs the catchall sentry after its injected probe fails the survivor |
| A3 | `prohibit_foundation_strategy_nonlocal_imports` | generic positive dependency authority replaces the Foundation-only negative allowlist after bad and clean samples pass |
| A4 | `prohibit_foundation_rules_tectonics_shim_reexports` | export-only, local-source nested indices absorb the absent shim shape after survivor proof |
| A5 | `validate_ecology_op_contract_quality` | retired: generic MapGen Grit authority owns declaration-site JSDoc; current schema descriptions were repaired semantically; the Ecology-only source proxy and stale recipe path have no survivor |
| A6 | `preserve_decomposed_foundation_contract_surfaces` | split operation inventory and strategy-import clauses into generic owners; keep artifact and projection concerns exterior |
| A7 | `prohibit_cross_op_runtime_calls` | replace the under-scoped runtime-call proxy after generic private cross-import authority proves fixture and injected parity |

Rule deletion and retirement mutate the shared cleanup ledger at state 11.
This descent links that mutation; it does not copy the active rule corpus.

## B. Confirmed Dependency Anomalies

The sealed generic law forbids private cross-domain and private
sibling-operation reaches. It asserts capability classes rather than a path
inventory.

| Row | Current pointer | Boundary failure |
| --- | --- | --- |
| B1 | `placement/ops/plan-starts/strategies/default.ts` imports `../../../../hydrology/index.js` | cross-domain reach bypasses an admitted public capability |
| B2 | `placement/ops/plan-natural-wonders/strategies/default.ts` imports `@mapgen/domain/hydrology/model/policy/river-class.js` | cross-domain private model reach |
| B3 | `ecology/ops/plot-effects-score-snow/strategies/default.ts` imports `../../plan-plot-effects/rules/index.js` | sibling-operation private reach |

The violation class is settled. Exact semantic remediation is derived from the
consuming contract and owner authority during row classification. If no
behavior-preserving destination already exists, the row is not mechanical and
must stop before execution.

## C. Topology Red Groups

### C1: Operation-local `types.ts`

Sixty operation roots contain `types.ts`. The target has no operation-root
type bucket. Advisory enforcement must expose each file; classification must
preserve exported types, consumers, and public import behavior before any move.

### C2: Operation-local `policy/`

Seven policy directories are outside the target:

- `ecology/ops/compute-feature-substrate/policy/`
- `ecology/ops/features-plan-ice/policy/`
- `ecology/ops/features-plan-reefs/policy/`
- `ecology/ops/features-plan-vegetation/policy/`
- `ecology/ops/features-plan-wetlands/policy/`
- `placement/ops/plan-starts/policy/`
- `resources/ops/select-resource-sites/policy/`

Policy behavior belongs in a named rule, named strategy, or an exterior owner
already admitted by the contracts. The settled topology does not preserve a
policy directory as an exception.

### C3: Rules aggregation

Four `rules/` directories lack the required export-only `index.ts`:

- `foundation/ops/compute-plates-tensors/rules/`
- `morphology/ops/compute-belt-drivers/rules/`
- `morphology/ops/plan-foothills/rules/`
- `morphology/ops/plan-ridges/rules/`

An empty `rules/index.ts` is valid when no named rule exists. Named rule files,
when present, are local leaves and the index exports only those leaves.

### C4: Required strategy containers

Nine Foundation operations lack `strategies/` despite declaring strategy
envelopes:

- `compute-crust-evolution`
- `compute-crust`
- `compute-mantle-forcing`
- `compute-mantle-potential`
- `compute-mesh`
- `compute-plate-graph`
- `compute-plate-motion`
- `compute-plates-tensors`
- `compute-tectonic-segments`

Sealed D1 requires a named strategy and export-only `strategies/index.ts` for
each operation. Inline root behavior is not an alternate valid shape. Any case
that cannot be wrapped without behavior or import change is law back-talk.

### C5: Root assembly and other catchalls

The target also excludes root behavior and helper/schema/data catchalls. Their
exact red surface is intentionally not inferred from filenames here; advisory
authority must produce the current file/clause rows.

## D. Exterior Contract Metadata Evidence

The six current `description:` gaps are:

- `foundation/ops/compute-hotspot-events/contract.ts`
- `foundation/ops/compute-segment-events/contract.ts`
- `foundation/ops/compute-tectonic-provenance/contract.ts`
- `foundation/ops/compute-tectonics-current/contract.ts`
- `foundation/ops/compute-tracer-advection/contract.ts`
- `placement/ops/plan-wonders/contract.ts`

They remain evidence for package-owned verification, not A.2 migration rows.
Their presence must not reopen the topology target or resurrect the mixed
ecology validator.

## Row Visibility And First Use

Grouping here does not hide future obligations. The advisory run expands every
hit to a file/clause/predicate row, and source-moving rows expand further to
behavior-bearing definitions and consumer evidence. Execution is legal only
after every row has a deterministic, behavior-preserving destination under
the settled law.

This seed contains no authorization, completion claim, or substitute for the
first live advisory census.
