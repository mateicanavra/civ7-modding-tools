# Context

Status: packet context artifact

## Current Frame

Container: Habitat authority activation. `.habitat` is the authored authority
plane; `tools/habitat` is execution mechanics.

Nested container: closed-structure domain blueprint work. Positive target shape
is defined first, then red current paths are burned down only into already
named destinations.

Current stage: prework decision qualification before source movement or
enforcement.

Exact step: row-level disposition for
`mods/mod-swooper-maps/src/domain/foundation/lib/**`.

## User Intent

Run one reusable prework decision packet for `Foundation lib/ / Tectonics
Disposition`, using the packet skeleton and inventory item. The work should
produce a portable, reviewable decision artifact and write the result back to
the owning workstream references. It should not perform implementation movement
unless explicitly converted into an implementation slice.

## In Frame

- Current foundation `lib/**` source files as evidence.
- Foundation operation consumers and downstream recipe/stage consumers.
- Active domain blueprint scope and decision-book owner classes.
- `packages/mapgen-core` as a possible owner only where source is pure
  reusable mechanics with no foundation semantics.
- Exact row dispositions and resolved dominoes.

## Out Of Frame

- Source movement.
- Source deletion.
- Grit rule authoring.
- `structure.toml` edits.
- Runtime behavior changes.
- Generic replacement buckets such as `foundation/model/policy/misc.ts` or
  `packages/mapgen-core/src/lib/helpers.ts`.

## Domino Logic

This packet makes positive assertions about where foundation `lib/**` material
can legally land before any red-path burn-down begins. That preserves leverage:
future enforcement can intentionally turn the current `lib/**` shape red, then
mechanically burn each row down into a destination already mapped here.

This is one domain-blueprint decision, but the pattern is meant to repeat for
multiple blueprints until the codebase has enforceable authority over what
kinds of software objects can be built, where they live, and which owners can
claim them.
