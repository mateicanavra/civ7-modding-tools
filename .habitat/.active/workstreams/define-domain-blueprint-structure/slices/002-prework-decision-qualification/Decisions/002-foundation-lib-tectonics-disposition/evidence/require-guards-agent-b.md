# Require Guards Agent B Destination Classification

Status: investigation evidence

Scope: operation-local destination classification for
`mods/mod-swooper-maps/src/domain/foundation/lib/require.ts` guards. This file
does not edit TypeScript source, tests, generated artifacts, Grit,
`structure.toml`, or runtime behavior.

Evidence used:

- Narsil repo `civ7-modding-tools#2fa31857` was indexed with git and call graph
  enabled; `find_references` matched the supplied Agent A consumer matrix for
  source consumers.
- `rg` over `mods/mod-swooper-maps/src/domain/foundation` corroborated direct
  imports and wrapper-mediated strategy calls.
- Current operation layouts were inspected under
  `mods/mod-swooper-maps/src/domain/foundation/ops/**/{contract.ts,index.ts,rules,index.ts,strategies/default.ts}`.
- `mods/mod-swooper-maps/project.json` defines `test` and `check` targets.
  `nx show project mod-swooper-maps --json` could not run in this worktree
  because the Habitat Nx plugin failed to load
  `tools/habitat/dist/service/model/rules/index.js`.

Proof shorthand:

- `localize-proof`: add characterization coverage for the localized guard
  semantics, run
  `rg -n "<guardName>" mods/mod-swooper-maps/src/domain/foundation mods/mod-swooper-maps/test`,
  then `nx run mod-swooper-maps:test` and
  `nx run mod-swooper-maps:check`.
- `wrapper-proof`: run
  `rg -n "<wrapperName>|<guardName>" mods/mod-swooper-maps/src/domain/foundation mods/mod-swooper-maps/test`,
  then `nx run mod-swooper-maps:test` and
  `nx run mod-swooper-maps:check`.

| Guard | Operation id | Existing wrapper status | Local destination | Action | State-space outcome | Future proof |
| --- | --- | --- | --- | --- | --- | --- |
| `requireMesh` | `foundation/compute-plates-tensors` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plates-tensors/index.ts` | Duplicate the mesh precondition at the run boundary and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-tectonic-segments` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-tectonic-segments/index.ts` | Duplicate the mesh precondition at the run boundary and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-crust` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-crust/index.ts` | Duplicate the mesh precondition at the run boundary and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-plate-graph` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plate-graph/index.ts` | Duplicate the mesh precondition at the run boundary and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-mantle-potential` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-mantle-potential/index.ts` | Duplicate the mesh precondition at the run boundary and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-mantle-forcing` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-mantle-forcing/index.ts` | Duplicate the mesh precondition at the run boundary and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-plate-motion` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plate-motion/index.ts` | Duplicate the mesh precondition at the run boundary and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-crust-evolution` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-crust-evolution/index.ts` | Duplicate the mesh precondition at the run boundary and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-era-plate-membership` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-era-plate-membership/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-tectonic-provenance` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-tectonic-provenance/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-segment-events` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-segment-events/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-era-tectonic-fields` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-era-tectonic-fields/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-hotspot-events` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-hotspot-events/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requireMesh` |
| `requireMesh` | `foundation/compute-tracer-advection` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-tracer-advection/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requireMesh` |
| `requireCrust` | `foundation/compute-plates-tensors` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plates-tensors/index.ts` | Duplicate the crust precondition using this op's `mesh.cellCount` and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireCrust` |
| `requireCrust` | `foundation/compute-tectonic-segments` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-tectonic-segments/index.ts` | Duplicate the crust precondition using this op's `mesh.cellCount` and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireCrust` |
| `requireCrust` | `foundation/compute-plate-graph` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plate-graph/index.ts` | Duplicate the crust precondition using this op's `mesh.cellCount` and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireCrust` |
| `requireCrust` | `foundation/compute-crust-evolution` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-crust-evolution/index.ts` | Duplicate the `crustInit` precondition using this op's `cellCount` and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireCrust` |
| `requireCrust` | `foundation/compute-segment-events` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-segment-events/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requireCrust` |
| `requireMantlePotential` | `foundation/compute-mantle-forcing` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-mantle-forcing/index.ts` | Duplicate the mantle-potential precondition using this op's `mesh.cellCount` and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireMantlePotential` |
| `requireMantleForcing` | `foundation/compute-crust` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-crust/index.ts` | Duplicate the mantle-forcing precondition using this op's `mesh.cellCount` and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireMantleForcing` |
| `requireMantleForcing` | `foundation/compute-plate-motion` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plate-motion/index.ts` | Duplicate the mantle-forcing precondition using this op's `mesh.cellCount` and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requireMantleForcing` |
| `requireMantleForcing` | `foundation/compute-hotspot-events` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-hotspot-events/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requireMantleForcing` |
| `requireMantleForcing` | `foundation/compute-tracer-advection` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-tracer-advection/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requireMantleForcing` |
| `requirePlateGraph` | `foundation/compute-plates-tensors` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plates-tensors/index.ts` | Duplicate the plate-graph precondition using this op's `mesh.cellCount` and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requirePlateGraph` |
| `requirePlateGraph` | `foundation/compute-tectonic-segments` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-tectonic-segments/index.ts` | Duplicate the plate-graph precondition using this op's `mesh.cellCount` and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requirePlateGraph` |
| `requirePlateGraph` | `foundation/compute-plate-motion` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plate-motion/index.ts` | Duplicate the plate-graph precondition using this op's `mesh.cellCount` and remove this op's shared import. | localize without new abstraction | `localize-proof` for `requirePlateGraph` |
| `requirePlateGraph` | `foundation/compute-era-plate-membership` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-era-plate-membership/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requirePlateGraph` |
| `requirePlateGraph` | `foundation/compute-tectonic-provenance` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-tectonic-provenance/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requirePlateGraph` |
| `requirePlateMotion` | `foundation/compute-plates-tensors` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plates-tensors/index.ts` | Duplicate the plate-motion precondition using this op's `mesh.cellCount` and `plateGraph.plates.length`; remove this op's shared import. | localize without new abstraction | `localize-proof` for `requirePlateMotion` |
| `requirePlateMotion` | `foundation/compute-tectonic-segments` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-tectonic-segments/index.ts` | Duplicate the plate-motion precondition using this op's `mesh.cellCount` and `plateGraph.plates.length`; remove this op's shared import. | localize without new abstraction | `localize-proof` for `requirePlateMotion` |
| `requirePlateMotion` | `foundation/compute-era-plate-membership` | pure pass-through in `rules/index.ts`; strategy imports wrapper | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-era-plate-membership/rules/input-guards.ts` | Create op-local input guard, update strategy import to that file, and delete pass-through guard export/import from `rules/index.ts`. | delete pass-through wrapper; narrow op-local split | `wrapper-proof` for `requirePlateMotion` |
| `requireTectonics` | `foundation/compute-plates-tensors` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plates-tensors/index.ts` | Duplicate the current-tectonics precondition using this op's `mesh.cellCount`; remove this op's shared import. Flag artifact-contract helper review to Agent C, but do not invent one here. | localize without new abstraction; artifact candidate flagged | `localize-proof` for `requireTectonics` |
| `requireTectonics` | `foundation/compute-crust-evolution` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-crust-evolution/index.ts` | Duplicate the current-tectonics precondition using this op's `cellCount`; remove this op's shared import. Flag artifact-contract helper review to Agent C, but do not invent one here. | localize without new abstraction; artifact candidate flagged | `localize-proof` for `requireTectonics` |
| `requireTectonicHistory` | `foundation/compute-plates-tensors` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plates-tensors/index.ts` | Duplicate the tectonic-history precondition using this op's `mesh.cellCount`; remove this op's shared import. Flag artifact-contract helper review to Agent C, but do not invent one here. | localize without new abstraction; artifact candidate flagged | `localize-proof` for `requireTectonicHistory` |
| `requireTectonicHistory` | `foundation/compute-crust-evolution` | none; direct `index.ts` import | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-crust-evolution/index.ts` | Duplicate the tectonic-history precondition using this op's `cellCount`; remove this op's shared import. Flag artifact-contract helper review to Agent C, but do not invent one here. | localize without new abstraction; artifact candidate flagged | `localize-proof` for `requireTectonicHistory` |
| `requireTectonicProvenance` | `foundation/compute-plates-tensors` | none; direct optional `index.ts` call | `mods/mod-swooper-maps/src/domain/foundation/ops/compute-plates-tensors/index.ts` | Keep optionality at the operation boundary and duplicate the provenance precondition only inside the truthy branch; remove this op's shared import. Flag artifact-contract helper review to Agent C, but do not invent one here. | localize without new abstraction; artifact candidate flagged | `localize-proof` for `requireTectonicProvenance` |

## Open Questions

No blockers remain from operation-topology perspective. Artifact-contract helper
replacement remains the Agent C/steward authority lane for `requireTectonics`,
`requireTectonicHistory`, and `requireTectonicProvenance`; this table does not
invent those helpers and does not depend on them to name operation-local fallback
destinations.

