# Domino 001 Frame

Status: active slice frame

Domino 001 is the first closed-structure cut for the MapGen `domain` blueprint.
It covers domain roots by blueprint kind and the immediate `ops/` child
topology. This documentation slice records the law, inventory, and later
execution plan.

## Why This Slice

This slice has enough force to collapse real structural debt while preserving
deeper operation, strategy, rule, policy, and data decisions for their owning
scope passes.

Selected depth: domain root plus immediate `ops/` child set. This reaches the
first repeated structural failure below the root: helper-family folders in the
place reserved for registered operation roots.

## Scope Commitments

In:

- direct domain-root children;
- direct children of each selected domain `ops/` folder;
- registered operation ids as the selected operation folders;
- exact red-path inventory and locked write-set classes.

Foreground:

- positive allow-list first;
- selected slots carry affirmative owner roles;
- explicit forbids serve as failure messages for the positive law;
- helper-bucket names such as `semantics.ts`, `shared`, `utils`, `common`,
  `internal`, `support`, and `public` route to red inventory and owner
  classification;
- every folder under `ops/` is a registry file or a registered operation root.

Deferred:

- full grammar inside operation `policy/`, `rules/`, and `strategies/`;
- Gameplay/narrative liveness and owner-law consolidation;
- public import-routing law for downstream callers;
- live Habitat `structure.toml` and Grit packet implementation.

## Hard Core

1. The selected topology is closed and allow-list first.
2. Root `index.ts`, root `ops.ts`, and `ops/contracts.ts` carry real domain
   boundary roles.
3. Root `index.ts` is the domain contract surface; duplicate root
   `contract.ts` rows burn down through import proof.
4. Model config, model policy, model data, and artifact contracts are narrow
   owner slots with local content laws.
5. A child under `ops/` is a registry file or a registered operation root.
6. Execution completes when red paths are burned down and overlapping rule
   packets are removed, absorbed, or retained with a distinct concern.

## Reframe Conditions

Falsifier:

If the Habitat structure runner lacks an expression for closed direct-child
topology over domain roots and immediate `ops/` children, this domino reframes
around the smallest existing enforcement owner that produces the same red/green
state.

Degeneration trigger:

If implementation needs a new generic bucket to avoid deletion or owner
placement, stop. The topology is carrying debt instead of closing it.

## Mechanical Trust Condition

Domino 001 is mechanically trusted only when topology enforcement is paired
with source-shape gates for root `index.ts` and root `ops.ts`.

The source-shape gates prevent the failure mode where the structure rule deletes
root helper files but implementers paste their contents into a selected
filename to keep imports green.

## Placement And Narrative

The domain blueprint applies by kind to domain roots. `placement` and
`narrative` references in this slice are current source rows for liveness and
owner classification.

`placement` rows execute where `inventory.md` names exact destinations.
`narrative` rows feed the liveness and Gameplay/narrative owner-law prework:
current symbols are classified as live owned material, deleted material, or
later Gameplay/story-artifact rows.

## Superseded Root Slots

An earlier working grammar treated root `config.ts`, root `policy/`, and
data-only root `lib/` as transitional direct slots. The selected target law now
uses `model/config/`, `model/policy/`, and `model/data/`. Domino 001 treats old
root config/policy/lib placements as source evidence and red inventory.

## Rule Packet Collapse Candidates

After live enforcement exists, these candidates can be absorbed or deleted only
after scan-root and failure-condition parity are proven:

- `prohibit_domain_entrypoint_self_reexports`
- `prohibit_domain_artifacts_modules`
- `prohibit_retired_domain_root_catalogs`

These candidates stay distinct unless the new gates prove the same concern:

- `require_public_domain_surfaces_in_recipes_and_maps`
- `require_public_domain_surfaces_in_tests`
- `prohibit_recipe_imports_in_domain_source`
- `prohibit_unknown_bag_config_usage`
