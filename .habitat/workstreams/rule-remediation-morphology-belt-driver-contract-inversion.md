# Rule Remediation: Morphology Belt-Driver Contract Inversion

Status: closed on `codex/habitat-morphology-belt-driver-contract-inversion`

Canonical record:
`.habitat/workstreams/rule-remediation-layer1-action-matrix.json`

## Purpose

Close the morphology contract legacy plate-driver dependency row by absorbing
its negative token predicate into the existing positive belt-driver contract
authority.

## Decision

`prohibit_morphology_contract_legacy_plate_driver_dependencies` covered the
same two morphology contract files as `preserve_morphology_belt_driver_contracts`.
The durable authority is the positive contract-currentness rule: these
contracts must require the canonical foundation/belt-driver inputs and must not
reintroduce retired plate-driver dependency names.

The separate Grit rule was therefore retired after the survivor script absorbed
the retired-token absence check.

## Disposition Receipt

| Rule id | Action | Reason |
| --- | --- | --- |
| `preserve_morphology_belt_driver_contracts` | preserved and expanded | Owns the exact morphology belt-driver contract surface for the same two files. |
| `prohibit_morphology_contract_legacy_plate_driver_dependencies` | retired/deleted | Absorbed by the positive contract-currentness survivor. |

## Proof Scope

The survivor Habitat script proves required canonical belt-driver references and
specific retired-token absence in the two morphology contract files. It does
not claim broad artifact blueprint authority or generated contract-manifest
authority.
