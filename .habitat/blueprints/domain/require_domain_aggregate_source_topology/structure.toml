schemaVersion = 1

[[scopes]]
name = "mapgen-domain-aggregate-roots"
root = "mods/*/src/domain/!(*.*)"
kind = "directory"
mode = "closed"
required = [
  "index.ts",
  "contract.ts",
  "router.ts",
]
allowed = [
  "!(*.*)",
]
