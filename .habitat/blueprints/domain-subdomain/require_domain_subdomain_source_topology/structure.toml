schemaVersion = 1

[[scopes]]
name = "mapgen-domain-subdomain-roots"
root = "plugins/mod/map/*/src/domain/!(*.*)/modules/!(*.*)"
kind = "directory"
mode = "closed"
required = [
  "index.ts",
  "contract.ts",
  "router.ts",
  "ops",
]
allowed = [
  "artifacts",
  "model",
]
