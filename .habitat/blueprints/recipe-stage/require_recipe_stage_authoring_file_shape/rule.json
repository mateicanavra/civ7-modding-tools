{
  "schemaVersion": 2,
  "id": "require_recipe_stage_authoring_file_shape",
  "title": "Require Recipe Stage Authoring File Shape",
  "placement": {
    "niche": "civ7/mapgen/pipeline",
    "blueprint": "recipe-stage",
    "category": "contract"
  },
  "operation": {
    "kind": "check"
  },
  "ownerProject": "swooper-physics",
  "lane": "enforced",
  "forbids": "recipe stage index modules without exactly one canonical default createStage authority, with additional runtime exports, with deep operation-member access, with a detached full public override, with a compiler that has no inline public boundary to translate, or with an obvious empty, identity, or spread-only compiler wrapper",
  "why": "A recipe stage index.ts owns one public runtime stage identity, step selection, and any exceptional semantic public override. Ordinary authoring configuration flows from step schemas, bound operation configuration, and stage knobs. Requiring an intentional full override to be visibly authored inline with the stage prevents inherited or detached configuration authorities. Cheap syntax checks reject only unmistakable no-op compiler wrappers; review determines whether a nontrivial public-to-internal transform is semantically justified.",
  "remediate": "Default-export exactly one createStage value with id and steps, remove detached public-config assemblies and compiler-only wrappers, let step and operation schemas expose ordinary configuration, inline any justified Type.Object public override beside a compiler that meaningfully changes the public shape, apply ordinary knobs during step normalization, consume operation contracts through public domain roots, and remove every additional runtime export or re-export.",
  "message": "Recipe stage index.ts must expose only its canonical default createStage runtime authority.",
  "pathCoverage": [
    {
      "kind": "exact-path",
      "patterns": [
        "plugins/mod/map/*/src/recipes/*/stages/*(!(*.*|steps|artifacts|model)/)!(*.*|steps|artifacts|model)/index.ts"
      ]
    }
  ],
  "hookCheck": true,
  "supportFiles": {
    "baseline": ".habitat/blueprints/recipe-stage/require_recipe_stage_authoring_file_shape/baseline.json"
  },
  "runner": {
    "name": "grit",
    "files": {
      "pattern": ".habitat/blueprints/recipe-stage/require_recipe_stage_authoring_file_shape/pattern.md"
    },
    "patternName": "require_recipe_stage_authoring_file_shape",
    "acquisition": {
      "kind": "check",
      "roots": ["plugins/mod/map"]
    }
  }
}
