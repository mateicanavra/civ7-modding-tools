schemaVersion = 1

[[scopes]]
name = "mapgen-domain-operation-strategy-roots"
root = "mods/*/src/domain/!(*.*)/ops/!(*.*)/strategies"
kind = "directory"
mode = "closed"
required = [
  "index.ts",
]
allowed = [
  "+([a-z0-9])*(-+([a-z0-9])).ts",
]
forbidden = [
  "default.ts",
]
