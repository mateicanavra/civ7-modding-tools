{
  "schemaVersion": 2,
  "id": "require_artifact_file_shape",
  "title": "Require Artifact File Shape",
  "placement": {
    "niche": "civ7/mapgen",
    "blueprint": "artifact",
    "category": "contract"
  },
  "operation": {
    "kind": "check"
  },
  "ownerProject": "swooper-physics",
  "lane": "enforced",
  "forbids": "artifact owner files outside their canonical one-object authority, closed dependency, export, and admission shape",
  "why": "Artifact owners define immutable MapGen data products. Exact-file acquisition lets Grit require one exported artifact = defineArtifact(...) authority without searching unrelated mod source. The artifact owns its complete payload schema and optional relational refinement inline; imported atoms may contribute smaller primitives and cohesive subentities but never replace that root container. Core constructs structural validation and composes the refinement into the same authority.",
  "remediate": "In a domain module's artifacts/*.artifact.ts member, import only the canonical defineArtifact/Type authoring surface plus optional Static and TypedArraySchemas, public Core lib helpers, Civ7 types or policy, and model atoms or policy from that exact module or its owning domain. Export only artifact = defineArtifact({ name, id, schema: Type.*(...), refine?: (...) => ... }) plus erased TypeScript types. Keep the root schema and optional refinement inline; compose model atoms only as smaller schema parts.",
  "message": "Artifact owner files must expose one canonical artifact authority with a closed dependency and admission shape.",
  "pathCoverage": [
    {
      "kind": "exact-path",
      "patterns": ["plugins/mod/map/*/src/domain/*/modules/*/artifacts/*.artifact.ts"]
    }
  ],
  "hookCheck": true,
  "supportFiles": {
    "baseline": ".habitat/blueprints/artifact/require_artifact_file_shape/baseline.json"
  },
  "runner": {
    "name": "grit",
    "files": {
      "pattern": ".habitat/blueprints/artifact/require_artifact_file_shape/pattern.md"
    },
    "patternName": "require_artifact_file_shape",
    "acquisition": {
      "kind": "check",
      "roots": ["plugins/mod/map"]
    }
  }
}
