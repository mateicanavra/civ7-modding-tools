{
  "schemaVersion": 2,
  "id": "require_typed_recipe_step_dependencies",
  "title": "Require Typed Recipe Step Dependencies",
  "placement": {
    "niche": "civ7/mapgen/pipeline",
    "blueprint": "recipe-step",
    "category": "contract"
  },
  "operation": {
    "kind": "check"
  },
  "ownerProject": "swooper-physics",
  "lane": "enforced",
  "forbids": "string literal entries in recipe-step requires or provides lists",
  "why": "Recipe steps select exact Artifact authorities or owned typed CompletionId constants so dependency identity cannot drift from its owner.",
  "remediate": null,
  "message": "Select the exact Artifact authority or an owned typed CompletionId constant instead of a string literal.",
  "pathCoverage": [
    {
      "kind": "exact-path",
      "patterns": ["plugins/mod/map/*/src/recipes/*/stages/**/steps/*/config.ts"]
    }
  ],
  "hookCheck": true,
  "supportFiles": {
    "baseline": ".habitat/blueprints/recipe-step/require_typed_recipe_step_dependencies/baseline.json"
  },
  "runner": {
    "name": "grit",
    "files": {
      "pattern": ".habitat/blueprints/recipe-step/require_typed_recipe_step_dependencies/pattern.md"
    },
    "patternName": "require_typed_recipe_step_dependencies",
    "acquisition": {
      "kind": "check",
      "roots": ["plugins/mod/map"]
    }
  }
}
