{
  "$schema": "https://json-schema.org/schema",
  "$id": "HabitatProjectGenerator",
  "title": "Habitat project generator",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Project slug without workspace prefix.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "kind": {
      "type": "string",
      "description": "Supported uniform project kind.",
      "enum": [
        "app",
        "foundation",
        "plugin",
        "kind:app",
        "kind:foundation",
        "kind:plugin"
      ]
    },
    "packageName": {
      "type": "string",
      "description": "Optional package name override."
    },
    "directory": {
      "type": "string",
      "description": "Optional repo-relative project root override."
    }
  },
  "required": ["name", "kind"]
}
