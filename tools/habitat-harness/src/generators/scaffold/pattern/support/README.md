# Pattern Generator Schema

HABITAT TODO: `schema.json` moved to
`.habitat/tooling/components/generator-schemas/scaffold/pattern/schema.json`.
`tools/habitat-harness/generators.json` now points at the Habitat authority
copy. This package-local directory should keep only generator implementation
source.
