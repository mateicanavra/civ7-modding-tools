import habitatConfidence from "./habitat-confidence/index.js";

/** Chooses the strongest wetland-family habitat per unoccupied land tile after substrate-specific scoring. Implementations available to the recipe's semantic strategy selection. */
export default [habitatConfidence] as const;
