import warmHumid from "./warm-humid/index.js";

/** Scores warm humid closed-canopy habitat from energy, water, stress, and biomass evidence. Implementations available to the recipe's semantic strategy selection. */
export default [warmHumid] as const;
