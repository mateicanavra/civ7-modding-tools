import warmShallowLake from "./warm-shallow-lake/index.js";

/** Scores warm shallow lake water near shore for lake-lotus habitat. Implementations available to the recipe's semantic strategy selection. */
export default [warmShallowLake] as const;
