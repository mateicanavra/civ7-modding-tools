import temperateHumid from "./temperate-humid/index.js";

/** Scores temperate humid forest habitat from energy, water, stress, biomass, and fertility evidence. Implementations available to the recipe's semantic strategy selection. */
export default [temperateHumid] as const;
