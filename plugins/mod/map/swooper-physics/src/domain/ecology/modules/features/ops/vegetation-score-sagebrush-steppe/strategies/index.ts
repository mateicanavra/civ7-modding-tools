import semiaridOpen from "./semiarid-open/index.js";

/** Scores semiarid open vegetation from energy, water stress, and biomass evidence. Implementations available to the recipe's semantic strategy selection. */
export default [semiaridOpen] as const;
