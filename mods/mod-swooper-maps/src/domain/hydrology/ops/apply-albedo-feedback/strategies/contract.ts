import boundedSnowIce from "./bounded-snow-ice/contract.js";

/** Bounded snow-and-ice iteration is the sole albedo posture so the feedback remains stable. */
export default [boundedSnowIce] as const;
