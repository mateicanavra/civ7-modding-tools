import temperatureThresholds from "./temperature-thresholds/index.js";

/** Temperature thresholds are the sole cryosphere posture so frozen-state classifications remain mutually coherent. */
export default [temperatureThresholds] as const;
