import bioclimaticSubstrate from "./bioclimatic-substrate/index.js";

/** Normalizes climate and soil evidence into shared energy, water, stress, biomass, and fertility fields used by vegetation scorers. Implementations available to the recipe's semantic strategy selection. */
export default [bioclimaticSubstrate] as const;
