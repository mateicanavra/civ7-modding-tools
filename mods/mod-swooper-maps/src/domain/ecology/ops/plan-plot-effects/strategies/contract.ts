import rankedCoverage from "./ranked-coverage/contract.js";

/** Ranks snow, sand, burned, and jungle suitability into deterministic coverage budgets and optional hazard intent. Strategy contracts vary authored policy without redefining the operation input or output. */
export default [rankedCoverage] as const;
