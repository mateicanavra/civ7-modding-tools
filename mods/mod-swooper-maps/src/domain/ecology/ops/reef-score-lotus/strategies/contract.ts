import warmShallowLake from "./warm-shallow-lake/contract.js";

/** Scores warm shallow lake water near shore for lake-lotus habitat. Strategy contracts vary authored policy without redefining the operation input or output. */
export default [warmShallowLake] as const;
