import gridCellSummary from "./grid-cell-summary/contract.js";

/** Aggregates tile-level soil and fertility evidence into stable grid-cell summaries for downstream inspection. Strategy contracts vary authored policy without redefining the operation input or output. */
export default [gridCellSummary] as const;
