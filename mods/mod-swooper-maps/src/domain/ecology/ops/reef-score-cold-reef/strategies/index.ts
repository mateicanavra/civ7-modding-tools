import coldShelf from "./cold-shelf/index.js";

/** Scores cold shelf water within authored temperature, depth, and coast-distance windows. Implementations available to the recipe's semantic strategy selection. */
export default [coldShelf] as const;
