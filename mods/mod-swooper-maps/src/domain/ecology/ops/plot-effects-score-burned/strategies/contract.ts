import aridThermal from "./arid-thermal/contract.js";

/** Scores hot, arid, sparse, unfrozen land in admitted biomes for burned plot-effect intent. Strategy contracts vary authored policy without redefining the operation input or output. */
export default [aridThermal] as const;
