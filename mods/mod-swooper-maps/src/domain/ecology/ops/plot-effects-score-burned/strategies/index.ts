import aridThermal from "./arid-thermal/index.js";

/** Scores hot, arid, sparse, unfrozen land in admitted biomes for burned plot-effect intent. Implementations available to the recipe's semantic strategy selection. */
export default [aridThermal] as const;
