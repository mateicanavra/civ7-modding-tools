import gaussian from "./gaussian/contract.js";

/** Smooths land-biome boundaries over the hex grid while retaining the water sentinel unchanged. Strategy contracts vary authored policy without redefining the operation input or output. */
export default [gaussian] as const;
