import habitatConfidence from "./habitat-confidence/contract.js";

/** Chooses the strongest wetland-family habitat per unoccupied land tile after substrate-specific scoring. Strategy contracts vary authored policy without redefining the operation input or output. */
export default [habitatConfidence] as const;
