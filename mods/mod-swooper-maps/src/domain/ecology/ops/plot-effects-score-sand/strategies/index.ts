import aridThermal from "./arid-thermal/index.js";

/** Scores warm, arid, sparse, unfrozen land in admitted biomes for sand plot-effect intent. Implementations available to the recipe's semantic strategy selection. */
export default [aridThermal] as const;
