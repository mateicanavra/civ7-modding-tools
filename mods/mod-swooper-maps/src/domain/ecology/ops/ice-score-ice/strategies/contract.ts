import thermalElevation from "./thermal-elevation/contract.js";

/** Scores sea and alpine ice suitability from temperature, elevation, freeze persistence, and land-water state. Strategy contracts vary authored policy without redefining the operation input or output. */
export default [thermalElevation] as const;
