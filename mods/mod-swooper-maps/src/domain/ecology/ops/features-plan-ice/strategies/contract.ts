import scoreThreshold from "./score-threshold/contract.js";

/** Converts freeze suitability into sparse ice intent without claiming reserved or occupied tiles. Strategy contracts vary authored policy without redefining the operation input or output. */
export default [scoreThreshold] as const;
