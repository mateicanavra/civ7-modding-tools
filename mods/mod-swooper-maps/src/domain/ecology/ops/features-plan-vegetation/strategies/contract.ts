import habitatConfidence from "./habitat-confidence/contract.js";

/** Chooses the strongest forest-family habitat per unoccupied land tile under family-specific confidence floors. Strategy contracts vary authored policy without redefining the operation input or output. */
export default [habitatConfidence] as const;
