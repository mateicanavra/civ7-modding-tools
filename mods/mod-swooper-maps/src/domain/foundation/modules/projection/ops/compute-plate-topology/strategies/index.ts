import wrappedHexAdjacency from "./wrapped-hex-adjacency/index.js";

/** Complete executable strategy tuple for deriving plate adjacency on the wrapped tile grid. */
export default [wrappedHexAdjacency] as const;
