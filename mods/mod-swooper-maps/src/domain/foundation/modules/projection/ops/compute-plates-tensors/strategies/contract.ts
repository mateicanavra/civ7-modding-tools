import foundationModelProjection from "./foundation-model-projection/contract.js";

/** Complete strategy-contract tuple admitted by the shared plate-projection operation contract. */
export default [foundationModelProjection] as const;
