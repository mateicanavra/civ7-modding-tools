/** Minimum era count that preserves a useful tectonic reconstruction timeline. */
export const ERA_COUNT_MIN = 5;
/** Maximum era count admitted before reconstruction cost outweighs additional temporal detail. */
export const ERA_COUNT_MAX = 8;
