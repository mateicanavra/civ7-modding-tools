Removed Nx architecture targets retained as migration canaries.

These target names were removed from project.json because they encoded hidden
authority or package-test slices as standalone graph targets. Package `test`
still runs the underlying tests where they remain package-local, and
`habitat:check` now owns the migrated static guardrail checks.

Next domino: embedded hidden authority migration should inspect each canary and
either migrate the structural oracle into `.habitat` or explicitly leave the
runtime/product proof package-local.

- test:architecture-cutover
- test:architecture-rng-authority
- test:architecture-static-guardrails
- test:architecture-ecology-step-imports
- test:architecture-m11-projection-band
- test:architecture-map-bundle-runtime-imports
